# PAGA 轨迹分析脚本说明文档

## 📋 脚本概述

**脚本名称：** `7.paga_trajectory.py`

**分析工具：** PAGA (Partition-based Graph Abstraction)

**主要功能：** 基于图抽象的拓扑轨迹推断，构建细胞类型间的连接网络

---

## 🎯 为什么使用 PAGA？

### PAGA 的独特优势

| 特性 | 说明 | 优势 |
|------|------|------|
| **拓扑结构** | 构建细胞类型间的抽象图 | 清晰展示发育路径 |
| **连接强度** | 量化细胞类型间的连接 | 识别主要/次要发育路径 |
| **分支检测** | 自动识别分支点和环状结构 | 适合复杂轨迹 |
| **计算效率** | 基于聚类的抽象 | 快速处理大数据集 |
| **可解释性** | 直观的图可视化 | 易于生物学解释 |

### 与其他方法的对比

| 方法 | Monocle3 | Slingshot | **PAGA** ⭐ |
|------|----------|-----------|----------|
| 算法 | 图学习 | 主曲线 | **图抽象** |
| 输入 | 完整矩阵 | 降维+聚类 | **降维+聚类** |
| 速度 | 慢 | 快 | **非常快** ⚡ |
| 拓扑 | 复杂 | 简单树状 | **任意拓扑** ⭐ |
| 可视化 | 轨迹线 | 轨迹曲线 | **连接图** ⭐ |
| 适用场景 | 复杂分支 | 简单轨迹 | **任意轨迹** |

---

## 🔧 核心功能

### 1. 三种UMAP空间完整支持 ⭐

所有分析和可视化支持三种UMAP空间：

#### RNA UMAP
- **数据来源：** 基因表达
- **适用场景：** 转录组驱动的轨迹分析
- **文件命名：** `*_rna_umap.png`

#### ADT UMAP ⭐
- **数据来源：** 表面蛋白
- **适用场景：** 蛋白标志物驱动的轨迹
- **文件命名：** `*_adt_umap.png`

#### WNN UMAP （推荐）⭐⭐⭐
- **数据来源：** RNA + ADT 加权整合
- **适用场景：** 多模态综合轨迹分析（最准确）
- **文件命名：** `*_wnn_umap.png`

### 2. PAGA 拓扑分析

#### 连接图构建
- 细胞类型间的连接网络
- 连接强度量化（0-1）
- 自动分支点识别

#### 发育路径推断
- 主要发育路径
- 次要/替代路径
- 环状结构检测

#### 扩散伪时间 (DPT)
- 基于扩散过程的伪时间计算
- 从早期细胞（Pre-cDC）到成熟细胞的时间估计
- 支持多起点分析

### 3. 多模式支持

```bash
# cDC1 特异分析
python 7.paga_trajectory.py --mode cDC1

# cDC2 特异分析
python 7.paga_trajectory.py --mode cDC2

# 整合分析
python 7.paga_trajectory.py --mode integrated
```

---

## 📥 输入数据

### 数据来源

来自 `4.downstream_analysis.Rmd` 的输出：

#### 方法1：直接从 Seurat 对象加载（推荐）

```bash
python 7.paga_trajectory.py --mode cDC1 \
  --input results/cDC1/Robjects/seurat_obj_annotated.rds
```

**优势：**
- ✅ 完整的基因表达数据
- ✅ 所有降维结果（PCA、UMAP）
- ✅ 完整的metadata
- ✅ 支持基因差异分析

**要求：**
- 需要安装 `rpy2` 包：`pip install rpy2`

#### 方法2：从 CSV 文件加载

```bash
python 7.paga_trajectory.py --mode cDC1 \
  --data-dir results/cDC1/Tables
```

**需要的文件：**
- `cell_metadata.csv` - 细胞元数据（必需）
- `umap_coordinates.csv` - RNA UMAP 坐标
- `adt_umap_coordinates.csv` - ADT UMAP 坐标
- `wnn_umap_coordinates.csv` - WNN UMAP 坐标

**优势：**
- ✅ 不需要 rpy2
- ✅ 文件小，加载快
- ✅ 易于在Python环境使用

**限制：**
- ❌ 没有完整基因表达数据
- ❌ 仅支持UMAP可视化

#### 方法3：从 h5ad 文件加载

```bash
python 7.paga_trajectory.py --mode cDC1 \
  --input data/cDC1_processed.h5ad
```

**适用场景：** 已经在Python中预处理过的数据

---

## 📤 输出结果

### 目录结构

```
paga_analysis_{mode}/
├── data/
│   ├── adata_processed.h5ad           # 处理后的 AnnData 对象
│   └── paga_results.pkl               # PAGA 结果（可选）
├── Plots/
│   ├── PAGA/                          # PAGA 图
│   │   ├── paga_graph_rna_umap.png    # RNA UMAP 上的 PAGA 图
│   │   ├── paga_graph_adt_umap.png    # ADT UMAP 上的 PAGA 图
│   │   ├── paga_graph_wnn_umap.png    # WNN UMAP 上的 PAGA 图 ⭐
│   │   ├── paga_connectivity.png      # 连接强度热图
│   │   └── paga_paths.png             # 发育路径图
│   ├── Trajectory/                    # 轨迹可视化
│   │   ├── trajectory_rna_umap.png
│   │   ├── trajectory_adt_umap.png
│   │   └── trajectory_wnn_umap.png
│   ├── Pseudotime/                    # 伪时间可视化
│   │   ├── dpt_rna_umap.png          # 扩散伪时间 - RNA UMAP
│   │   ├── dpt_adt_umap.png          # 扩散伪时间 - ADT UMAP
│   │   └── dpt_wnn_umap.png          # 扩散伪时间 - WNN UMAP
│   └── Comparison/                    # 对比图
│       ├── umap_comparison.png        # 三种 UMAP 空间对比
│       └── trajectory_comparison.png   # 与 Monocle3 对比（可选）
└── Tables/
    ├── paga_connectivity.csv          # 细胞类型连接矩阵
    ├── dpt_pseudotime.csv             # 每个细胞的扩散伪时间
    ├── trajectory_genes.csv           # 轨迹相关基因（可选）
    └── cell_transitions.csv           # 细胞类型转换表
```

### 关键输出文件说明

#### 1. PAGA 连接图

**文件：** `Plots/PAGA/paga_graph_wnn_umap.png`

**内容：**
- 节点 = 细胞类型
- 边 = 细胞类型间的连接
- 边的粗细 = 连接强度

**解读：**
- 粗边 = 主要发育路径
- 细边 = 次要/替代路径
- 无边 = 不相关的细胞类型

**示例：**
```
Pre-cDC1s ──────> Proliferating cDC1s ──────> Early mature cDC1 ──────> Mature cDC1
     │                                                                        ↑
     └────────────────────────────────────────────────────────────────────────┘
                              (环状结构，表示成熟后可能重新进入增殖)
```

#### 2. 连接强度矩阵

**文件：** `Tables/paga_connectivity.csv`

**格式：**
```csv
,Pre-cDC1s,Proliferating cDC1s,Early mature cDC1,Mature cDC1
Pre-cDC1s,1.00,0.85,0.12,0.05
Proliferating cDC1s,0.85,1.00,0.78,0.23
Early mature cDC1,0.12,0.78,1.00,0.91
Mature cDC1,0.05,0.23,0.91,1.00
```

**解读：**
- 对角线 = 1.00（自身连接）
- 值越大 = 连接越强
- 典型阈值：> 0.1 表示有意义的连接

#### 3. 扩散伪时间

**文件：** `Tables/dpt_pseudotime.csv`

**格式：**
```csv
cell_id,cell_type,dpt_pseudotime
AAACCCAAGAAACCCA,Pre-cDC1s,0.05
AAACCCAAGAAACGCC,Proliferating cDC1s,0.32
AAACCCAAGAAATCCC,Early mature cDC1,0.68
AAACCCAAGAACAACA,Mature cDC1,0.95
```

**解读：**
- 值范围：0 - 1
- 0 = 最早期细胞（起点）
- 1 = 最晚期细胞（终点）
- 可用于下游基因表达动态分析

#### 4. 细胞类型转换表

**文件：** `Tables/cell_transitions.csv`

**格式：**
```csv
source,target,connectivity
Pre-cDC1s,Proliferating cDC1s,0.85
Proliferating cDC1s,Early mature cDC1,0.78
Early mature cDC1,Mature cDC1,0.91
```

**解读：**
- 按连接强度排序
- 仅包含有意义的连接（> 0.1）
- 用于识别关键转换点

---

## 🚀 使用指南

### 基础用法

#### 1. 最简单的运行方式（自动检测数据）

```bash
python 7.paga_trajectory.py --mode cDC1
```

**自动查找：**
1. `results/cDC1/Robjects/seurat_obj_annotated.rds`
2. `results/cDC1/Tables/*.csv`

#### 2. 指定输入文件

```bash
# 从 RDS 文件
python 7.paga_trajectory.py --mode cDC1 \
  --input results/cDC1/Robjects/seurat_obj_annotated.rds

# 从 CSV 文件夹
python 7.paga_trajectory.py --mode cDC1 \
  --data-dir results/cDC1/Tables

# 从 h5ad 文件
python 7.paga_trajectory.py --mode cDC1 \
  --input data/cDC1.h5ad
```

#### 3. 自定义输出目录

```bash
python 7.paga_trajectory.py --mode cDC1 \
  --output-dir my_paga_results
```

### 高级参数

#### 调整聚类分辨率

```bash
# 更细的聚类（更多细节）
python 7.paga_trajectory.py --mode cDC1 --resolution 1.5

# 更粗的聚类（更简洁）
python 7.paga_trajectory.py --mode cDC1 --resolution 0.5
```

**建议：**
- cDC1：`1.0` - `1.2`
- cDC2：`0.8` - `1.0`
- integrated：`1.5` - `2.0`

#### 选择主要 UMAP 空间

```bash
# 使用 WNN UMAP（推荐）
python 7.paga_trajectory.py --mode cDC1 --primary-umap wnn

# 使用 RNA UMAP
python 7.paga_trajectory.py --mode cDC1 --primary-umap rna

# 使用 ADT UMAP
python 7.paga_trajectory.py --mode cDC1 --primary-umap adt
```

#### 指定细胞类型列名

```bash
# 使用作者注释（默认）
python 7.paga_trajectory.py --mode cDC1 --cell-type-col author_annotation

# 使用详细注释
python 7.paga_trajectory.py --mode cDC1 --cell-type-col Annotation_detailed

# 使用聚类结果
python 7.paga_trajectory.py --mode cDC1 --cell-type-col SCT_clusters
```

---

## 📊 结果解读

### PAGA 图的生物学解释

#### cDC1 发育轨迹示例

```
Pre-cDC1s (起点)
    ↓ (强连接: 0.85)
Proliferating cDC1s (增殖期)
    ↓ (强连接: 0.78)
Early mature cDC1 (早期成熟)
    ↓ (非常强连接: 0.91)
Mature cDC1 (终点)
```

**生物学意义：**
1. **Pre-cDC1s → Proliferating cDC1s** (0.85)
   - 前体细胞进入增殖状态
   - 关键基因：细胞周期相关基因（Mki67, Top2a）

2. **Proliferating cDC1s → Early mature cDC1** (0.78)
   - 退出细胞周期，开始成熟
   - 关键基因：成熟标志物（Cd80, Cd86, H2-Aa）

3. **Early mature cDC1 → Mature cDC1** (0.91)
   - 完全成熟
   - 关键基因：功能分子（Ccr7, Il12b, Cxcl9）

#### 异常模式的识别

**1. 弱连接（< 0.3）**
- 可能表示：不直接相关的细胞类型
- 或：需要中间状态的转换

**2. 双向连接（相等的来回连接）**
- 可能表示：平衡状态
- 或：可逆的细胞状态转换

**3. 环状结构**
- 可能表示：循环过程
- 或：反馈调控机制

### 与 Monocle3/Slingshot 结果对比

#### 交叉验证策略

1. **轨迹一致性**
   - PAGA 的连接路径应该与 Monocle3/Slingshot 的轨迹一致
   - 例如：都显示 Pre-cDC1s → Proliferating → Mature

2. **伪时间相关性**
   - PAGA 的扩散伪时间 (DPT) 应该与 Monocle3 伪时间相关
   - 计算 Pearson 相关系数（应该 > 0.7）

3. **分支点验证**
   - PAGA 识别的分支点应该与其他方法一致
   - 例如：cDC1/cDC2 分化点

#### 不一致时的处理

如果三种方法结果不一致：

1. **检查数据质量**
   - QC 参数是否合适？
   - 是否有批次效应？

2. **调整参数**
   - PAGA：调整 `resolution`
   - Monocle3：调整 `min_branch_len`
   - Slingshot：调整起点细胞

3. **生物学验证**
   - 查看关键基因表达
   - 参考文献中的发育模型

---

## 🔬 下游分析建议

### 1. 轨迹相关基因识别

基于 PAGA 伪时间，识别沿轨迹动态变化的基因：

```python
import scanpy as sc

# 加载 PAGA 结果
adata = sc.read_h5ad('paga_analysis_cDC1/data/adata_processed.h5ad')

# 识别伪时间相关基因
sc.tl.rank_genes_groups(adata, groupby='cell_type', method='wilcoxon')

# 导出
result = adata.uns['rank_genes_groups']
groups = result['names'].dtype.names
for group in groups:
    genes = pd.DataFrame({
        'gene': result['names'][group],
        'logfoldchange': result['logfoldchanges'][group],
        'pvalue': result['pvals'][group],
        'pvalue_adj': result['pvals_adj'][group]
    })
    genes.to_csv(f'trajectory_genes_{group}.csv', index=False)
```

### 2. 关键转换点分析

聚焦于连接强度最高的转换点：

```python
# 读取连接矩阵
conn = pd.read_csv('paga_analysis_cDC1/Tables/paga_connectivity.csv', index_col=0)

# 找到最强连接
max_conn = conn.max().max()
source, target = np.where(conn.values == max_conn)

print(f"最强转换：{conn.index[source[0]]} → {conn.columns[target[0]]}")
print(f"连接强度：{max_conn:.2f}")

# 提取这两个细胞类型进行差异分析
```

### 3. 整合多种轨迹结果

创建综合报告，整合 PAGA、Monocle3、Slingshot 结果：

```python
import pandas as pd

# 加载三种方法的伪时间
paga_pt = pd.read_csv('paga_analysis_cDC1/Tables/dpt_pseudotime.csv')
monocle3_pt = pd.read_csv('monocle3_analysis_cDC1/Tables/pseudotime_by_cell.csv')

# 合并
combined = paga_pt.merge(monocle3_pt, on='cell_id', suffixes=('_paga', '_monocle3'))

# 计算相关性
from scipy.stats import pearsonr
corr, pval = pearsonr(combined['dpt_pseudotime'], combined['pseudotime'])

print(f"PAGA vs Monocle3 伪时间相关性：r = {corr:.3f}, p = {pval:.2e}")

# 可视化
import matplotlib.pyplot as plt
import seaborn as sns

plt.figure(figsize=(8, 6))
sns.scatterplot(data=combined, x='dpt_pseudotime', y='pseudotime',
                hue='cell_type_paga', alpha=0.6)
plt.xlabel('PAGA Pseudotime')
plt.ylabel('Monocle3 Pseudotime')
plt.title(f'Pseudotime Correlation (r = {corr:.3f})')
plt.tight_layout()
plt.savefig('pseudotime_comparison.png', dpi=300)
```

---

## ⚙️ 依赖环境

### Python 包要求

```bash
# 核心包
pip install scanpy>=1.9.0
pip install anndata>=0.8.0

# 数据处理
pip install pandas>=1.3.0
pip install numpy>=1.20.0

# 可视化
pip install matplotlib>=3.5.0
pip install seaborn>=0.11.0

# 可选：从 RDS 文件加载
pip install rpy2>=3.5.0

# 完整安装
pip install scanpy anndata pandas numpy matplotlib seaborn rpy2
```

### R 环境（仅当从 RDS 文件加载时需要）

```r
# 在 R 中安装 Seurat
install.packages("Seurat")
```

### 环境测试

```bash
# 测试 Python 包
python -c "import scanpy; print(f'scanpy version: {scanpy.__version__}')"

# 测试 rpy2（可选）
python -c "import rpy2; print(f'rpy2 version: {rpy2.__version__}')"
```

---

## ❓ 常见问题

### Q1: PAGA 和 Monocle3/Slingshot 结果差异很大怎么办？

**A:** 这是正常的，不同方法侧重点不同：

| 方法 | 侧重点 | 适用场景 |
|------|-------|---------|
| PAGA | 细胞类型间拓扑 | 识别主要发育路径 |
| Monocle3 | 单细胞水平轨迹 | 详细轨迹和分支 |
| Slingshot | 连续轨迹曲线 | 平滑轨迹可视化 |

**建议：**
1. 优先看 PAGA 的大方向是否正确
2. 用 Monocle3 验证细节
3. 用 Slingshot 辅助可视化

### Q2: 扩散伪时间没有计算成功？

**A:** 可能的原因：

1. **没有找到起点细胞类型**
   - 解决：检查细胞类型名称，确保有 "Pre-cDC1s" 或 "Pre-cDC2s"
   - 或手动指定起点

2. **数据质量问题**
   - 解决：检查 QC，过滤低质量细胞

3. **聚类太粗**
   - 解决：增加 `--resolution` 参数

### Q3: 为什么 PAGA 图上有些细胞类型没有连接？

**A:** 这是正常的，说明：

1. **真正不相关** - 例如 pDCs 和 cDC1s
2. **连接太弱** - 低于显示阈值（默认 0.1）
3. **间接连接** - 通过其他细胞类型间接相关

### Q4: 如何选择最佳的 resolution 参数？

**A:** 依据数据特点：

| 数据特点 | 建议 resolution | 原因 |
|---------|----------------|------|
| 细胞类型少（< 5） | 0.5 - 0.8 | 避免过度聚类 |
| 细胞类型适中（5-10） | 1.0 - 1.2 | 默认值通常合适 |
| 细胞类型多（> 10） | 1.5 - 2.0 | 需要更细的分群 |
| 发育阶段连续 | 0.8 - 1.0 | 保持连续性 |

**测试方法：**
```bash
# 尝试不同 resolution
for res in 0.5 0.8 1.0 1.2 1.5; do
  python 7.paga_trajectory.py --mode cDC1 \
    --resolution $res \
    --output-dir paga_res_$res
done

# 比较结果，选择最符合生物学的
```

### Q5: CSV 数据加载模式的限制？

**A:** 主要限制：

❌ **无法进行的分析：**
- 轨迹相关基因识别（需要基因表达矩阵）
- 差异表达分析
- 功能富集分析

✅ **可以进行的分析：**
- PAGA 拓扑图构建
- 扩散伪时间计算
- 细胞类型连接分析
- UMAP 可视化

**建议：** 如果需要基因分析，使用 RDS 或 h5ad 输入

---

## 📚 参考文献

### PAGA 方法论文

Wolf, F. A., Hamey, F. K., Plass, M., Solana, J., Dahlin, J. S., Göttgens, B., ... & Theis, F. J. (2019).
**PAGA: graph abstraction reconciles clustering with trajectory inference through a topology preserving map of single cells.**
*Genome biology*, 20(1), 1-9.

### Scanpy 工具

Wolf, F. A., Angerer, P., & Theis, F. J. (2018).
**SCANPY: large-scale single-cell gene expression data analysis.**
*Genome biology*, 19(1), 1-5.

### 扩散伪时间

Haghverdi, L., Büttner, M., Wolf, F. A., Buettner, F., & Theis, F. J. (2016).
**Diffusion pseudotime robustly reconstructs lineage branching.**
*Nature methods*, 13(10), 845-848.

---

## 🎉 总结

### PAGA 分析的关键要点

1. **拓扑优先** - PAGA 关注细胞类型间的连接拓扑，而非单细胞轨迹
2. **快速高效** - 适合快速探索发育路径
3. **多模态整合** - 充分利用 RNA + ADT 信息
4. **交叉验证** - 与 Monocle3/Slingshot 互相验证
5. **可解释性强** - 图可视化易于生物学解释

### 推荐分析流程

```
1. 运行 PAGA 分析
   ↓
2. 检查拓扑图，识别主要路径
   ↓
3. 与 Monocle3/Slingshot 结果对比
   ↓
4. 基于伪时间识别关键转换点
   ↓
5. 聚焦关键转换点进行差异分析
   ↓
6. 生物学验证（关键基因、功能富集）
```

### 下一步分析建议

- **已完成轨迹分析：** Monocle3, Slingshot, PAGA ✅
- **推荐下一步：** RNA Velocity（动力学分析）
- **或者：** CellOracle（基因调控网络）

**祝分析顺利！** 🚀
