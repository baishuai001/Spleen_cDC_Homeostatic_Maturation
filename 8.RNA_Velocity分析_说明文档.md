# RNA Velocity 分析脚本说明文档

## 📋 脚本概述

**脚本名称：** `8.rna_velocity.py`

**分析工具：** scVelo (RNA Velocity)

**主要功能：** 通过未剪接/剪接mRNA比率推断细胞分化的方向和速度

---

## 🎯 什么是 RNA Velocity？

### 核心原理

RNA Velocity 利用转录动力学推断细胞的未来状态：

```
未剪接 mRNA (unspliced)
      ↓ 剪接
剪接后 mRNA (spliced)
      ↓ 降解
消失
```

**关键洞察：**
- **高未剪接/剪接比** → 基因表达正在上升 ↗
- **低未剪接/剪接比** → 基因表达正在下降 ↘
- **未剪接 ≈ 剪接** → 基因表达稳定 →

通过整合成百上千个基因的动态，可以预测每个细胞的未来状态！

### 生物学意义

| 应用 | 价值 |
|------|------|
| **发育方向** | 预测细胞将向哪个方向分化 |
| **分化速度** | 量化细胞的分化速度（快/慢） |
| **稳定状态** | 识别终末分化的稳定细胞 |
| **轨迹验证** | 验证Monocle3/Slingshot/PAGA的轨迹方向 |
| **调控动力学** | 识别快速响应的调控基因 |

---

## 🔧 核心功能

### 1. 速度计算模式

#### Deterministic Mode（确定性模式）⚡
```bash
python 8.rna_velocity.py --mode cDC1 --velocity-mode deterministic
```

**特点：**
- 最快（几分钟）
- 假设稳态（steady-state）
- 适合快速探索

**适用场景：**
- 初步分析
- 数据质量检查
- 大规模数据集

#### Stochastic Mode（随机模式）⭐ 推荐
```bash
python 8.rna_velocity.py --mode cDC1 --velocity-mode stochastic
```

**特点：**
- 平衡速度和准确性
- 考虑转录噪声
- 默认模式

**适用场景：**
- 大多数分析
- 平衡准确性和效率

#### Dynamical Mode（动力学模式）🎯 最准确
```bash
python 8.rna_velocity.py --mode cDC1 --velocity-mode dynamical
```

**特点：**
- 最准确
- 拟合完整动力学模型
- 最慢（可能需要数小时）

**适用场景：**
- 最终分析
- 需要高精度
- 重要发现的验证

### 2. 三种UMAP空间支持 ⭐

所有可视化支持：
- **RNA UMAP** - 基因表达空间
- **ADT UMAP** - 蛋白表达空间
- **WNN UMAP** - 整合空间（推荐）⭐

### 3. 核心分析

#### 速度向量（Velocity Vectors）
- 每个细胞的未来方向
- 箭头指向细胞的预测未来状态

#### 速度流场（Velocity Stream）
- 平滑的发育流场可视化
- 类似"水流"的连续轨迹

#### 相位图（Phase Portraits）
- 单基因的剪接-未剪接动态
- 识别基因的调控模式

#### 潜在时间（Latent Time）
- RNA velocity 版本的"伪时间"
- 基于转录动力学的细胞年龄

---

## 📥 数据准备

### 必需步骤：Velocyto 预处理

**⚠ 重要：** RNA Velocity 需要区分未剪接和剪接的mRNA，需要先运行 velocyto 工具！

#### 步骤1：准备文件

需要：
1. BAM 文件（来自CellRanger或其他比对工具）
2. GTF 注释文件（基因组注释）
3. 重复序列掩码文件（可选但推荐）

#### 步骤2：运行 Velocyto

```bash
# 安装 velocyto
pip install velocyto

# 对于 10x 数据
velocyto run10x \
  -m mm10_rmsk.gtf \
  /path/to/sample_folder \
  /path/to/genes.gtf

# 对于普通 BAM 文件
velocyto run \
  -b barcodes.tsv \
  -o output_folder \
  -m mm10_rmsk.gtf \
  sample.bam \
  genes.gtf
```

**输出：** `sample.loom` 文件（包含剪接/未剪接计数）

**时间估计：**
- 小数据集（< 5000 cells）：30分钟 - 1小时
- 中等数据集（5000-20000 cells）：2-4小时
- 大数据集（> 20000 cells）：6-12小时

#### 步骤3：运行本脚本

```bash
python 8.rna_velocity.py \
  --mode cDC1 \
  --loom sample.loom \
  --umap results/cDC1/Tables/wnn_umap_coordinates.csv
```

---

## 📤 输出结果

### 目录结构

```
rna_velocity_{mode}/
├── data/
│   ├── adata_with_velocity.h5ad       # 完整AnnData（包含velocity）
│   └── velocity_genes.txt             # Top 100 动态基因列表
├── Plots/
│   ├── Velocity/                      # 速度可视化
│   │   ├── velocity_stream_rna_umap.png    # RNA UMAP速度流
│   │   ├── velocity_stream_adt_umap.png    # ADT UMAP速度流
│   │   ├── velocity_stream_wnn_umap.png    # WNN UMAP速度流 ⭐
│   │   ├── velocity_grid_rna_umap.png      # 网格速度（RNA）
│   │   ├── velocity_grid_adt_umap.png      # 网格速度（ADT）
│   │   ├── velocity_grid_wnn_umap.png      # 网格速度（WNN）
│   │   └── velocity_embedding_*.png        # 速度嵌入图
│   ├── Phase/                         # 相位图
│   │   ├── phase_portraits_top_genes.png   # Top动态基因相位图
│   │   └── phase_portraits_markers.png     # 标志基因相位图
│   ├── Dynamics/                      # 动力学指标
│   │   ├── velocity_length.png            # 速度大小（速率）
│   │   ├── velocity_confidence.png        # 预测置信度
│   │   └── latent_time.png                # 潜在时间
│   └── Validation/                    # 验证分析
│       ├── velocity_vs_pseudotime.png     # 与轨迹伪时间对比
│       └── speed_by_celltype.png          # 各细胞类型速度分布
└── Tables/
    ├── velocity_scores.csv                # 每个细胞的速度指标
    ├── latent_time.csv                    # RNA velocity潜在时间
    ├── velocity_genes.csv                 # 动态基因排序
    └── validation_metrics.csv             # 验证指标（可选）
```

### 关键输出文件说明

#### 1. 速度流图 ⭐

**文件：** `Plots/Velocity/velocity_stream_wnn_umap.png`

**内容：**
- 箭头 = 细胞的预测运动方向
- 箭头长度 = 变化速度
- 颜色 = 细胞类型

**解读示例（cDC1发育）：**
```
Pre-cDC1s  →→→  Proliferating  →→→  Early Mature  →→  Mature cDC1
(长箭头)         (长箭头)            (中等箭头)        (短箭头/无箭头)
快速分化          快速成熟             正在成熟           稳定终末状态
```

**生物学解释：**
- **长箭头** = 细胞正在快速转换状态（活跃分化）
- **短箭头** = 细胞接近稳定状态
- **无箭头** = 终末分化细胞（稳定）

#### 2. 相位图（Phase Portraits）

**文件：** `Plots/Phase/phase_portraits_top_genes.png`

**内容：** 每个基因的未剪接 vs 剪接 mRNA 散点图

**四种典型模式：**

```
1. 诱导（Induction）          2. 抑制（Repression）
   未剪接 ↑                      未剪接 ↓
   ▲                             ▲
   │  ●●                         │●●
   │ ●●●                         │●●
   │●●●                          │ ●●
   └─────→ 剪接                  └─────→ 剪接
   基因表达上升                   基因表达下降

3. 稳态高表达                  4. 稳态低表达
   未剪接                        未剪接
   ▲                             ▲
   │    ●●●                      │●●
   │   ●●●●                      │●●
   │    ●●●                      │●●
   └─────→ 剪接                  └─────→ 剪接
   稳定高表达                     稳定低表达
```

**如何识别关键调控基因：**
- **诱导模式** + 早期细胞 → 驱动分化的激活基因
- **抑制模式** + 晚期细胞 → 成熟时被关闭的基因

#### 3. 速度大小（Velocity Length）

**文件：** `Plots/Dynamics/velocity_length.png`

**内容：** 每个细胞的速度大小（分化速率）

**颜色含义：**
- **红色（高值）** = 快速分化的细胞
- **蓝色（低值）** = 稳定/慢速分化的细胞

**典型模式：**
```
Pre-cDC1s: 高速度（红色）- 正在分化
Proliferating: 高速度（红色）- 快速增殖和分化
Early Mature: 中等速度（黄色）- 正在成熟
Mature cDC1: 低速度（蓝色）- 稳定终末状态
```

#### 4. 潜在时间（Latent Time）

**文件：** `Plots/Dynamics/latent_time.png`

**内容：** 基于RNA动力学的"细胞年龄"

**与Monocle3伪时间的区别：**

| 特性 | Monocle3 伪时间 | RNA Velocity 潜在时间 |
|------|----------------|---------------------|
| 依据 | 基因表达距离 | 转录动力学 |
| 方向性 | 需要指定起点 | 自动推断 |
| 速度信息 | 无 | 有 |
| 计算速度 | 快 | 慢 |

**推荐用法：** 两者结合验证

#### 5. 速度 vs 伪时间验证

**文件：** `Plots/Validation/velocity_vs_pseudotime.png`

**内容：** RNA velocity 潜在时间 vs Monocle3/Slingshot 伪时间

**期望结果：**
- 高相关性（r > 0.7）→ 方法一致，结果可靠 ✅
- 中等相关性（0.4 < r < 0.7）→ 部分一致，可能存在差异
- 低相关性（r < 0.4）→ 需要检查数据质量或参数

**不一致时的可能原因：**
1. Velocyto 数据质量问题
2. 伪时间起点选择不当
3. 真实存在复杂动力学（如环状轨迹）

---

## 🚀 使用指南

### 基础用法

#### 1. 最简单运行（需要先运行velocyto）

```bash
# 假设已有 sample.loom 文件
python 8.rna_velocity.py \
  --mode cDC1 \
  --loom data/cDC1.loom \
  --umap results/cDC1/Tables/wnn_umap_coordinates.csv
```

#### 2. 从 Seurat 对象加载 UMAP（推荐）

```bash
python 8.rna_velocity.py \
  --mode cDC1 \
  --loom data/cDC1.loom \
  --seurat results/cDC1/Robjects/seurat_obj_annotated.rds
```

**优势：**
- 自动加载所有UMAP空间（RNA/ADT/WNN）
- 自动加载细胞注释
- 无需手动准备CSV文件

#### 3. 完整分析（包含验证）

```bash
python 8.rna_velocity.py \
  --mode cDC1 \
  --loom data/cDC1.loom \
  --seurat results/cDC1/Robjects/seurat_obj_annotated.rds \
  --pseudotime monocle3_analysis_cDC1/Tables/pseudotime_by_cell.csv \
  --velocity-mode dynamical
```

### 高级参数

#### 选择速度计算模式

```bash
# 快速探索（推荐开始用这个）
python 8.rna_velocity.py --velocity-mode deterministic

# 标准分析（默认）
python 8.rna_velocity.py --velocity-mode stochastic

# 最终发表级分析
python 8.rna_velocity.py --velocity-mode dynamical
```

#### 调整基因过滤参数

```bash
# 更严格的过滤（提高质量但减少基因数）
python 8.rna_velocity.py \
  --n-top-genes 3000 \
  --min-shared-counts 50

# 更宽松的过滤（保留更多基因）
python 8.rna_velocity.py \
  --n-top-genes 2000 \
  --min-shared-counts 20
```

**参数说明：**
- `--n-top-genes`：使用的高变基因数量
  - 默认：2000
  - 建议范围：1000-5000

- `--min-shared-counts`：基因在多少细胞中必须表达
  - 默认：30
  - 建议范围：20-50（过高会丢失稀有细胞类型特异基因）

---

## 📊 结果解读

### 速度方向验证

#### 与轨迹分析一致性检查

```
Monocle3/Slingshot 轨迹：Pre-cDC1 → Proliferating → Mature
RNA Velocity 方向：     →→→        →→→            →→
                      ✅一致      ✅一致         ✅一致
```

**如果不一致怎么办？**

1. **检查数据质量**
   ```python
   # 在Python中检查
   import scvelo as scv
   adata = scv.read('rna_velocity_cDC1/data/adata_with_velocity.h5ad')

   # 检查spliced/unspliced比例
   print(adata.layers['unspliced'].sum() / adata.layers['spliced'].sum())
   # 期望：0.1 - 0.5 之间
   ```

2. **调整参数**
   - 尝试不同的`--velocity-mode`
   - 调整`--n-top-genes`

3. **检查生物学**
   - 是否有真实的环状轨迹？
   - 是否有批次效应？

### 识别关键调控基因

#### 从速度基因列表找候选

```bash
# 查看top动态基因
head -20 rna_velocity_cDC1/data/velocity_genes.txt
```

**典型cDC1发育的动态基因：**

| 基因 | 模式 | 功能 |
|------|------|------|
| **Mki67** | 诱导→抑制 | 增殖标志，Pre到Proliferating上升 |
| **Top2a** | 诱导→抑制 | 增殖标志，Proliferating后下降 |
| **Irf8** | 持续诱导 | cDC1转录因子，稳定上升 |
| **Batf3** | 诱导 | cDC1特异TF，早期激活 |
| **Xcr1** | 诱导 | cDC1表面标志，成熟时上升 |
| **Cd80** | 诱导 | 成熟标志，晚期诱导 |

### 细胞状态分类

基于速度大小和置信度：

```
高速度 + 高置信度 = 活跃转换状态
  → Pre-cDC1s, Proliferating cDC1s

低速度 + 高置信度 = 稳定终末状态
  → Mature cDC1

低速度 + 低置信度 = 异质性高/数据质量差
  → 可能需要进一步分群
```

---

## 🔬 下游分析

### 1. 速度驱动的差异分析

识别沿速度方向变化的基因：

```python
import scvelo as scv
import pandas as pd

# 加载数据
adata = scv.read('rna_velocity_cDC1/data/adata_with_velocity.h5ad')

# 基于潜在时间分组
adata.obs['latent_time_group'] = pd.cut(adata.obs['latent_time'], bins=3,
                                         labels=['Early', 'Middle', 'Late'])

# 差异分析
scv.tl.rank_velocity_genes(adata, groupby='latent_time_group')

# 导出
top_genes = adata.uns['rank_velocity_genes']
```

### 2. 与PAGA轨迹整合

```python
import scanpy as sc

# 加载PAGA结果
adata_paga = sc.read_h5ad('paga_analysis_cDC1/data/adata_processed.h5ad')

# 加载velocity结果
adata_velo = scv.read('rna_velocity_cDC1/data/adata_with_velocity.h5ad')

# 在PAGA图上叠加velocity
scv.tl.paga(adata_velo, groups='cell_type')
scv.pl.paga(adata_velo, basis='wnn_umap', size=50, alpha=.1,
            min_edge_width=2, node_size_scale=1.5)
```

### 3. 识别转录因子动力学

```python
# 定义转录因子列表
tfs = ['Irf8', 'Batf3', 'Id2', 'Zeb2', 'Irf4']

# 筛选存在的TF
available_tfs = [tf for tf in tfs if tf in adata.var_names]

# 绘制相位图
scv.pl.scatter(adata, basis=available_tfs, ncols=3,
               figsize=(12, 8), save='tf_phase_portraits.png')
```

---

## ⚙️ 依赖环境

### Python 包

```bash
# 核心包
pip install scvelo>=0.2.5
pip install scanpy>=1.9.0
pip install loompy>=3.0.7

# 数据处理
pip install anndata>=0.8.0
pip install pandas numpy

# 可视化
pip install matplotlib seaborn

# 可选：从Seurat加载
pip install rpy2

# 一键安装
pip install scvelo scanpy loompy anndata pandas numpy matplotlib seaborn
```

### Velocyto CLI

```bash
# 安装velocyto命令行工具（用于预处理）
pip install velocyto
```

### 测试环境

```bash
# 测试scvelo
python -c "import scvelo as scv; print(scv.__version__)"

# 测试loompy
python -c "import loompy; print(loompy.__version__)"
```

---

## ❓ 常见问题

### Q1: Velocyto 预处理失败？

**问题：** `velocyto run10x` 报错或没有输出

**可能原因 & 解决：**

1. **GTF文件格式问题**
   ```bash
   # 确保使用正确的GTF文件
   # 对于小鼠：Mus_musculus.GRCm38.99.gtf
   # 对于人类：Homo_sapiens.GRCh38.99.gtf
   ```

2. **BAM文件路径错误**
   ```bash
   # 确保指向CellRanger输出的正确路径
   # 应该包含：outs/possorted_genome_bam.bam
   ```

3. **内存不足**
   ```bash
   # Velocyto需要大量内存，建议：
   # 小数据集：16GB
   # 大数据集：32GB+
   ```

### Q2: 速度向量很短或几乎没有？

**问题：** 生成的速度图箭头很短或看不见

**可能原因 & 解决：**

1. **细胞确实处于稳态**
   - 终末分化细胞（如Mature cDC1）本来速度就低
   - 这是正常的生物学现象 ✅

2. **数据质量问题**
   ```python
   # 检查unspliced reads比例
   import scvelo as scv
   adata = scv.read('sample.loom')
   ratio = adata.layers['unspliced'].sum() / adata.layers['spliced'].sum()
   print(f"Unspliced/Spliced ratio: {ratio:.2f}")

   # 期望：0.1 - 0.5
   # 如果太低（< 0.05）说明测序深度不足
   ```

3. **参数过于严格**
   ```bash
   # 尝试降低过滤阈值
   python 8.rna_velocity.py --min-shared-counts 20
   ```

### Q3: RNA velocity 和 Monocle3 伪时间相关性低？

**问题：** 验证图显示相关系数 r < 0.5

**可能原因：**

1. **起点选择不同**
   - Monocle3：手动选择起点
   - RNA velocity：自动推断
   - 解决：检查两者起点是否一致

2. **方法侧重点不同**
   - Monocle3：基于基因表达距离
   - RNA velocity：基于转录动力学
   - 这是正常的，不一定是问题 ✅

3. **复杂轨迹拓扑**
   - 如果有环状结构或多分支，两种方法可能给出不同结果
   - 解决：结合PAGA查看拓扑结构

### Q4: 如何处理批次效应？

**问题：** 多个样本的velocity方向不一致

**解决方案：**

```python
import scvelo as scv

# 加载数据
adata = scv.read('merged.loom')

# 方法1：使用scanorama校正
import scanorama
scanorama.integrate_scanpy(adata, key='batch')

# 方法2：使用harmony
import scanpy.external as sce
sce.pp.harmony_integrate(adata, 'batch')

# 然后重新计算velocity
scv.pp.moments(adata, n_neighbors=30)
scv.tl.velocity(adata, mode='stochastic')
```

### Q5: 内存不足怎么办？

**问题：** 大数据集分析时内存溢出

**解决方案：**

1. **降采样**
   ```python
   import scanpy as sc
   adata = scv.read('large.loom')

   # 每个细胞类型采样固定数量
   sc.pp.subsample(adata, n_obs=5000)
   ```

2. **使用deterministic模式**
   ```bash
   # 内存占用最少
   python 8.rna_velocity.py --velocity-mode deterministic
   ```

3. **减少基因数**
   ```bash
   python 8.rna_velocity.py --n-top-genes 1000
   ```

---

## 📚 参考文献

### RNA Velocity 方法论文

La Manno, G., Soldatov, R., Zeisel, A., Braun, E., Hochgerner, H., Petukhov, V., ... & Kharchenko, P. V. (2018).
**RNA velocity of single cells.**
*Nature*, 560(7719), 494-498.

### scVelo 改进

Bergen, V., Lange, M., Peidli, S., Wolf, F. A., & Theis, F. J. (2020).
**Generalizing RNA velocity to transient cell states through dynamical modeling.**
*Nature biotechnology*, 38(12), 1408-1414.

### Velocyto 工具

La Manno, G., Soldatov, R., Zeisel, A., Braun, E., Hochgerner, H., Petukhov, V., ... & Kharchenko, P. V. (2018).
**Velocyto: RNA velocity analysis for single cell transcriptomics.**
*bioRxiv*, 10.1101/206052.

---

## 🎉 总结

### RNA Velocity 的关键优势

1. **预测性** - 不仅看当前状态，还预测未来方向
2. **无需人工指定** - 自动推断发育方向
3. **基因水平动力学** - 识别快速响应的调控基因
4. **验证工具** - 交叉验证轨迹分析结果

### 最佳实践流程

```
1. 运行 Velocyto 预处理（生成 .loom）
   ↓
2. 运行 RNA Velocity 分析
   ↓
3. 检查速度流图，验证方向
   ↓
4. 与 Monocle3/PAGA 结果对比
   ↓
5. 分析相位图，识别动态基因
   ↓
6. 提取调控基因进行功能验证
```

### 注意事项

⚠ **数据质量很重要！**
- 需要足够的测序深度捕获未剪接reads
- 建议每个细胞 > 5000 UMIs
- Unspliced/Spliced 比例应在 0.1-0.5

⚠ **不是所有细胞都有强velocity！**
- 终末分化细胞速度低是正常的
- 稳态组织可能整体速度都低

⚠ **需要与其他方法结合！**
- 单独使用RNA velocity可能误导
- 必须与Monocle3/Slingshot/PAGA交叉验证

### 下一步分析

- **已完成：** Monocle3, Slingshot, PAGA, RNA Velocity ✅
- **推荐下一步：** CellOracle（基因调控网络预测）
- **或者：** CytoTRACE（分化潜能评分）

**祝分析顺利！** 🚀
