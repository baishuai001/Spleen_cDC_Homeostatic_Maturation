# Monocle3 轨迹分析 - 完整说明文档

## 📋 概述

`5.monocle3_trajectory_analysis.Rmd` 是 GSE228544 cDC CITE-seq 数据分析流程的**轨迹分析模块**，使用 Monocle3 揭示 cDC 细胞的发育动态和分化轨迹。

### 核心特性

✅ **多模态UMAP空间支持**
- RNA UMAP（基于基因表达）
- ADT UMAP（基于表面蛋白）
- WNN UMAP（加权最近邻整合，推荐）

✅ **三种分析模式**
- cDC1：cDC1 特异分析
- cDC2：cDC2 特异分析
- integrated：整合分析

✅ **完整轨迹分析流程**
- 轨迹构建（trajectory construction）
- 伪时间推断（pseudotime inference）
- 差异基因识别（trajectory-dependent genes）
- 多空间可视化（RNA/ADT/WNN UMAP）

---

## 📁 文件夹结构

### 输入数据位置

脚本会按以下优先级搜索输入文件 `data_for_monocle3.rds`：

```
1. monocle3_analysis_{mode}/data/data_for_monocle3.rds  （推荐）
2. monocle3_analysis/data/data_for_monocle3.rds
3. data/data_for_monocle3.rds
```

**推荐文件夹结构**（支持多模式并行）：

```
项目根目录/
├─ 5.monocle3_trajectory_analysis.Rmd          # 分析脚本
├─ 5.Monocle3_轨迹分析_说明文档.md              # 本文档
│
├─ monocle3_analysis_cDC1/                     # cDC1 模式
│  ├─ data/
│  │  └─ data_for_monocle3.rds                 # 输入数据（用户放入）
│  ├─ Robjects/                                # R对象（脚本生成）
│  ├─ Plots/                                   # 图表（脚本生成）
│  └─ Tables/                                  # 数据表（脚本生成）
│
├─ monocle3_analysis_cDC2/                     # cDC2 模式
│  ├─ data/
│  │  └─ data_for_monocle3.rds
│  ├─ Robjects/
│  ├─ Plots/
│  └─ Tables/
│
└─ monocle3_analysis_integrated/               # 整合模式
   ├─ data/
   │  └─ data_for_monocle3.rds
   ├─ Robjects/
   ├─ Plots/
   └─ Tables/
```

### 输出目录结构

运行脚本后，会在对应的 `monocle3_analysis_{mode}/` 目录下生成：

```
monocle3_analysis_{mode}/
├─ data/
│  └─ data_for_monocle3.rds           # 输入数据（用户提供）
│
├─ Robjects/                          # R对象存储
│  ├─ cds_object.rds                  # Monocle3 CDS对象（核心）
│  ├─ trajectory_genes.rds            # 显著轨迹基因
│  └─ pseudotime_results.rds          # 伪时间结果（可选）
│
├─ Plots/                             # 可视化结果
│  ├─ Trajectory/                     # 轨迹图
│  │  ├─ trajectory_rna_umap.png      # RNA UMAP空间轨迹（伪时间+细胞类型）
│  │  ├─ trajectory_adt_umap.png      # ADT UMAP空间轨迹
│  │  ├─ trajectory_wnn_umap.png      # WNN UMAP空间轨迹
│  │  ├─ pseudotime_rna_umap.png      # RNA UMAP伪时间单图
│  │  ├─ pseudotime_adt_umap.png      # ADT UMAP伪时间单图
│  │  ├─ pseudotime_wnn_umap.png      # WNN UMAP伪时间单图
│  │  └─ trajectory_three_umaps_comparison.png  # 三空间对比
│  │
│  ├─ Genes/                          # 基因表达动态
│  │  ├─ trajectory_genes_heatmap.png            # 轨迹基因热图
│  │  └─ top_genes_pseudotime_trends.png         # Top基因伪时间趋势
│  │
│  └─ QC/                             # 质控图（可选）
│     └─ trajectory_qc_metrics.png
│
└─ Tables/                            # 数据导出
   ├─ trajectory_genes.csv            # 轨迹依赖基因列表（完整）
   ├─ pseudotime_by_cell.csv          # 每个细胞的伪时间值
   └─ branch_genes.csv                # 分支特异基因（可选）
```

---

## 🚀 使用指南

### 步骤 1：准备输入数据

从上游脚本 `4.downstream_analysis.Rmd` 获取 `data_for_monocle3.rds`，放入对应目录：

```bash
# 示例：cDC1 模式
mkdir -p monocle3_analysis_cDC1/data
cp results/cDC1/Robjects/data_for_monocle3.rds monocle3_analysis_cDC1/data/

# 示例：cDC2 模式
mkdir -p monocle3_analysis_cDC2/data
cp results/cDC2/Robjects/data_for_monocle3.rds monocle3_analysis_cDC2/data/

# 示例：整合模式
mkdir -p monocle3_analysis_integrated/data
cp results/integrated/Robjects/data_for_monocle3.rds monocle3_analysis_integrated/data/
```

### 步骤 2：检查输入数据

确保 `data_for_monocle3.rds` 包含以下组件：

**必需组件**：
- `counts`: 原始计数矩阵
- `normalized`: 归一化表达矩阵
- `metadata`: 细胞元数据
- `umap`: RNA UMAP 坐标
- `cell_type`: 细胞类型注释
- `analysis_mode`: 分析模式（"cDC1"/"cDC2"/"integrated"）

**可选组件**（强烈推荐）：
- `adt_umap`: ADT UMAP 坐标
- `wnn_umap`: WNN UMAP 坐标（最推荐用于轨迹构建）
- `pca`: PCA 降维结果

### 步骤 3：配置分析参数

打开 `5.monocle3_trajectory_analysis.Rmd`，在配置区域修改参数：

```r
# ==================== 选择分析模式 ====================
# 注意：实际会从输入数据中读取 analysis_mode
# 此处仅作为备用默认值
default_analysis_mode <- "cDC1"  # 可选: "cDC1", "cDC2", "integrated"

# ==================== UMAP空间选择 ====================
# 设置轨迹构建的首选UMAP空间（按优先级自动降级）
preferred_umap_for_trajectory <- "wnn"  # 可选: "wnn", "adt", "rna"

# ==================== 差异基因参数 ====================
diff_genes_qval <- 0.05      # q值阈值（FDR校正）
n_top_genes <- 20            # 展示的top基因数量
```

### 步骤 4：运行分析

在 RStudio 中：

1. 打开 `5.monocle3_trajectory_analysis.Rmd`
2. 点击 **Knit** 按钮
3. 选择 **Knit to HTML**
4. 等待分析完成（约5-10分钟）

或使用命令行：

```bash
Rscript -e "rmarkdown::render('5.monocle3_trajectory_analysis.Rmd')"
```

### 步骤 5：查看结果

分析完成后：

1. **HTML报告**：`5.monocle3_trajectory_analysis.html`（包含所有图表和分析）
2. **轨迹图**：`monocle3_analysis_{mode}/Plots/Trajectory/`
3. **基因列表**：`monocle3_analysis_{mode}/Tables/trajectory_genes.csv`
4. **CDS对象**：`monocle3_analysis_{mode}/Robjects/cds_object.rds`（可用于进一步分析）

---

## 🧬 技术细节

### 数据流程

```
data_for_monocle3.rds
    ↓
创建 CellDataSet (CDS)
    ↓
添加降维结果 (PCA, UMAP, ADT_UMAP, WNN_UMAP)
    ↓
选择UMAP空间用于轨迹构建 (优先级: WNN > ADT > RNA)
    ↓
聚类细胞 (cluster_cells)
    ↓
学习轨迹图 (learn_graph)
    ↓
计算伪时间 (order_cells)
    ↓
识别轨迹依赖基因 (graph_test)
    ↓
三种UMAP空间可视化 (RNA/ADT/WNN)
```

### UMAP空间选择策略

脚本支持在**三种UMAP空间**上可视化轨迹，但**轨迹构建**只在一个空间上进行：

#### 1. 轨迹构建空间选择

通过 `preferred_umap_for_trajectory` 参数控制，优先级：

```
WNN UMAP (最推荐) → ADT UMAP → RNA UMAP
```

- **WNN UMAP**：整合 RNA + 蛋白质信息，最全面，推荐用于轨迹构建
- **ADT UMAP**：基于表面蛋白，适合表型驱动的分析
- **RNA UMAP**：基于基因表达，传统方法

如果首选空间不可用，脚本会自动降级到下一个可用空间。

#### 2. 条件检测逻辑

```r
# 检查 ADT UMAP 是否可用
if ("ADT_UMAP" %in% names(reducedDims(cds))) {
  # 使用 ADT UMAP
}

# 检查 WNN UMAP 是否可用
if ("WNN_UMAP" %in% names(reducedDims(cds))) {
  # 使用 WNN UMAP
}
```

#### 3. 可视化策略

无论在哪个空间构建轨迹，脚本都会在**所有可用的UMAP空间**上可视化：

- RNA UMAP：`trajectory_rna_umap.png`
- ADT UMAP：`trajectory_adt_umap.png`（如果可用）
- WNN UMAP：`trajectory_wnn_umap.png`（如果可用）
- 三空间对比：`trajectory_three_umaps_comparison.png`

### 分析模式识别

脚本会自动从输入数据中读取 `analysis_mode`：

```r
if (!is.null(monocle3_data$analysis_mode)) {
  analysis_mode <- monocle3_data$analysis_mode
}
```

然后根据模式设置输出目录：

```r
output_dir <- file.path(base_dir, paste0("monocle3_analysis_", analysis_mode))
```

这确保了三种模式的结果完全独立，不会相互覆盖。

### 伪时间根节点选择

脚本提供**两种方式**选择根节点（发育起点）：

#### 方法 1：自动选择（推荐）

基于细胞类型注释自动识别早期细胞：

```r
early_celltype_markers <- list(
  cDC1 = c("pre-cDC1", "early_cDC1", "CDP", "pre-DC"),
  cDC2 = c("pre-cDC2", "early_cDC2", "CDP", "pre-DC"),
  integrated = c("CDP", "pre-DC", "pre-cDC1", "pre-cDC2")
)
```

**优点**：
- 全自动，适合批量分析
- 生物学上合理

**缺点**：
- 依赖准确的细胞类型注释
- 如果注释中没有早期标记，会失败

#### 方法 2：交互式选择

在 RStudio Console 中运行时，可以交互式点击选择根节点：

```r
cds <- order_cells(cds)  # 会弹出图形界面
```

**优点**：
- 灵活，用户可以根据数据特点选择
- 不依赖细胞类型注释

**缺点**：
- 需要人工干预
- 不适合批量分析

### 轨迹依赖基因识别

使用 Monocle3 的 `graph_test()` 方法：

```r
trajectory_genes <- graph_test(
  cds,
  neighbor_graph = "principal_graph",
  cores = 4
)
```

**原理**：
- 测试每个基因的表达是否沿轨迹（principal graph）显著变化
- 使用 Moran's I 统计量
- 考虑细胞在轨迹上的空间位置

**输出**：
- `status`: 基因状态
- `morans_test_statistic`: Moran's I 统计量
- `morans_I`: 归一化的 Moran's I
- `p_value`: 原始 p 值
- `q_value`: FDR 校正后的 q 值

### 可视化技术

#### 1. 轨迹图（带图结构）

使用 Monocle3 内置函数：

```r
plot_cells(
  cds,
  reduction_method = "WNN_UMAP",
  color_cells_by = "pseudotime",
  label_branch_points = FALSE
)
```

显示：
- 细胞点（按伪时间或细胞类型着色）
- 轨迹主图（principal graph）
- 分支点（可选）

#### 2. 伪时间分布图

自定义 ggplot2 绘图：

```r
plot_trajectory_on_umap <- function(cds, reduction_name, color_by = "pseudotime") {
  # 提取坐标
  umap_coords <- reducedDims(cds)[[reduction_name]]

  # 创建数据框
  plot_df <- data.frame(
    UMAP_1 = umap_coords[, 1],
    UMAP_2 = umap_coords[, 2],
    pseudotime = pseudotime(cds)
  )

  # 绘图
  ggplot(plot_df, aes(x = UMAP_1, y = UMAP_2, color = pseudotime)) +
    geom_point() +
    scale_color_viridis()
}
```

#### 3. 基因表达热图

按伪时间排序细胞，展示基因表达模式：

```r
# 按伪时间排序
pseudotime_order <- order(pseudotime(cds))

# 提取表达矩阵
expr_matrix_ordered <- expr_matrix[, pseudotime_order]

# Z-score标准化
expr_matrix_scaled <- t(scale(t(expr_matrix_ordered)))

# 绘制热图
pheatmap(expr_matrix_scaled, cluster_cols = FALSE)
```

---

## 📊 输出文件详解

### 1. Robjects/cds_object.rds

**Monocle3 CellDataSet 对象**，包含：

- 表达数据（`exprs(cds)`）
- 细胞元数据（`colData(cds)`）
- 基因元数据（`rowData(cds)`）
- 降维结果（`reducedDims(cds)`）
  - `PCA`
  - `UMAP`（RNA UMAP）
  - `ADT_UMAP`（如果可用）
  - `WNN_UMAP`（如果可用）
- 轨迹信息
  - 聚类（`clusters(cds)`）
  - 分区（`partitions(cds)`）
  - 伪时间（`pseudotime(cds)`）
  - 主图（`principal_graph(cds)`）

**用途**：
- 后续深入分析
- 重新可视化
- 导出到其他工具

**加载示例**：

```r
cds <- readRDS("monocle3_analysis_cDC1/Robjects/cds_object.rds")

# 提取伪时间
pt <- pseudotime(cds)

# 提取UMAP坐标
umap_coords <- reducedDims(cds)[["WNN_UMAP"]]

# 重新可视化
plot_cells(cds, color_cells_by = "cell_type")
```

### 2. Tables/trajectory_genes.csv

**轨迹依赖基因列表**，包含所有基因的统计结果：

| 列名 | 描述 |
|------|------|
| `gene` | 基因名称 |
| `status` | 测试状态（OK/FAIL） |
| `morans_test_statistic` | Moran's I 测试统计量 |
| `morans_I` | 归一化的 Moran's I（-1 到 1） |
| `p_value` | 原始 p 值 |
| `q_value` | FDR 校正后的 q 值 |

**使用建议**：
- 筛选 `q_value < 0.05` 获得显著基因
- 按 `q_value` 排序获得最显著的基因
- `morans_I > 0`：表达在轨迹上聚集（相似细胞表达相似）
- `morans_I < 0`：表达在轨迹上分散（相邻细胞表达不同）

**R 读取示例**：

```r
traj_genes <- read.csv("monocle3_analysis_cDC1/Tables/trajectory_genes.csv")

# 筛选显著基因
sig_genes <- traj_genes[traj_genes$q_value < 0.05, ]

# 按显著性排序
sig_genes <- sig_genes[order(sig_genes$q_value), ]

# Top 10 基因
head(sig_genes, 10)
```

### 3. Tables/pseudotime_by_cell.csv

**每个细胞的伪时间值**：

| 列名 | 描述 |
|------|------|
| `cell_id` | 细胞 barcode |
| `pseudotime` | 伪时间值（0 到最大值） |
| `cell_type` | 细胞类型注释 |
| `cluster` | Monocle3 聚类 ID |
| `partition` | Monocle3 分区 ID |

**用途**：
- 按伪时间排序细胞
- 分析不同细胞类型的伪时间分布
- 与其他分析工具整合

**Python 读取示例**：

```python
import pandas as pd

pseudotime_df = pd.read_csv("monocle3_analysis_cDC1/Tables/pseudotime_by_cell.csv")

# 按伪时间排序
pseudotime_df_sorted = pseudotime_df.sort_values("pseudotime")

# 查看早期细胞
early_cells = pseudotime_df_sorted.head(100)
```

### 4. Plots/ 目录图表

#### Trajectory/ - 轨迹可视化

- **trajectory_rna_umap.png**（14×6）：RNA UMAP 空间，左：伪时间，右：细胞类型
- **trajectory_adt_umap.png**（14×6）：ADT UMAP 空间，左：伪时间，右：细胞类型
- **trajectory_wnn_umap.png**（14×6）：WNN UMAP 空间，左：伪时间，右：细胞类型
- **pseudotime_rna_umap.png**（8×6）：RNA UMAP 伪时间单图
- **pseudotime_adt_umap.png**（8×6）：ADT UMAP 伪时间单图
- **pseudotime_wnn_umap.png**（8×6）：WNN UMAP 伪时间单图
- **trajectory_three_umaps_comparison.png**（18×6）：三空间对比图

**用于**：
- 论文 Figure
- 报告展示
- 多空间对比

#### Genes/ - 基因表达动态

- **trajectory_genes_heatmap.png**（10×10）：Top 轨迹基因热图（按伪时间排序）
- **top_genes_pseudotime_trends.png**（12×8）：Top 6 基因沿伪时间的表达趋势

**用于**：
- 识别基因表达模式
- 发现关键调控因子
- 验证已知标志基因

---

## 🔍 常见问题与解决

### Q1: 找不到输入文件

**错误信息**：

```
❌ 未找到 data_for_monocle3.rds！
```

**解决方法**：

1. 确认文件确实存在
2. 检查文件路径是否正确
3. 将文件放入以下位置之一：
   - `monocle3_analysis_{mode}/data/data_for_monocle3.rds`（推荐）
   - `monocle3_analysis/data/data_for_monocle3.rds`
   - `data/data_for_monocle3.rds`

### Q2: 缺少 WNN UMAP

**问题**：脚本警告 "⚠ WNN UMAP 不可用"

**原因**：输入数据中没有 `wnn_umap` 组件

**解决方法**：

1. 确认上游脚本 `4.downstream_analysis.Rmd` 使用了 `*_final.rds` 作为输入（而非 `*_pre_wnn.rds`）
2. 检查 `data_for_monocle3.rds` 中是否包含 `wnn_umap`：

```r
data <- readRDS("path/to/data_for_monocle3.rds")
"wnn_umap" %in% names(data)  # 应该返回 TRUE
```

3. 如果确实没有，脚本会自动使用 ADT UMAP 或 RNA UMAP

### Q3: Monocle3 安装问题

**错误信息**：

```
Error: package 'monocle3' is not available
```

**解决方法**：

Monocle3 需要从 GitHub 安装：

```r
# 安装 devtools（如果还没有）
install.packages("devtools")

# 安装 Bioconductor 依赖
if (!requireNamespace("BiocManager", quietly = TRUE))
    install.packages("BiocManager")

BiocManager::install(c("SingleCellExperiment", "SummarizedExperiment", "batchelor"))

# 从 GitHub 安装 Monocle3
devtools::install_github("cole-trapnell-lab/monocle3")
```

如果遇到编译问题，可能需要安装系统依赖（Ubuntu/Debian）：

```bash
sudo apt-get install libgsl-dev libxml2-dev libcurl4-openssl-dev
```

### Q4: 伪时间计算失败

**问题**：所有细胞的伪时间都是 NA

**可能原因**：

1. 轨迹图未正确学习
2. 根节点选择不合适
3. 数据中存在多个不连通的分区

**解决方法**：

1. 检查轨迹图是否正确：

```r
plot_cells(cds, label_branch_points = TRUE, label_leaves = TRUE)
```

2. 尝试手动选择根节点（在 RStudio Console 中）：

```r
cds <- order_cells(cds)  # 点击图中的根节点
```

3. 如果有多个分区，考虑只分析主分区：

```r
# 保留最大分区
main_partition <- names(sort(table(partitions(cds)), decreasing = TRUE))[1]
cds_main <- cds[, partitions(cds) == main_partition]
```

### Q5: 找不到早期细胞类型

**警告信息**：

```
⚠ 未找到匹配的早期细胞，使用交互式选择
```

**原因**：数据中的细胞类型注释与脚本中定义的早期标记不匹配

**解决方法**：

1. 查看数据中的细胞类型：

```r
table(monocle3_data$cell_type)
```

2. 修改脚本中的早期标记定义：

```r
early_celltype_markers <- list(
  cDC1 = c("你的早期类型1", "你的早期类型2"),
  cDC2 = c("..."),
  integrated = c("...")
)
```

3. 或使用交互式选择（在 Console 中运行）

### Q6: 内存不足

**错误信息**：

```
Error: cannot allocate vector of size ...
```

**原因**：数据集太大，内存不足

**解决方法**：

1. 增加 R 的内存限制（Linux/Mac）：

```bash
R --max-mem-size=32G
```

2. 减少细胞数（下采样）：

```r
# 随机抽样 50% 细胞
set.seed(42)
cells_to_keep <- sample(colnames(cds), size = floor(ncol(cds) * 0.5))
cds_sub <- cds[, cells_to_keep]
```

3. 减少基因数（保留高变基因）：

```r
# 只保留高变基因
var_genes <- monocle3_data$variable_genes  # 如果有
cds_sub <- cds[var_genes, ]
```

---

## 🎯 高级用法

### 1. 批量分析三种模式

创建批处理脚本 `run_all_monocle3.sh`：

```bash
#!/bin/bash

# 设置分析模式列表
modes=("cDC1" "cDC2" "integrated")

for mode in "${modes[@]}"
do
  echo "========================================="
  echo "Processing mode: $mode"
  echo "========================================="

  # 检查输入文件
  input_file="monocle3_analysis_${mode}/data/data_for_monocle3.rds"
  if [ ! -f "$input_file" ]; then
    echo "Warning: $input_file not found, skipping..."
    continue
  fi

  # 运行分析
  Rscript -e "
    rmarkdown::render(
      '5.monocle3_trajectory_analysis.Rmd',
      output_file = '5.monocle3_trajectory_analysis_${mode}.html'
    )
  "

  echo "✓ $mode completed"
  echo ""
done

echo "All analyses completed!"
```

运行：

```bash
chmod +x run_all_monocle3.sh
./run_all_monocle3.sh
```

### 2. 自定义根节点选择

如果已知特定细胞应该作为根节点：

```r
# 方法 1：使用特定细胞类型
root_cells <- which(colData(cds)$cell_type == "CDP")
cds <- order_cells(cds, root_cells = colnames(cds)[root_cells])

# 方法 2：使用特定基因表达低的细胞（如成熟标志基因低）
mature_gene <- "Cd83"
expr_values <- exprs(cds)[mature_gene, ]
root_cells <- which(expr_values < quantile(expr_values, 0.1))
cds <- order_cells(cds, root_cells = colnames(cds)[root_cells])

# 方法 3：使用伪时间最早的cluster
# 先运行一次默认的order_cells
cds <- order_cells(cds)
# 找到伪时间最早的cluster
early_cluster <- names(sort(tapply(pseudotime(cds), clusters(cds), median)))[1]
root_cells <- which(clusters(cds) == early_cluster)
# 重新计算伪时间
cds <- order_cells(cds, root_cells = colnames(cds)[root_cells])
```

### 3. 分支点分析

识别和分析分化分支点：

```r
# 获取分支点位置
branch_points <- cds@principal_graph_aux[[selected_umap_name]]$branch_points

# 找到分支点附近的细胞
# （需要根据图结构手动实现）

# 识别分支特异基因
branch_genes <- graph_test(
  cds,
  neighbor_graph = "principal_graph",
  cores = 4
)

# 筛选在特定分支上变化的基因
# （需要结合伪时间和分支信息）
```

### 4. 与其他工具整合

#### 导出到 Python (Scanpy/scVelo)

```r
# 导出表达矩阵
library(Matrix)
writeMM(exprs(cds), "expression_matrix.mtx")

# 导出细胞元数据（包括伪时间）
write.csv(
  data.frame(
    cell_id = colnames(cds),
    pseudotime = pseudotime(cds),
    cell_type = colData(cds)$cell_type
  ),
  "cell_metadata.csv"
)

# 导出基因名
writeLines(rownames(cds), "gene_names.txt")
```

在 Python 中：

```python
import scanpy as sc
from scipy.io import mmread

# 读取数据
X = mmread("expression_matrix.mtx").T.tocsr()
genes = open("gene_names.txt").read().splitlines()
cells = pd.read_csv("cell_metadata.csv", index_col=0)

# 创建 AnnData
adata = sc.AnnData(X=X, obs=cells, var=pd.DataFrame(index=genes))

# 使用伪时间
adata.obs["monocle3_pseudotime"] = cells["pseudotime"]
```

#### 导出到 CytoTRACE

```r
# CytoTRACE 需要归一化表达矩阵
expr_mat <- as.matrix(normalized_counts(cds))

# 保存
write.csv(expr_mat, "expression_for_cytotrace.csv")
```

### 5. 定制可视化

#### 自定义颜色方案

```r
# 定义细胞类型颜色
celltype_colors <- c(
  "CDP" = "#E41A1C",
  "pre-cDC1" = "#377EB8",
  "cDC1" = "#4DAF4A",
  "pre-cDC2" = "#984EA3",
  "cDC2" = "#FF7F00"
)

# 使用自定义颜色
plot_cells(
  cds,
  color_cells_by = "cell_type"
) +
  scale_color_manual(values = celltype_colors)
```

#### 添加基因表达叠加

```r
# 在轨迹图上显示特定基因的表达
genes_of_interest <- c("Xcr1", "Irf8", "Cd209a")

for (gene in genes_of_interest) {
  p <- plot_cells(
    cds,
    genes = gene,
    label_cell_groups = FALSE
  ) +
    ggtitle(paste("Expression of", gene))

  print(p)
}
```

---

## 📚 参考资源

### Monocle3 官方文档

- **主页**：https://cole-trapnell-lab.github.io/monocle3/
- **教程**：https://cole-trapnell-lab.github.io/monocle3/docs/trajectories/
- **GitHub**：https://github.com/cole-trapnell-lab/monocle3

### 相关论文

1. **Monocle3 方法论**：
   - Cao et al. (2019). "The single-cell transcriptional landscape of mammalian organogenesis." *Nature*. https://doi.org/10.1038/s41586-019-0969-x

2. **轨迹推断综述**：
   - Saelens et al. (2019). "A comparison of single-cell trajectory inference methods." *Nature Biotechnology*. https://doi.org/10.1038/s41587-019-0071-9

3. **cDC 发育**：
   - Anderson et al. (2021). "Decoding cDC development and activation." *Science Immunology*. https://doi.org/10.1126/sciimmunol.abk1029

### 相关工具

- **RNA Velocity**：动态RNA分析，补充轨迹分析
- **CellRank**：基于RNA velocity的轨迹推断
- **PAGA**：轨迹拓扑分析（Scanpy）
- **Slingshot**：另一种轨迹推断方法（Bioconductor）

---

## 🔄 工作流整合

### 上游依赖

- **4.downstream_analysis.Rmd** → `data_for_monocle3.rds`

### 下游分析

1. **RNA Velocity**（`6.rna_velocity_analysis.Rmd/py`）
   - 使用伪时间结果验证发育方向
   - 识别关键转录调控事件

2. **CellOracle**（`7.celloracle_grn_analysis.py`）
   - 结合轨迹依赖基因构建GRN
   - 预测关键调控因子

3. **scTenifoldKnk**（`8.sctenifoldknk_perturbation.Rmd`）
   - 模拟关键基因敲除对轨迹的影响

### 完整流程

```
CITE-seq_Complete_Analysis_v3.Rmd
    ↓
*_final.rds (含 WNN UMAP)
    ↓
4.downstream_analysis.Rmd
    ↓
data_for_monocle3.rds
    ↓
5.monocle3_trajectory_analysis.Rmd  ← 本脚本
    ↓
├─ cds_object.rds
├─ trajectory_genes.csv
├─ pseudotime_by_cell.csv
└─ 轨迹可视化图表
    ↓
6-8. 进一步下游分析
```

---

## ✅ 检查清单

运行分析前：

- [ ] 已安装 Monocle3 和所有依赖包
- [ ] `data_for_monocle3.rds` 已放入正确位置
- [ ] 输入数据包含所有必需组件（counts, normalized, umap, cell_type）
- [ ] （推荐）输入数据包含 WNN UMAP
- [ ] 已配置分析参数（UMAP空间选择、差异基因阈值）

运行分析后：

- [ ] HTML报告生成成功
- [ ] 轨迹图显示合理的发育路径
- [ ] 伪时间分布与细胞类型一致（早期→晚期）
- [ ] 识别到显著的轨迹依赖基因（q < 0.05）
- [ ] 三种UMAP空间图表（如果可用）都已生成
- [ ] 输出文件已保存到正确目录

---

## 📧 问题反馈

如有问题或建议，请：

1. 检查本文档的"常见问题"部分
2. 查阅 Monocle3 官方文档
3. 联系分析团队

---

**最后更新**：2026-01-20
**版本**：v1.0
**脚本**：5.monocle3_trajectory_analysis.Rmd
