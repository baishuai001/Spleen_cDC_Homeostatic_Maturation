---
title: "CITE-seq 完整分析流程 (智能缓存 + 参数化 + 全量分析)"
author: "baishuai"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_float: true
    code_folding: show
    theme: cosmo
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, message = FALSE, warning = FALSE, fig.width = 10, fig.height = 8)
```

# 📚 使用说明

## 🎯 四种分析模式

本脚本支持**四种分析模式**，通过修改 `analysis_mode` 参数即可切换：

| 模式 | 说明 | 数据来源 |
|------|------|---------|
| `cDC1` | 仅分析 cDC1 细胞 | GSE228544_..._subset_2023.rds.gz |
| `cDC2` | 仅分析 cDC2 细胞 | GSE228544_..._subset2_v2_2023.rds.gz |
| `integrated` | cDC1 + cDC2 整合分析 | 两个文件整合 + Harmony 批次校正 |
| `full` | **全量细胞分析 (新增!)** | 完整 10X 数据，所有细胞类型 |

## ✨ 核心特性

1. **参数化配置**: 一个参数控制全部行为
2. **智能缓存**: 耗时操作自动缓存，重复运行仅需几分钟
3. **模式隔离**: 不同模式使用独立缓存，互不干扰
4. **批次校正**: 整合模式自动启用 Harmony
5. **自动标注**: 全量模式自动识别细胞类型
6. **进度提示**: 清晰显示每个步骤的状态和耗时

---

# 1. 参数配置

## 1.1 核心参数设置

```{r analysis_mode_config}
################################################################################
##################### 🎯 核心参数: 选择分析模式 #################################
################################################################################

# ==================== 在这里修改分析模式 ====================
analysis_mode <- "cDC1"  # 可选: "cDC1", "cDC2", "integrated", "full"

# ==================== 通用路径配置 ====================
data_dir <- "./data/GSE228544/"
output.dir <- "results/"

cat("\n", strrep("=", 80), "\n")
cat("🎯 当前分析模式:", analysis_mode, "\n")
cat(strrep("=", 80), "\n\n")
```

## 1.2 加载 R 包

```{r load_packages, message=FALSE, warning=FALSE}
################################################################################
############################## 加载 R 包 ########################################
################################################################################

cat("📦 加载 R 包...\n")

suppressPackageStartupMessages({
  library("tidyverse")
  library("Seurat")
  library("SingleCellExperiment")
  library("scater")
  library("scran")
  library('DoubletFinder')
  library("dplyr")
  library("stringr")
  library("readr")
  library("scuttle")
  library("ggplot2")
  library("patchwork")
  library("RColorBrewer")
  library("R.utils")
  library("Matrix")
})

# 整合分析需要额外的包
if (analysis_mode == "integrated") {
  suppressPackageStartupMessages({
    if (!require("harmony", quietly = TRUE)) {
      message("⚠️  Harmony 包未安装，整合分析需要此包")
      message("   安装命令: install.packages('harmony')")
    } else {
      library("harmony")
    }
  })
}

cat("✓ R 包加载完成\n\n")
```

## 1.3 工具函数定义

```{r utility_functions}
################################################################################
##################### 通用工具函数 ##############################################
################################################################################
#
# 📖 本节定义：
#    1. plot_and_save(): 绘图并自动保存为 PNG + PDF
#    2. 其他可能需要的工具函数
#
# 🎯 使用 plot_and_save() 的优势：
#    ✅ 一次调用，同时完成显示和保存
#    ✅ 自动保存 PNG（适合 PPT/Word）和 PDF（适合发表）
#    ✅ 自动管理输出目录结构
#    ✅ 统一图片尺寸和分辨率（发表级别 300 dpi）
#    ✅ 避免重复运行代码来保存图片
#
################################################################################

#' 绘图并自动保存（PNG + PDF）
#'
#' @description
#' 这是一个包装函数，用于：
#' 1. 在 RMarkdown 中显示图片（print）
#' 2. 自动保存为 PNG（300 dpi，适合 PPT/Word）
#' 3. 自动保存为 PDF（矢量图，适合发表）
#' 4. 自动创建和管理输出目录
#'
#' @param plot_obj ggplot 对象或 patchwork 组合图
#' @param filename 文件名（不含扩展名），例如 "UMAP_clustering"
#' @param subfolder 子文件夹名称，例如 "QC", "Clustering", "DEG", "DoubletFinder"
#'                  会保存到 results/Plots/{mode}/{subfolder}/
#' @param width 图片宽度（英寸），默认 10
#' @param height 图片高度（英寸），默认 8
#' @param dpi 分辨率，默认 300（发表级别）
#'
#' @return 无返回值，直接保存文件并在控制台显示保存路径
#'
#' @examples
#' # 示例1：保存 UMAP 图到 Clustering 子文件夹
#' p <- DimPlot(seurat_obj, reduction = "umap")
#' plot_and_save(p, "UMAP_clusters", "Clustering", width = 12, height = 10)
#'
#' # 示例2：保存组合图到 DEG 子文件夹
#' p_combined <- p1 | p2 | p3
#' plot_and_save(p_combined, "DEG_heatmap", "DEG", width = 18, height = 8)
#'
plot_and_save <- function(plot_obj, filename, subfolder = "",
                          width = 10, height = 8, dpi = 300) {

  # ==================== 步骤1：在 RMarkdown 中显示图片 ====================
  print(plot_obj)

  # ==================== 步骤2：构建保存路径 ====================
  # 基础目录：results/Plots/{mode}/
  base_dir <- paste0(output.dir, "Plots/", config$plot_dir, "/")

  # 如果指定了子文件夹，添加到路径中
  if (subfolder != "") {
    save_dir <- paste0(base_dir, subfolder, "/")
  } else {
    save_dir <- base_dir
  }

  # 创建目录（如果不存在）
  dir.create(save_dir, showWarnings = FALSE, recursive = TRUE)

  # ==================== 步骤3：保存为 PNG ====================
  # PNG 格式：适合 PPT、Word、网页展示
  # 优点：文件小，兼容性好
  png_path <- paste0(save_dir, filename, ".png")
  ggsave(
    filename = png_path,
    plot = plot_obj,
    width = width,
    height = height,
    dpi = dpi,
    bg = "white"  # 白色背景，避免透明背景问题
  )

  # ==================== 步骤4：保存为 PDF ====================
  # PDF 格式：矢量图，适合发表、印刷
  # 优点：无限放大不失真，期刊要求
  pdf_path <- paste0(save_dir, filename, ".pdf")
  ggsave(
    filename = pdf_path,
    plot = plot_obj,
    width = width,
    height = height,
    device = "pdf"
  )

  # ==================== 步骤5：显示保存信息 ====================
  cat("  ✓ 图片已保存:\n")
  cat("    PNG:", png_path, "\n")
  cat("    PDF:", pdf_path, "\n\n")
}

cat("✓ 工具函数加载完成\n")
cat("  - plot_and_save(): 绘图并自动保存 PNG + PDF\n\n")
```

## 1.4 感兴趣的基因列表（用户可自定义）

```{r genes_of_interest}
################################################################################
##################### 感兴趣的基因列表 ##########################################
################################################################################
#
# 📝 使用说明：
#
# 这里定义您在各种可视化中想要查看的基因。
# 修改此处后，后续所有相关可视化都会自动使用这些基因。
#
# 🎯 优势：
# ✅ 集中管理，一次修改全局生效
# ✅ 避免在多处代码中重复修改
# ✅ 便于针对特定研究问题定制分析
# ✅ 便于复用和分享分析流程
#
# 💡 如何使用：
# 1. 根据您的研究兴趣，修改下面的基因列表
# 2. 后续所有 FeaturePlot、DotPlot、Heatmap 等都会使用这些基因
# 3. 也可以临时在特定代码块中覆盖这些设置
#
################################################################################

# ==================== cDC1 相关基因 ====================
# 这些基因用于识别和表征 cDC1 细胞
genes_cDC1 <- c(
  "Xcr1",      # cDC1 经典 marker，趋化因子受体
  "Clec9a",    # cDC1 特异性 C-type lectin，抗原提呈
  "Itgae",     # CD103，组织驻留标志
  "Cadm1",     # 细胞粘附分子
  "Ccr7",      # 成熟和迁移 marker
  "Nr4a3",     # 核受体转录因子
  "Batf3",     # cDC1 发育必需转录因子
  "Irf8"       # cDC1 分化关键转录因子
)

# ==================== cDC2 相关基因 ====================
# 这些基因用于识别和表征 cDC2 细胞
genes_cDC2 <- c(
  "Sirpa",     # cDC2 经典 marker，CD172a
  "Cd24a",     # CD24，成熟 marker
  "Notch2",    # cDC2 发育必需
  "Clec4a4",   # C-type lectin，cDC2 特异
  "Ccr7",      # 成熟和迁移 marker
  "Cd274",     # PD-L1，免疫调节
  "Irf4",      # cDC2 分化关键转录因子
  "Zbtb46"     # DC 特异转录因子
)

# ==================== 免疫细胞 lineage markers ====================
# 用于区分不同免疫细胞类型
genes_lineage <- c(
  "Cd3e",      # T 细胞通用 marker
  "Cd4",       # CD4+ T 细胞
  "Cd8a",      # CD8+ T 细胞
  "Cd19",      # B 细胞
  "Ms4a1",     # B 细胞（CD20）
  "Ncr1",      # NK 细胞
  "Nkg7",      # NK 细胞
  "Ly6c2",     # 单核细胞
  "Ccr2",      # 单核细胞
  "Siglech",   # pDC 特异
  "Bst2",      # pDC marker
  "S100a8",    # 中性粒细胞
  "S100a9"     # 中性粒细胞
)

# ==================== 功能相关基因 ====================
# 用于评估细胞的功能状态
genes_functional <- c(
  "Mki67",     # 增殖 marker
  "Top2a",     # 增殖 marker
  "Il12b",     # 炎症因子，Th1 极化
  "Il12a",     # 炎症因子
  "Ifng",      # 干扰素 gamma
  "Ifnb1",     # I 型干扰素
  "Tnf",       # TNF-alpha
  "Il6",       # IL-6
  "Il10",      # 抗炎因子
  "Tgfb1",     # TGF-beta，免疫抑制
  "Cd86",      # 共刺激分子
  "Cd40"       # 共刺激分子
)

# ==================== 成熟/激活相关基因 ====================
# 用于评估 DC 的成熟和激活状态
genes_maturation <- c(
  "Ccr7",      # 成熟和迁移
  "Cd83",      # 成熟 marker
  "Cd86",      # 共刺激分子，激活
  "Cd40",      # 共刺激分子，激活
  "Il12b",     # 成熟 DC 分泌
  "Relb",      # NF-κB 通路，成熟
  "Ccl22",     # 趋化因子，吸引 Treg
  "Fscn1"      # 成熟 DC marker
)

# ==================== 质控相关基因 ====================
# 用于评估数据质量和识别低质量细胞
genes_qc <- c(
  "Mt-co1",    # 线粒体基因
  "Mt-co2",    # 线粒体基因
  "Mt-nd1",    # 线粒体基因
  "Mt-atp6",   # 线粒体基因
  "Rpl3",      # 核糖体大亚基
  "Rpl4",      # 核糖体大亚基
  "Rps3",      # 核糖体小亚基
  "Rps4x"      # 核糖体小亚基
)

# ==================== 根据分析模式自动选择主要基因 ====================
# 这个逻辑会根据当前分析模式，自动选择最相关的基因列表
# 后续可视化会优先使用 genes_primary

if (analysis_mode == "cDC1") {
  genes_primary <- genes_cDC1
  genes_comparison <- genes_cDC2
  cat("✓ 检测到 cDC1 分析模式\n")
  cat("  主要关注 cDC1 相关基因\n")

} else if (analysis_mode == "cDC2") {
  genes_primary <- genes_cDC2
  genes_comparison <- genes_cDC1
  cat("✓ 检测到 cDC2 分析模式\n")
  cat("  主要关注 cDC2 相关基因\n")

} else if (analysis_mode == "integrated") {
  genes_primary <- c(genes_cDC1, genes_cDC2)
  genes_comparison <- genes_lineage
  cat("✓ 检测到整合分析模式\n")
  cat("  主要关注 cDC1 + cDC2 相关基因\n")

} else if (analysis_mode == "full") {
  genes_primary <- genes_lineage
  genes_comparison <- genes_functional
  cat("✓ 检测到全量分析模式\n")
  cat("  主要关注所有免疫细胞 lineage markers\n")
}

cat("\n📊 基因列表配置完成:\n")
cat("  - 主要基因 (genes_primary):", length(genes_primary), "个\n")
cat("    前5个:", paste(genes_primary[1:min(5, length(genes_primary))], collapse = ", "), "\n")
cat("  - 对比基因 (genes_comparison):", length(genes_comparison), "个\n")
cat("    前5个:", paste(genes_comparison[1:min(5, length(genes_comparison))], collapse = ", "), "\n")
cat("  - 功能基因 (genes_functional):", length(genes_functional), "个\n")
cat("  - 成熟基因 (genes_maturation):", length(genes_maturation), "个\n")
cat("  - 质控基因 (genes_qc):", length(genes_qc), "个\n\n")

cat("💡 自定义提示：\n")
cat("   如需修改可视化的基因，请编辑上方的基因列表\n")
cat("   所有后续可视化都会自动使用这些基因\n\n")
```

## 1.5 配置函数

```{r config_function}
################################################################################
##################### 配置函数: 根据模式返回参数 ###############################
################################################################################

# 动态生成足够数量的颜色
generate_colors <- function(n) {
  if (n <= 0) return(character(0))

  # 组合多个调色板以获得丰富的颜色
  base_colors <- c(
    RColorBrewer::brewer.pal(min(9, max(3, n)), "Set1"),
    RColorBrewer::brewer.pal(min(8, max(3, n)), "Set2"),
    RColorBrewer::brewer.pal(min(12, max(3, n)), "Set3"),
    RColorBrewer::brewer.pal(min(12, max(3, n)), "Paired")
  )

  # 去重并取前 n 个
  unique_colors <- unique(base_colors)

  # 如果还不够，使用 hcl.colors 补充
  if (length(unique_colors) < n) {
    additional <- hcl.colors(n - length(unique_colors), palette = "Dynamic")
    unique_colors <- c(unique_colors, additional)
  }

  return(unique_colors[1:n])
}

get_analysis_config <- function(mode) {
  configs <- list(

    # ==================== cDC1 配置 ====================
    cDC1 = list(
      source_file = "GSE228544_seuratObj_paper_dietSAM2and3_WT_subset_2023.rds.gz",
      # 不预设 cell_types，从 RDS 中自动读取所有 Idents
      cell_types = NULL,  # 将在数据加载时自动填充
      dims_rna = 25,
      dims_adt = 15,
      resolution = 0.8,
      project_name = "GSE228544_cDC1",
      cache_prefix = "cDC1",
      plot_dir = "cDC1",
      colors = NULL,  # 动态生成，见下方函数
      key_markers = c("Xcr1", "Clec9a", "Itgae", "Ccr7", "Nr4a3", "Cebpb")
    ),

    # ==================== cDC2 配置 ====================
    cDC2 = list(
      source_file = "GSE228544_seuratObj_paper_dietSAM2and3_WT_subset2_v2_2023.rds.gz",
      # 不预设 cell_types，从 RDS 中自动读取所有 Idents
      cell_types = NULL,  # 将在数据加载时自动填充
      dims_rna = 30,
      dims_adt = 20,
      resolution = 0.8,
      project_name = "GSE228544_cDC2",
      cache_prefix = "cDC2",
      plot_dir = "cDC2",
      colors = NULL,  # 动态生成，见下方函数
      key_markers = c("Sirpa", "Cd24a", "Notch2", "Ccr7", "Cd274", "Nr4a3")
    ),

    # ==================== 整合分析配置 ====================
    integrated = list(
      source_files = c(
        "GSE228544_seuratObj_paper_dietSAM2and3_WT_subset_2023.rds.gz",
        "GSE228544_seuratObj_paper_dietSAM2and3_WT_subset2_v2_2023.rds.gz"
      ),
      # 不预设，自动读取
      cell_types_list = NULL,
      dims_rna = 25,
      dims_adt = 20,
      resolution = 0.6,
      project_name = "GSE228544_integrated",
      cache_prefix = "integrated",
      plot_dir = "integrated",
      batch_correction = TRUE,
      integration_method = "harmony",
      colors = NULL,  # 动态生成，见下方函数
      key_markers = c("Xcr1", "Clec9a", "Sirpa", "Cd24a", "Ccr7", "Nr4a3")
    ),

    # ==================== 全量分析配置 (新增!) ====================
    full = list(
      source = "10X_raw_data",  # 直接从 10X 数据读取
      cell_types = "auto_discover",  # 自动发现
      expected_types = c("cDC1", "cDC2", "B cells", "T cells",
                        "Monocytes", "NK cells", "pDC", "Neutrophils"),
      dims_rna = 30,
      dims_adt = 25,
      resolution = 0.8,
      project_name = "GSE228544_full",
      cache_prefix = "full",
      plot_dir = "full",
      batch_correction = FALSE,  # 单一数据源，无需批次校正
      colors = NULL,  # 动态生成，见下方函数
      # 已知的细胞类型 marker（用于自动标注）
      marker_genes = list(
        cDC1 = c("Xcr1", "Clec9a", "Itgae", "Cadm1"),
        cDC2 = c("Sirpa", "Cd24a", "Notch2", "Clec4a4"),
        B_cells = c("Cd79a", "Ms4a1", "Cd19"),
        T_cells = c("Cd3e", "Cd3d", "Cd4", "Cd8a"),
        NK_cells = c("Ncr1", "Nkg7", "Klrb1c"),
        Monocytes = c("Ly6c2", "Ccr2", "Cd14"),
        pDC = c("Siglech", "Bst2", "Tcf4"),
        Neutrophils = c("S100a8", "S100a9", "Csf3r")
      )
    )
  )

  if (!mode %in% names(configs)) {
    stop("❌ Invalid analysis_mode: ", mode,
         "\n   Choose from: ", paste(names(configs), collapse = ", "))
  }

  config <- configs[[mode]]

  # 打印配置信息
  cat("\n📋 分析配置加载完成:\n")
  cat("  - 项目名称:", config$project_name, "\n")
  if (mode == "full") {
    cat("  - 数据源: 完整 10X 数据 (所有细胞)\n")
    cat("  - 分析模式: 自动发现细胞类型\n")
  } else if (mode != "integrated") {
    cat("  - 数据源:", config$source_file, "\n")
    cat("  - 细胞类型: 从 RDS 自动读取\n")
  } else {
    cat("  - 数据源: cDC1 + cDC2 (整合)\n")
    cat("  - 批次校正:", config$integration_method, "\n")
  }
  cat("  - RNA维度:", config$dims_rna, " | ADT维度:", config$dims_adt, "\n")
  cat("  - 聚类分辨率:", config$resolution, "\n\n")

  return(config)
}

# 加载配置
config <- get_analysis_config(analysis_mode)

# 创建输出目录
dir.create(output.dir, showWarnings = FALSE, recursive = TRUE)
dir.create(paste0(output.dir, "Robjects"), showWarnings = FALSE)
dir.create(paste0(output.dir, "Cache"), showWarnings = FALSE)
dir.create(paste0(output.dir, "Plots/", config$plot_dir), showWarnings = FALSE, recursive = TRUE)
dir.create(paste0(output.dir, "Plots/", config$plot_dir, "/RNA"), showWarnings = FALSE)
dir.create(paste0(output.dir, "Plots/", config$plot_dir, "/ADT"), showWarnings = FALSE)
dir.create(paste0(output.dir, "Files"), showWarnings = FALSE)
```

## 1.4 智能缓存系统

```{r cache_system}
################################################################################
##################### 智能缓存系统 ##############################################
################################################################################

cache_dir <- paste0(output.dir, "Cache/")

load_or_compute <- function(cache_file, compute_fn, force_recompute = FALSE, description = "") {
  cache_path <- paste0(cache_dir, cache_file)

  if (!force_recompute && file.exists(cache_path)) {
    cat("\n", strrep("=", 80), "\n")
    cat("✓ [缓存加载] ", description, "\n")
    cat("  文件: ", cache_file, "\n")
    cat("  大小: ", round(file.size(cache_path)/1024/1024, 2), " MB\n")
    cat(strrep("=", 80), "\n\n")
    return(readRDS(cache_path))
  } else {
    cat("\n", strrep("=", 80), "\n")
    cat("⚙ [开始计算] ", description, "\n")
    cat("  这可能需要几分钟...\n")
    cat(strrep("=", 80), "\n\n")

    start_time <- Sys.time()
    result <- compute_fn()
    end_time <- Sys.time()

    saveRDS(result, cache_path, compress = TRUE)

    cat("\n", strrep("=", 80), "\n")
    cat("✓ [计算完成] ", description, "\n")
    cat("  耗时: ", round(difftime(end_time, start_time, units = "mins"), 2), " 分钟\n")
    cat("  已保存至: ", cache_file, "\n")
    cat("  大小: ", round(file.size(cache_path)/1024/1024, 2), " MB\n")
    cat(strrep("=", 80), "\n\n")

    return(result)
  }
}

clean_memory <- function(objects_to_remove = NULL) {
  if (!is.null(objects_to_remove)) {
    rm(list = objects_to_remove, envir = .GlobalEnv)
  }
  invisible(gc(verbose = FALSE))
}

cat("✓ 智能缓存系统已加载\n")
cat("  缓存目录:", cache_dir, "\n\n")
```

---

# 2. 数据加载

## 2.1 加载原始 10X 数据

```{r load_raw_data}
################################################################################
##################### 加载原始 10X 数据 ########################################
################################################################################

cat("📦 加载原始 10X 数据...\n")
rawData <- Read10X(data.dir = data_dir)
rawDataRNA <- rawData$`Gene Expression`
rawDataADT <- rawData$`Antibody Capture`

cat("✓ 数据加载完成\n")
cat("  RNA 特征数:", nrow(rawDataRNA), "\n")
cat("  ADT 特征数:", nrow(rawDataADT), "\n")
cat("  细胞条形码数:", ncol(rawDataRNA), "\n\n")
```

## 2.2 提取细胞子集 (模式自适应)

```{r extract_subset}
################################################################################
##################### 提取细胞子集 (根据分析模式) ################################
################################################################################

if (analysis_mode %in% c("cDC1", "cDC2")) {

  # ==================== 单一细胞类型提取 ====================
  subset_data <- load_or_compute(
    cache_file = paste0(config$cache_prefix, "_01_subset_extracted.rds"),
    description = paste0(config$cache_prefix, " 子集提取"),
    compute_fn = function() {
      # 解压和读取
      full_path <- file.path(data_dir, config$source_file)
      cat("  解压文件:", config$source_file, "\n")
      try(gunzip(full_path, remove = FALSE, overwrite = TRUE), silent = TRUE)

      rds_path <- gsub("\\.gz$", "", full_path)
      cat("  读取 Seurat 对象...\n")
      subset_seurat <- readRDS(rds_path)

      cat("  原始对象细胞类型分布:\n")
      print(table(Idents(subset_seurat)))

      # 自动获取所有可用的 Idents
      all_idents <- levels(Idents(subset_seurat))
      cat("\n  ✓ 检测到", length(all_idents), "种细胞类型:\n")
      cat("   ", paste(all_idents, collapse = ", "), "\n")

      # 更新配置中的 cell_types
      config$cell_types <<- all_idents

      subset_seurat <- UpdateSeuratObject(subset_seurat)
      # 提取所有细胞（不过滤）
      cells_to_keep <- colnames(subset_seurat)

      # 过滤数据
      rawDataRNA_filtered <- rawDataRNA[, cells_to_keep]
      rawDataADT_filtered <- rawDataADT[, cells_to_keep]

      cat("\n  ✓ 提取完成:\n")
      cat("    细胞数:", length(cells_to_keep), "\n")
      cat("    RNA 特征:", nrow(rawDataRNA_filtered), "\n")
      cat("    ADT 特征:", nrow(rawDataADT_filtered), "\n")

      return(list(
        rawDataRNA = rawDataRNA_filtered,
        rawDataADT = rawDataADT_filtered,
        cells = cells_to_keep,
        metadata = subset_seurat@meta.data[cells_to_keep, ],
        original_idents = Idents(subset_seurat)[cells_to_keep],
        cell_types = all_idents
      ))
    }
  )

} else if (analysis_mode == "integrated") {

  # ==================== cDC1 + cDC2 整合提取 ====================
  subset_data <- load_or_compute(
    cache_file = "integrated_01_subset_extracted.rds",
    description = "cDC1 + cDC2 整合数据提取",
    compute_fn = function() {
      all_cells <- list()
      all_metadata <- list()
      all_idents <- list()
      all_cell_types <- list()

      subset_names <- c("cDC1", "cDC2")

      for (i in seq_along(config$source_files)) {
        subset_name <- subset_names[i]
        file_path <- file.path(data_dir, config$source_files[i])

        cat("\n  处理", subset_name, "...\n")
        cat("  解压:", basename(file_path), "\n")
        try(gunzip(file_path, remove = FALSE, overwrite = TRUE), silent = TRUE)

        rds_path <- gsub("\\.gz$", "", file_path)
        seurat_obj <- readRDS(rds_path)
        seurat_obj <- UpdateSeuratObject(seurat_obj)

        # 自动获取所有 Idents
        cell_types <- levels(Idents(seurat_obj))
        cat("  检测到细胞类型:", paste(cell_types, collapse = ", "), "\n")
        all_cell_types[[subset_name]] <- cell_types

        # 提取所有细胞（不过滤）
        cells <- colnames(seurat_obj)
        cat("  提取细胞数:", length(cells), "\n")

        all_cells[[subset_name]] <- cells

        # 添加批次标签
        metadata <- seurat_obj@meta.data[cells, ]
        metadata$subset <- subset_name
        metadata$original_cluster <- as.character(Idents(seurat_obj)[cells])
        all_metadata[[subset_name]] <- metadata
        all_idents[[subset_name]] <- Idents(seurat_obj)[cells]
      }

      # 更新配置
      config$cell_types_list <<- all_cell_types

      # 合并
      combined_cells <- unlist(all_cells)
      combined_metadata <- do.call(rbind, all_metadata)
      combined_idents <- unlist(all_idents)

      cat("\n  ✓ 整合完成:\n")
      cat("    cDC1 细胞:", length(all_cells$cDC1), "\n")
      cat("    cDC2 细胞:", length(all_cells$cDC2), "\n")
      cat("    总计:", length(combined_cells), "个细胞\n")

      # 过滤 RNA 和 ADT
      rawDataRNA_filtered <- rawDataRNA[, combined_cells]
      rawDataADT_filtered <- rawDataADT[, combined_cells]

      return(list(
        rawDataRNA = rawDataRNA_filtered,
        rawDataADT = rawDataADT_filtered,
        cells = combined_cells,
        metadata = combined_metadata,
        batch_info = combined_metadata$subset,
        original_idents = combined_idents,
        cell_counts = sapply(all_cells, length),
        cell_types_list = all_cell_types
      ))
    }
  )

} else if (analysis_mode == "full") {

  # ==================== 全量数据分析 (新增!) ====================
  subset_data <- list(
    rawDataRNA = rawDataRNA,
    rawDataADT = rawDataADT,
    cells = colnames(rawDataRNA),
    metadata = NULL  # 将在后续创建
  )

  cat("\n✓ 全量数据模式\n")
  cat("  使用所有细胞:", ncol(rawDataRNA), "个\n")
  cat("  将自动发现细胞类型\n\n")
}

# 提取到全局变量
rawDataRNA <- subset_data$rawDataRNA
rawDataADT <- subset_data$rawDataADT
metadata_original <- subset_data$metadata

if (analysis_mode == "integrated") {
  batch_info <- subset_data$batch_info
  cat("\n批次信息:\n")
  print(table(batch_info))
}

cat("\n✓ 子集提取完成\n\n")

# 清理
rm(subset_data)
gc()
```

---

# 3. 质控

## 3.1 构建 SCE 对象并计算 QC 指标

```{r qc_sce}
################################################################################
##################### 质控: SCE 对象构建 ########################################
################################################################################

library(SingleCellExperiment)
library(scuttle)

# 确保是稀疏矩阵
if(!is(rawDataRNA, "dgCMatrix")) {
  rawDataRNA <- Matrix::Matrix(as.matrix(rawDataRNA), sparse = TRUE)
}

sce <- SingleCellExperiment(list(counts=rawDataRNA))

# QC 计算
is.mito <- grepl("^MT-|^mt-", rownames(sce), ignore.case = TRUE)
sce <- addPerCellQC(sce, subsets=list(Mito=is.mito))

# 提取 Metadata
plot_df <- as.data.frame(colData(sce))

cat("✓ SCE 对象构建完成\n")
cat("  细胞数:", ncol(sce), "\n")
cat("  基因数:", nrow(sce), "\n")
cat("  线粒体基因数:", sum(is.mito), "\n\n")
```

## 3.2 QC 可视化

> **为什么需要 QC 可视化？**
>
> 在设定质控阈值前，必须先可视化数据分布，了解：
> 1. **基因检测数（nGene）分布**：判断细胞质量，正常细胞通常检测到 500-5000 个基因
> 2. **线粒体比例分布**：识别损伤细胞，健康细胞通常 < 10-20%
> 3. **UMI vs 线粒体相关性**：检查是否存在系统性偏差
>
> **如何解读这些图？**
>
> - **小提琴图**：显示分布的整体形状（单峰/双峰）和离群值
> - **散点图**：识别异常细胞群（如高线粒体 + 低 UMI 的损伤细胞）

```{r qc_visualization, fig.width=12, fig.height=4}
################################################################################
##################### QC 可视化：过滤前的数据分布 ##################################
################################################################################
#
# 【功能说明】
# 在设定质控阈值前，先可视化数据的原始分布，识别潜在的质量问题
#
# 【图表说明】
# 图1：基因检测数分布（nGene）
#   - 正常范围：500-5000 基因/细胞
#   - 过低：空液滴或损伤细胞
#   - 过高：可能是 doublets
#
# 图2：线粒体比例分布（Mito %）
#   - 正常范围：< 10-20%
#   - 过高：细胞膜破损，胞质 mRNA 丢失
#
# 图3：UMI vs 线粒体相关性
#   - 预期：无显著相关
#   - 如果高线粒体细胞集中在低 UMI 区域 → 说明质控策略合理
#
################################################################################

# 小提琴图：基因检测数
# 用途：观察基因检测数的整体分布，识别低质量细胞群
p1 <- ggplot(plot_df, aes(x="Sample", y=detected)) +
  geom_violin(fill="#56B4E9", alpha=0.7) +  # 蓝色小提琴
  geom_jitter(size=0.1, alpha=0.2) +        # 叠加散点显示单个细胞
  theme_classic() +
  labs(title="Detected Genes per Cell",
       subtitle="Typical range: 500-5000 genes",
       y="nGene (detected features)", x="")

# 小提琴图：线粒体比例
# 用途：识别损伤细胞（线粒体比例异常高）
p2 <- ggplot(plot_df, aes(x="Sample", y=subsets_Mito_percent)) +
  geom_violin(fill="#E69F00", alpha=0.7) +  # 橙色小提琴
  geom_jitter(size=0.1, alpha=0.2) +
  geom_hline(yintercept = 20, linetype="dashed", color="red", alpha=0.5) +  # 参考线：20%
  theme_classic() +
  labs(title="Mito Percentage",
       subtitle="Typical threshold: < 10-20%",
       y="Mito %", x="")

# 散点图：UMI vs 线粒体比例
# 用途：检查 UMI 和线粒体比例的相关性
# 正常情况：高 UMI 细胞应该有低线粒体比例
# 异常情况：如果高 UMI 细胞也有高线粒体 → 可能是技术问题
p3 <- ggplot(plot_df, aes(x=sum, y=subsets_Mito_percent)) +
  geom_point(size=0.5, alpha=0.4, color="darkblue") +
  geom_hline(yintercept = 20, linetype="dashed", color="red", alpha=0.5) +
  theme_minimal() +
  labs(title="Metadata Correlation",
       subtitle="Check for systematic bias",
       x="Total UMI (nUMI)", y="Mito %")

# 组合三个图表
p_qc_before <- p1 + p2 + p3

# 保存图片（在 QC 子文件夹）
plot_and_save(p_qc_before, "qc_metrics_before_filtering",
              subfolder = "QC", width = 12, height = 4)
```

> **🔍 如何解读 QC 可视化结果？**
>
> **正常的数据分布**：
> - **nGene**：单峰分布，中位数在 1000-3000 之间
> - **Mito %**：单峰分布，大部分细胞 < 10%，少数离群值 > 20%
> - **UMI vs Mito**：大部分细胞聚集在高 UMI + 低 Mito 区域
>
> **异常信号**：
> - **nGene 双峰分布**：可能存在两种质量的细胞群，需要仔细检查
> - **Mito % > 20% 的细胞很多**：样本可能受损，或者解离过程有问题
> - **高 UMI 细胞也有高 Mito**：可能是技术问题，而非生物学差异

## 3.3 自动化质控过滤

> **为什么使用自动化过滤？**
>
> 传统的硬阈值（如 nGene > 200, Mito < 5%）无法适应不同样本的异质性。自动化过滤基于**中位数绝对偏差（MAD）**，能够：
> 1. 自动适应数据分布
> 2. 识别统计学意义上的离群值
> 3. 对不同质量的样本使用不同的阈值
>
> **MAD 方法原理**：
> - 计算数据的中位数和 MAD
> - 定义离群值为：偏离中位数超过 N × MAD 的数据点
> - N（nmads）通常设为 3-5（保守）或 5-10（宽松）

```{r qc_filtering}
################################################################################
##################### 质控: 自动化过滤 ##########################################
################################################################################
#
# 【方法说明】
# 使用基于 MAD (Median Absolute Deviation) 的自适应阈值
# 优势：不需要手动设定硬阈值（如 nGene > 200），自动适应数据分布
#
# 【参数说明】
# nmads = 5：表示偏离中位数 5 倍 MAD 的细胞被视为离群值
#   - nmads 越小 → 过滤越严格（剔除更多细胞）
#   - nmads 越大 → 过滤越宽松（保留更多细胞）
#   - 典型范围：3（严格）、5（适中）、10（宽松）
#
# 【过滤策略】
# 1. UMI 总数（sum）：双侧过滤（过低或过高都剔除）
#    - 过低：可能是空液滴或损伤细胞
#    - 过高：可能是 doublets（但 DoubletFinder 会进一步处理）
# 2. 基因检测数（detected）：双侧过滤
#    - 过低：低质量细胞
#    - 过高：doublets
# 3. 线粒体比例（Mito %）：单侧过滤（只剔除过高的）
#    - 过高：细胞膜破损
#
################################################################################

# 设置 QC 参数
# 注意：full 模式和 subset 模式目前使用相同参数，可根据需要调整
if (analysis_mode == "full") {
  # 全量模式：包含所有细胞类型，使用宽松参数避免误删稀有细胞类型
  nmad_low_feature  <- 5   # 基因数下限：5 MAD（适中）
  nmad_high_feature <- 5   # 基因数上限：5 MAD（适中）
  nmad_low_UMI      <- 5   # UMI 下限：5 MAD（适中）
  nmad_high_UMI     <- 5   # UMI 上限：5 MAD（适中）
  nmad_high_mito    <- 10  # 线粒体上限：10 MAD（宽松，避免误删高代谢细胞）
} else {
  # 子集模式：cDC1/cDC2，细胞类型相对均一，可以使用相同参数
  nmad_low_feature  <- 5
  nmad_high_feature <- 5
  nmad_low_UMI      <- 5
  nmad_high_UMI     <- 5
  nmad_high_mito    <- 10
}

cat("使用的 QC 参数（nmads）：\n")
cat("  基因数下限/上限:", nmad_low_feature, "/", nmad_high_feature, "\n")
cat("  UMI 下限/上限:", nmad_low_UMI, "/", nmad_high_UMI, "\n")
cat("  线粒体上限:", nmad_high_mito, "\n\n")

# 离群值识别
# type = "both": 检测双侧离群值（过低和过高）
# type = "higher": 只检测单侧离群值（过高）
# log = TRUE: 对数据取对数后再计算 MAD（适用于偏态分布）
outlier_sum <- isOutlier(sce$sum, nmads = nmad_low_UMI, type = "both", log = TRUE)
outlier_detected <- isOutlier(sce$detected, nmads = nmad_low_feature, type = "both", log = TRUE)
outlier_mito <- isOutlier(sce$subsets_Mito_percent, nmads = nmad_high_mito, type = "higher")

# 汇总过滤结果
# 只要满足任意一个过滤条件，细胞就被标记为 discard = TRUE
sce$discard <- outlier_sum | outlier_detected | outlier_mito

# 打印统计信息
cat("【自动化质控统计】\n")
cat("  低 UMI 剔除细胞数:", sum(outlier_sum), sprintf("(%.1f%%)", 100*sum(outlier_sum)/ncol(sce)), "\n")
cat("  低基因数剔除细胞数:", sum(outlier_detected), sprintf("(%.1f%%)", 100*sum(outlier_detected)/ncol(sce)), "\n")
cat("  高线粒体剔除细胞数:", sum(outlier_mito), sprintf("(%.1f%%)", 100*sum(outlier_mito)/ncol(sce)), "\n")
cat("  总计剔除细胞数:", sum(sce$discard), sprintf("(%.1f%%)", 100*sum(sce$discard)/ncol(sce)), "\n")
cat("  保留细胞数:", sum(!sce$discard), sprintf("(%.1f%%)", 100*sum(!sce$discard)/ncol(sce)), "\n\n")

# 提取自动计算的阈值（用于后续可视化）
thresholds <- list(
  sum = attr(outlier_sum, "thresholds")["lower"],
  detected = attr(outlier_detected, "thresholds")["lower"],
  mito = attr(outlier_mito, "thresholds")["higher"]
)

cat("【自动计算的阈值】\n")
cat("  UMI 下限:", round(thresholds$sum, 0), "\n")
cat("  基因数下限:", round(thresholds$detected, 0), "\n")
cat("  线粒体上限:", round(thresholds$mito, 2), "%\n\n")
```

> **🔍 如何评估过滤效果？**
>
> **合理的过滤比例**：
> - **总过滤率 < 10-20%**：说明样本质量良好
> - **总过滤率 20-40%**：中等质量，需要检查实验流程
> - **总过滤率 > 40%**：质量较差，可能需要调整 nmads 参数或重新实验
>
> **各指标的预期过滤率**：
> - **低 UMI**：通常 < 5%（除非样本有大量空液滴）
> - **低基因数**：通常 < 5%
> - **高线粒体**：通常 5-15%（取决于组织类型和解离方法）

## 3.4 QC 过滤可视化

> **如何评估过滤效果？**
>
> 这个散点图展示了自动计算的阈值（红色虚线）如何将细胞分为"保留"（绿色）和"剔除"（黄色）。通过这个图，您可以：
> 1. 确认阈值是否合理（是否准确分离了低质量细胞）
> 2. 检查是否存在边界模糊的区域（需要人工判断）
> 3. 识别异常的细胞群（如高 UMI + 高 Mito 的技术问题细胞）

```{r qc_filter_plot, fig.width=8, fig.height=6}
################################################################################
##################### QC 过滤效果可视化 ##########################################
################################################################################
#
# 【功能说明】
# 展示自动计算的阈值如何将细胞分为"保留"和"剔除"两类
#
# 【图表解读】
# - 绿色点：保留的细胞（通过质控）
# - 黄色点：剔除的细胞（未通过质控）
# - 红色虚线：自动计算的阈值
#
# 【检查要点】
# 1. 剔除的细胞是否主要集中在低 UMI + 高 Mito 区域？（合理）
# 2. 是否有大量高 UMI + 低 Mito 的细胞被剔除？（可疑，需要调整参数）
# 3. 边界是否清晰？（模糊的边界可能需要人工判断）
#
################################################################################

# 提取 SCE 对象的 metadata
plot_df <- as.data.frame(colData(sce))

# 绘制散点图：UMI vs 线粒体，按过滤结果着色
p_auto_qc <- ggplot(plot_df, aes(x = sum, y = subsets_Mito_percent, color = discard)) +
  geom_point(size = 0.5, alpha = 0.5) +
  # 颜色方案：绿色（保留）、黄色（剔除）
  scale_color_manual(values = c("FALSE" = "#21918c", "TRUE" = "#fde725"),
                     labels = c("FALSE" = "Retained", "TRUE" = "Discarded")) +
  # 添加阈值线（红色虚线）
  geom_hline(yintercept = thresholds$mito, linetype = "dashed", color = "red", size = 0.8) +
  geom_vline(xintercept = thresholds$sum, linetype = "dashed", color = "red", size = 0.8) +
  # 添加阈值标注
  annotate("text", x = thresholds$sum, y = max(plot_df$subsets_Mito_percent) * 0.9,
           label = paste0("UMI threshold: ", round(thresholds$sum, 0)),
           color = "red", size = 3, hjust = -0.1) +
  annotate("text", x = max(plot_df$sum) * 0.9, y = thresholds$mito,
           label = paste0("Mito threshold: ", round(thresholds$mito, 2), "%"),
           color = "red", size = 3, vjust = -0.5) +
  theme_minimal(base_size = 12) +
  labs(title = paste0("Adaptive QC Thresholds - ", config$project_name),
       subtitle = paste0("Retained: ", sum(!sce$discard), " cells | Discarded: ", sum(sce$discard), " cells (",
                        round(100*sum(sce$discard)/ncol(sce), 1), "%)"),
       x = "Total UMI Count", y = "Mitochondrial %",
       color = "Status")

# 保存图片
plot_and_save(p_auto_qc, "qc_adaptive_thresholds",
              subfolder = "QC", width = 10, height = 8)

# 执行过滤
sce_clean <- sce[, !sce$discard]

cat("\n", strrep("=", 80), "\n")
cat("✓ 质控过滤完成\n")
cat(strrep("=", 80), "\n")
cat("  过滤前细胞数:", ncol(sce), "\n")
cat("  过滤后细胞数:", ncol(sce_clean), "\n")
cat("  剔除细胞数:", sum(sce$discard), sprintf("(%.1f%%)\n", 100*sum(sce$discard)/ncol(sce)))
cat("  剔除原因分布：\n")
cat("    - 低 UMI:", sum(outlier_sum), sprintf("(%.1f%%)\n", 100*sum(outlier_sum)/ncol(sce)))
cat("    - 低基因数:", sum(outlier_detected), sprintf("(%.1f%%)\n", 100*sum(outlier_detected)/ncol(sce)))
cat("    - 高线粒体:", sum(outlier_mito), sprintf("(%.1f%%)\n", 100*sum(outlier_mito)/ncol(sce)))
cat(strrep("=", 80), "\n\n")
```

> **🔍 过滤结果判断指南**
>
> **理想的过滤效果**：
> - 剔除的细胞（黄色）主要位于**左下角**（低 UMI）和**右上角**（高 Mito）
> - 保留的细胞（绿色）形成清晰的主群体
> - 两类细胞之间有明确的边界
>
> **需要警惕的情况**：
> - **大量高质量细胞被剔除**：如果绿色主群体中有明显的黄色点 → 参数过严，建议增大 nmads
> - **低质量细胞未被剔除**：如果左下角或右上角有大量绿色点 → 参数过松，建议减小 nmads
> - **边界模糊**：如果黄色和绿色点混杂 → 可能需要手动检查边界细胞

---

# 4. 归一化

```{r normalization}
################################################################################
##################### Scran 归一化 ##############################################
################################################################################

sce_clean <- load_or_compute(
  cache_file = paste0(config$cache_prefix, "_03_sce_normalized.rds"),
  description = paste0("Scran 归一化 - ", config$cache_prefix),
  compute_fn = function() {
    library(scran)
    library(scuttle)

    cat("  步骤 1/3: quickCluster...\n")
    set.seed(100)
    quick_clusters <- quickCluster(sce_clean)

    cat("  步骤 2/3: computePooledFactors...\n")
    sce_clean <- computePooledFactors(sce_clean, clusters = quick_clusters)

    cat("  步骤 3/3: logNormCounts...\n")
    sce_clean <- logNormCounts(sce_clean)

    cat("\nSize Factors 统计:\n")
    print(summary(sizeFactors(sce_clean)))

    return(sce_clean)
  }
)

# 同步原始矩阵
rawDataFiltered <- rawDataRNA[rownames(sce_clean), colnames(sce_clean)]

cat("\n✓ 归一化完成\n")
cat("  细胞数:", ncol(sce_clean), "\n")
cat("  基因数:", nrow(sce_clean), "\n\n")
```

## 4.1 选择高变基因

```{r hvg}
library(scran)

dec <- modelGeneVar(sce_clean)

# 可视化
plot(dec$mean, dec$total, xlab="Mean log-expression", ylab="Variance")
curve(metadata(dec)$trend(x), add=TRUE, col="blue", lwd=2)

# 选择前 2000 个高变基因
top_hvgs <- getTopHVGs(dec, n=2000)
rowSubset(sce_clean) <- top_hvgs

cat("✓ 选择了", length(top_hvgs), "个高变基因\n\n")
```

---

# 5. Seurat 对象创建和聚类

```{r seurat_clustering}
################################################################################
##################### Seurat 聚类 (模式自适应) ##################################
################################################################################

seurat_obj <- load_or_compute(
  cache_file = paste0(config$cache_prefix, "_04_seurat_clustered.rds"),
  description = paste0("Seurat 聚类 - ", config$cache_prefix),
  compute_fn = function() {
    library(Seurat)

    cat("  步骤 1/8: 创建 Seurat 对象...\n")
    seurat_obj <- CreateSeuratObject(counts = rawDataFiltered, project = config$project_name)

    cat("  步骤 2/8: 注入 scran logcounts...\n")
    seurat_obj[["RNA"]]$data <- as(logcounts(sce_clean), "dgCMatrix")
    seurat_obj$percent.mito <- sce_clean$subsets_Mito_percent

    # 添加原始细胞类型信息
    if (exists("metadata_original", envir = .GlobalEnv) && !is.null(metadata_original)) {
      if ("original_cluster" %in% colnames(metadata_original)) {
        seurat_obj$original_cluster <- metadata_original$original_cluster[colnames(seurat_obj)]
      }
    }

    # 添加批次信息(整合模式)
    if (analysis_mode == "integrated") {
      seurat_obj$batch <- batch_info[colnames(seurat_obj)]
    }

    cat("  步骤 3/8: 运行 SCTransform...\n")
    seurat_obj <- SCTransform(seurat_obj, assay = "RNA", new.assay.name = "SCT",
                               vars.to.regress = "percent.mito", verbose = FALSE)

    cat("  步骤 4/8: 运行 PCA...\n")
    seurat_obj <- RunPCA(seurat_obj, assay = "SCT", npcs = 50, verbose = FALSE)

    # ==================== 整合分析特殊处理: Harmony 批次校正 ====================
    reduction_to_use <- "pca"
    if (analysis_mode == "integrated" && config$batch_correction) {
      if (require("harmony", quietly = TRUE)) {
        cat("  步骤 5/8: Harmony 批次校正...\n")
        seurat_obj <- RunHarmony(
          seurat_obj,
          group.by.vars = "batch",
          reduction = "pca",
          dims.use = 1:config$dims_rna,
          verbose = FALSE
        )
        reduction_to_use <- "harmony"
      } else {
        warning("Harmony 包未安装，跳过批次校正")
      }
    } else {
      cat("  步骤 5/8: 跳过批次校正 (非整合模式)...\n")
    }

    cat("  步骤 6/8: 运行 UMAP...\n")
    seurat_obj <- RunUMAP(seurat_obj, dims = 1:config$dims_rna,
                          reduction = reduction_to_use, verbose = FALSE,
                          reduction.name = "umap", reduction.key = "UMAP_")

    cat("  步骤 7/8: 运行 tSNE...\n")
    seurat_obj <- RunTSNE(seurat_obj, dims = 1:config$dims_rna,
                          reduction = reduction_to_use, verbose = FALSE,
                          reduction.name = "tsne", reduction.key = "tSNE_",
                          perplexity = 30)

    cat("  步骤 8/8: 聚类分析...\n")
    seurat_obj <- FindNeighbors(seurat_obj, dims = 1:config$dims_rna,
                                reduction = reduction_to_use, verbose = FALSE)
    seurat_obj <- FindClusters(seurat_obj, resolution = config$resolution, verbose = FALSE)

    cat("  ✓ 完成!\n")
    return(seurat_obj)
  }
)

cat("\n✓ Seurat 聚类完成\n")
cat("  聚类数:", length(unique(Idents(seurat_obj))), "\n\n")
```

## 5.1 基础可视化

```{r basic_visualization, fig.width=14, fig.height=6}
# 动态生成足够的颜色
n_clusters <- length(unique(Idents(seurat_obj)))
if (is.null(config$colors)) {
  config$colors <- generate_colors(n_clusters)
  cat("  生成", n_clusters, "种颜色用于可视化\n")
}

# UMAP
p_umap <- DimPlot(seurat_obj, reduction = "umap", label = TRUE, cols = config$colors) +
  labs(title = paste0(config$project_name, " - UMAP Clustering"))

# tSNE
p_tsne <- DimPlot(seurat_obj, reduction = "tsne", label = TRUE, cols = config$colors) +
  labs(title = paste0(config$project_name, " - tSNE"))

print(p_umap + p_tsne)

# 如果是整合分析,显示批次分布
if (analysis_mode == "integrated") {
  p_batch <- DimPlot(seurat_obj, reduction = "umap", group.by = "batch") +
    labs(title = "Batch Distribution (cDC1 vs cDC2)")

  p_original <- DimPlot(seurat_obj, reduction = "umap", group.by = "original_cluster",
                       label = TRUE, repel = TRUE) +
    labs(title = "Original Cell Type Annotations")

  print(p_batch + p_original)
}
```

## 5.2 全量模式：自动细胞类型标注

```{r auto_annotation, eval=(analysis_mode == "full"), fig.width=14, fig.height=10}
if (analysis_mode == "full") {
  cat("🔍 开始自动细胞类型标注...\n")

  # 计算每个聚类的 marker 基因
  all_markers <- FindAllMarkers(seurat_obj, assay = "SCT", only.pos = TRUE,
                                min.pct = 0.25, logfc.threshold = 0.25, verbose = FALSE)

  # 简单的自动标注逻辑
  cluster_annotation <- setNames(rep("Unknown", length(unique(Idents(seurat_obj)))),
                                levels(Idents(seurat_obj)))

  for (cluster_id in names(cluster_annotation)) {
    cluster_markers <- all_markers %>% filter(cluster == cluster_id) %>%
      slice_max(order_by = avg_log2FC, n = 10)

    # 检查已知 marker
    for (cell_type in names(config$marker_genes)) {
      known_markers <- config$marker_genes[[cell_type]]
      overlap <- sum(cluster_markers$gene %in% known_markers)

      if (overlap >= 2) {  # 至少2个 marker 匹配
        cluster_annotation[cluster_id] <- cell_type
        break
      }
    }
  }

  # 添加标注
  seurat_obj$cell_type_predicted <- cluster_annotation[as.character(Idents(seurat_obj))]

  # 可视化
  p1 <- DimPlot(seurat_obj, group.by = "seurat_clusters", label = TRUE) +
    labs(title = "Original Clusters")

  p2 <- DimPlot(seurat_obj, group.by = "cell_type_predicted", label = TRUE, repel = TRUE) +
    labs(title = "Predicted Cell Types")

  print(p1 / p2)

  # 统计
  cat("\n预测的细胞类型分布:\n")
  print(table(seurat_obj$cell_type_predicted))
}
```

## 5.3 Elbow Plot

> **如何使用 Elbow Plot 选择 PC 数量？**
>
> Elbow Plot 显示每个 PC 解释的方差比例。通常选择"拐点"处的 PC 数量，即曲线开始变平的位置。
>
> - **保守策略**：选择拐点（如 PC 15-20）
> - **宽松策略**：选择更多 PC（如 PC 30-50），包含更多生物学变异

```{r elbow_plot, fig.width=8, fig.height=6}
# 生成 Elbow Plot
p_elbow <- ElbowPlot(seurat_obj, ndims = 50) +
  geom_vline(xintercept = config$dims_rna, linetype = "dashed", color = "red") +
  labs(title = "Elbow Plot - PC Selection",
       subtitle = paste0("Current setting: PC 1-", config$dims_rna)) +
  theme_minimal()

# 显示并保存
plot_and_save(p_elbow, "elbow_plot_pc_selection",
              subfolder = "Clustering", width = 8, height = 6)
```

---

# 6. 双细胞检测

## 6.1 DoubletFinder 函数定义

```{r doubletfinder_function}
################################################################################
##################### DoubletFinder 函数 ########################################
################################################################################

runDoubletFinder <- function(object, PCs, minPCT = 1, maxPCT = 10, pN = 0.25, cluster_col = NULL) {
  require(dplyr)
  require(ggplot2)
  require(patchwork)
  require(Seurat)

  # Seurat v5 适配 - 检查并合并所有 assay 的层
  if (is(object, "Seurat")) {
    available_assays <- SeuratObject::Assays(object)

    # 检查 RNA assay
    if ("RNA" %in% available_assays && is(object[["RNA"]], "Assay5")) {
      message(">>> 检测到 Seurat v5 RNA assay，正在执行 JoinLayers...")
      object[["RNA"]] <- JoinLayers(object[["RNA"]])
    }
    # 检查 SCT assay (SCTransform 后的主要 assay)
    if ("SCT" %in% available_assays && is(object[["SCT"]], "Assay5")) {
      message(">>> 检测到 Seurat v5 SCT assay，正在执行 JoinLayers...")
      object[["SCT"]] <- JoinLayers(object[["SCT"]])
    }
    # 检查 ADT assay (CITE-seq)
    if ("ADT" %in% available_assays && is(object[["ADT"]], "Assay5")) {
      message(">>> 检测到 Seurat v5 ADT assay，正在执行 JoinLayers...")
      object[["ADT"]] <- JoinLayers(object[["ADT"]])
    }
  }

  # 第一步：删除所有旧的 DoubletFinder 列（如果存在）
  df_cols_to_remove <- grep("^pANN_|^DF\\.classifications|^DFPrediction",
                             colnames(object@meta.data), value = TRUE)
  if (length(df_cols_to_remove) > 0) {
    message(">>> 删除旧的 DoubletFinder 列: ", paste(df_cols_to_remove, collapse = ", "))
    object@meta.data <- object@meta.data[, !colnames(object@meta.data) %in% df_cols_to_remove, drop = FALSE]
  }

  # 清理 metadata - 移除可能导致 xtfrm 错误的列
  # 保存原始 metadata 的备份
  original_meta <- object@meta.data

  # 检查并移除数据框类型的列
  problematic_cols <- c()
  for (col_name in colnames(object@meta.data)) {
    col_data <- object@meta.data[[col_name]]
    # 如果列是数据框、列表或其他复杂类型，记录下来
    if (is.data.frame(col_data) || is.list(col_data) || is.matrix(col_data)) {
      problematic_cols <- c(problematic_cols, col_name)
    }
  }

  if (length(problematic_cols) > 0) {
    message(">>> 检测到复杂类型的 metadata 列: ", paste(problematic_cols, collapse = ", "))
    message(">>> 临时移除这些列以避免 DoubletFinder 错误")
    # 临时移除这些列
    object@meta.data <- object@meta.data[, !colnames(object@meta.data) %in% problematic_cols, drop = FALSE]
  }

  # 额外保护：确保所有 factor 列都是简单的 factor（不是 ordered factor）
  for (col_name in colnames(object@meta.data)) {
    if (is.factor(object@meta.data[[col_name]])) {
      object@meta.data[[col_name]] <- factor(as.character(object@meta.data[[col_name]]))
    }
  }

  # 确保 Idents 也是简单的 factor
  Idents(object) <- factor(as.character(Idents(object)))

  message(">>> Metadata 清理完成，准备运行 DoubletFinder")

  df_ns <- asNamespace("DoubletFinder")
  param_fun <- if (exists("paramSweep_v3", envir = df_ns)) {
    get("paramSweep_v3", envir = df_ns)
  } else {
    get("paramSweep", envir = df_ns)
  }
  sum_fun   <- get("summarizeSweep", envir = df_ns)
  pk_fun    <- get("find.pK", envir = df_ns)
  df_main_fun <- if (exists("doubletFinder_v3", envir = df_ns)) {
    get("doubletFinder_v3", envir = df_ns)
  } else {
    get("doubletFinder", envir = df_ns)
  }

  # 线粒体检测
  if (!"percent.mito" %in% colnames(object@meta.data)) {
    object[["percent.mito"]] <- PercentageFeatureSet(object, pattern = "^MT-|^mt-|^Mt-")
  }

  # ==================== 设置随机种子确保可重复性 ====================
  set.seed(123)
  message(">>> 设置随机种子为 123，确保结果可重复")

  # 参数寻优
  is_sct <- "SCT" %in% SeuratObject::Assays(object)
  if (length(PCs) == 1) PCs <- 1:PCs

  sweep.res <- param_fun(object, PCs = PCs, sct = is_sct)
  sweep.stats <- sum_fun(sweep.res, GT = FALSE)
  bcmvn <- pk_fun(sweep.stats)

  pKValue_char <- as.character(bcmvn$pK[which.max(bcmvn$BCmetric)])
  pKValue <- as.numeric(pKValue_char)
  message(">>> 选定最佳 pK: ", pKValue)

  # 保存 pK sweep 结果到 misc，以便缓存加载后重新绘图
  if (!is.null(object@misc)) {
    object@misc$doubletfinder_pk_sweep <- bcmvn
    object@misc$doubletfinder_pk_value <- pKValue
  } else {
    object@misc <- list(
      doubletfinder_pk_sweep = bcmvn,
      doubletfinder_pk_value = pKValue
    )
  }
  message(">>> pK sweep 结果已保存到 @misc 中，可用于缓存加载后重新绘图")

  p_pk <- ggplot(bcmvn, aes(x = as.numeric(as.character(pK)), y = BCmetric)) +
    geom_line() + geom_point() +
    geom_vline(xintercept = pKValue, color = "red", linetype = "dashed") +
    labs(title = "Step 1: pK Selection", x = "pK", y = "BCmetric") +
    theme_minimal()

  # 同型校正
  if (is.null(cluster_col)) {
    possible_cols <- grep("clusters|res|seurat_clusters", colnames(object@meta.data), value = TRUE)
    if (length(possible_cols) > 0) cluster_col <- possible_cols[1]
  }

  homotypic.prop <- 0
  if (!is.null(cluster_col) && cluster_col %in% colnames(object@meta.data)) {
    cluster_vec <- as.character(object@meta.data[[cluster_col]])
    if (exists("modelHomotypic", envir = df_ns)) {
      model_homo_fun <- get("modelHomotypic", envir = df_ns)
      homotypic.prop <- model_homo_fun(cluster_vec)
      message(">>> 检测到 Cluster 列: ", cluster_col, ", 同型比例: ", round(homotypic.prop, 4))
    }
  }

  # 核心计算
  nCells <- ncol(object)
  nExp_poi <- round(c(minPCT/100, (minPCT/100)*(1-homotypic.prop),
                      maxPCT/100, (maxPCT/100)*(1-homotypic.prop)) * nCells)
  names(nExp_poi) <- c("min", "min_adj", "max", "max_adj")

  # 第一轮：生成 pANN
  # 关键修复：使用 reuse.pANN = NULL（而不是 FALSE）解决 "cannot xtfrm data frames" 错误
  object <- df_main_fun(object, pN = pN, pK = pKValue, nExp = nExp_poi["min"],
                        reuse.pANN = NULL, PCs = PCs, sct = is_sct)

  all_pann_cols <- grep("^pANN_", colnames(object@meta.data), value = TRUE)
  pann_col <- all_pann_cols[length(all_pann_cols)]

  # 后续轮
  unique_nExps <- unique(nExp_poi[-1])
  for (n in unique_nExps) {
    object <- df_main_fun(object, pN = pN, pK = pKValue, nExp = n,
                          reuse.pANN = pann_col, PCs = PCs, sct = is_sct)
  }

  # 标签清洗
  safe_get_df_col <- function(n) {
    col <- grep(paste0("DF.classifications_.*_", n, "$"), colnames(object@meta.data), value = TRUE)
    if (length(col) == 0) return(NA)
    return(col[length(col)])
  }

  object$DFPrediction <- "Singlet"

  col_max_adj <- safe_get_df_col(nExp_poi["max_adj"])
  col_max     <- safe_get_df_col(nExp_poi["max"])
  col_min_adj <- safe_get_df_col(nExp_poi["min_adj"])
  col_min     <- safe_get_df_col(nExp_poi["min"])

  if (!is.na(col_max)) object$DFPrediction[object@meta.data[[col_max]] == "Doublet"] <- "Low Confidence"
  if (!is.na(col_max_adj)) object$DFPrediction[object@meta.data[[col_max_adj]] == "Doublet"] <- "Low Confidence adjusted"
  if (!is.na(col_min)) object$DFPrediction[object@meta.data[[col_min]] == "Doublet"] <- "High Confidence"
  if (!is.na(col_min_adj)) object$DFPrediction[object@meta.data[[col_min_adj]] == "Doublet"] <- "High Confidence adjusted"

  # 可视化
  doublet_cols <- c(
    "High Confidence"          = "#E41A1C",
    "High Confidence adjusted" = "#FB9A99",
    "Low Confidence"           = "#33A02C",
    "Low Confidence adjusted"  = "#B2DF8A",
    "Singlet"                  = "#E6E6E6"
  )

  p_umap <- DimPlot(object, group.by = "DFPrediction") +
    scale_color_manual(values = doublet_cols, na.value = "grey90") +
    labs(title = "Step 2: Doublet Spatial Distribution")

  p_qc <- FeatureScatter(object, feature1 = "nCount_RNA", feature2 = "percent.mito", group.by = "DFPrediction") +
    scale_color_manual(values = doublet_cols, na.value = "grey90") +
    NoLegend() +
    labs(title = "Step 3: QC Verification")

  # 组合三步可视化
  p_three_steps <- p_pk + p_umap + p_qc + plot_layout(ncol = 2)

  # 显示并保存
  print(p_three_steps)

  # 尝试保存图片（如果 plot_and_save 函数存在）
  if (exists("plot_and_save")) {
    plot_and_save(p_three_steps, "doubletfinder_three_steps_overview",
                  subfolder = "DoubletFinder", width = 12, height = 8)
  }

  # 恢复之前移除的复杂类型列（如果有的话）
  if (length(problematic_cols) > 0) {
    message(">>> 恢复之前移除的 metadata 列")
    # 获取 DoubletFinder 添加的新列
    new_cols <- setdiff(colnames(object@meta.data), colnames(original_meta))
    # 合并：原始列 + DoubletFinder 新增列
    for (col in problematic_cols) {
      if (col %in% colnames(original_meta)) {
        object@meta.data[[col]] <- original_meta[[col]]
      }
    }
  }

  return(object)
}
```

## 6.2 执行双细胞检测

```{r doublet_detection, fig.width=14, fig.height=10}
################################################################################
##################### 执行 DoubletFinder 双细胞检测 ##############################
################################################################################

doublet_result <- load_or_compute(
  cache_file = paste0(config$cache_prefix, "_05_doublet_detected.rds"),
  description = paste0("DoubletFinder - ", config$cache_prefix),
  compute_fn = function() {
    cat("  这是耗时步骤,可能需要 10-20 分钟...\n")
    seurat_obj_with_doublets <- runDoubletFinder(seurat_obj, PCs = 1:config$dims_rna,
                                                  minPCT = 1, maxPCT = 15, pN = 0.25)
    return(seurat_obj_with_doublets)
  }
)

seurat_obj <- doublet_result

# ==================== 重新生成三步概览图 ====================
# 说明：如果从缓存读取，runDoubletFinder 不会执行，图片不会显示
# 因此这里重新生成并保存概览图
# 如果 @misc 中保存了 pK sweep 结果，则生成完整的三步图

cat("\n正在生成 DoubletFinder 概览图...\n")

# 定义 doublet 颜色方案
doublet_cols <- c(
  "High Confidence"          = "#E41A1C",
  "High Confidence adjusted" = "#FB9A99",
  "Low Confidence"           = "#33A02C",
  "Low Confidence adjusted"  = "#B2DF8A",
  "Singlet"                  = "#E6E6E6"
)

# 检查是否有保存的 pK sweep 结果
has_pk_data <- !is.null(seurat_obj@misc$doubletfinder_pk_sweep)

if (has_pk_data) {
  cat("  ✓ 检测到保存的 pK sweep 结果，生成完整三步图\n")

  # Step 1: pK Selection
  bcmvn <- seurat_obj@misc$doubletfinder_pk_sweep
  pKValue <- seurat_obj@misc$doubletfinder_pk_value

  p_pk <- ggplot(bcmvn, aes(x = as.numeric(as.character(pK)), y = BCmetric)) +
    geom_line() + geom_point() +
    geom_vline(xintercept = pKValue, color = "red", linetype = "dashed") +
    labs(title = "Step 1: pK Selection",
         subtitle = paste0("Optimal pK: ", round(pKValue, 3)),
         x = "pK", y = "BCmetric") +
    theme_minimal()

  # Step 2: Doublet 空间分布
  p_umap <- DimPlot(seurat_obj, group.by = "DFPrediction", pt.size = 0.5) +
    scale_color_manual(values = doublet_cols, na.value = "grey90") +
    labs(title = "Step 2: Doublet Spatial Distribution") +
    theme(legend.position = "right")

  # Step 3: QC 验证
  p_qc <- FeatureScatter(seurat_obj,
                         feature1 = "nCount_RNA",
                         feature2 = "percent.mito",
                         group.by = "DFPrediction",
                         pt.size = 0.8) +
    scale_color_manual(values = doublet_cols, na.value = "grey90") +
    labs(title = "Step 3: QC Verification") +
    theme(legend.position = "right")

  # 组合三步图（2x2 布局）
  p_overview <- (p_pk | p_umap) / (plot_spacer() | p_qc) +
    plot_layout(heights = c(1, 1)) +
    plot_annotation(
      title = "DoubletFinder Three-Step Overview",
      subtitle = paste0("Total cells: ", ncol(seurat_obj)),
      theme = theme(plot.title = element_text(size = 16, face = "bold"))
    )

  print(p_overview)

  # 保存完整三步图
  plot_and_save(p_overview, "doubletfinder_three_steps_overview",
                subfolder = "DoubletFinder", width = 14, height = 10)

} else {
  cat("  ⚠️  未找到 pK sweep 结果（旧版本缓存），生成简化版两步图\n")

  # Step 2: Doublet 空间分布
  p_umap_overview <- DimPlot(seurat_obj, group.by = "DFPrediction", pt.size = 0.5) +
    scale_color_manual(values = doublet_cols, na.value = "grey90") +
    labs(title = "Step 2: Doublet Spatial Distribution",
         subtitle = paste0("Total cells: ", ncol(seurat_obj))) +
    theme(legend.position = "right")

  # Step 3: QC 验证
  p_qc_overview <- FeatureScatter(seurat_obj,
                                  feature1 = "nCount_RNA",
                                  feature2 = "percent.mito",
                                  group.by = "DFPrediction",
                                  pt.size = 0.8) +
    scale_color_manual(values = doublet_cols, na.value = "grey90") +
    labs(title = "Step 3: QC Verification",
         subtitle = "Doublets vs QC metrics") +
    theme(legend.position = "right")

  # 组合两步图
  p_overview <- p_umap_overview + p_qc_overview + plot_layout(ncol = 2)
  print(p_overview)

  # 保存两步图
  plot_and_save(p_overview, "doubletfinder_overview_step2_step3",
                subfolder = "DoubletFinder", width = 14, height = 6)
}

# ✅ 保留所有 DoubletFinder 结果列（pANN, DF.classifications, DFPrediction）
# 这些列在后续分析中很有用：
# - 可以在聚类后检查某个 cluster 是否富集 doublets
# - 可以调整阈值重新分类，而不需要重新运行 DoubletFinder
# - 可以用不同的过滤策略（如只删除 High Confidence doublets）

cat("\n", strrep("=", 80), "\n")
cat("✓ DoubletFinder 检测完成\n")
cat(strrep("=", 80), "\n\n")

cat("双细胞检测结果:\n")
print(table(seurat_obj$DFPrediction))

# 详细统计信息
n_total <- ncol(seurat_obj)
n_high_conf <- sum(seurat_obj$DFPrediction %in% c("High Confidence", "High Confidence adjusted"), na.rm = TRUE)
n_low_conf <- sum(seurat_obj$DFPrediction %in% c("Low Confidence", "Low Confidence adjusted"), na.rm = TRUE)
n_singlet <- sum(seurat_obj$DFPrediction == "Singlet", na.rm = TRUE)

cat("\n细胞分类统计:\n")
cat("  总细胞数:", n_total, "\n")
cat("  Singlet:", n_singlet, sprintf("(%.1f%%)", 100*n_singlet/n_total), "\n")
cat("  Low Confidence Doublet:", n_low_conf, sprintf("(%.1f%%)", 100*n_low_conf/n_total), "\n")
cat("  High Confidence Doublet:", n_high_conf, sprintf("(%.1f%%)", 100*n_high_conf/n_total), "\n\n")

cat("⚠️  重要说明:\n")
cat("   - Doublet 标签已保存在 seurat_obj$DFPrediction 列\n")
cat("   - 所有细胞（包括 doublets）均保留在对象中\n")
cat("   - 请查看下方【6.3 DoubletFinder 质控验证】了解详细验证结果\n")
cat("   - 如需过滤 doublets，请在验证后运行【6.4 可选代码块】\n\n")

# 继续使用包含所有细胞的对象（不创建 seurat_obj_clean）
rm(doublet_result)
gc()
```

## 6.3 DoubletFinder 质控验证 (QC Validation)

> **为什么需要质控验证？**
>
> DoubletFinder 是一个基于统计模型的预测工具，而非100%准确的"真相"。某些生物学状态的细胞（如增殖细胞、高转录活性细胞）可能被误判为doublets。因此，在物理删除这些细胞前，必须通过三个独立验证来确认预测的合理性：
>
> 1. **数量合理性**：预测的doublet比例是否符合预期（通常1-10%）？
> 2. **空间分布合理性**：doublets是否位于细胞类型交界处（合理），还是形成独立的cluster（可疑）？
> 3. **分子特征合理性**：doublets是否富集线粒体/核糖体基因或同时表达互斥markers（支持doublet假说）？
>
> **如何解读结果？**
>
> 本节将生成一份综合质控报告，包含柱状图、UMAP分布图和特征基因热图。您应该综合判断这些证据，而不是仅依赖某一个指标。

```{r doublet_qc_validation, fig.width=16, fig.height=12}
################################################################################
##################### DoubletFinder 质控验证 #####################################
################################################################################
#
# 【功能说明】
# 本代码块执行三个独立的质控验证，确保 DoubletFinder 的预测是可靠的
#
# 【代码结构】
# 验证1：细胞数量统计（检查 doublet 比例是否合理）
# 验证2：UMAP 空间分布（检查 doublets 是否位于细胞类型交界处）
# 验证3：特征基因分析（检查 doublets 是否富集 MT/Ribo 基因或互斥 markers）
#
# 【输出文件】
# - doublet_validation_summary.png/pdf（综合质控报告，6个子图）
# - doublet_barplot.png/pdf（细胞分类柱状图）
# - doublet_umap_distribution.png/pdf（UMAP 分布图）
# - doublet_markers_heatmap.png/pdf（特征基因热图）
#
################################################################################

cat("\n", strrep("=", 80), "\n")
cat("📊 DoubletFinder 质控验证\n")
cat(strrep("=", 80), "\n\n")

# ==================== 验证1：细胞数量变化 ====================
# 目的：检查预测的 doublet 比例是否合理
# 正常范围：1-10%（取决于上样浓度和实验平台）
# 如果比例异常高（>15%），可能需要调整 DoubletFinder 参数

cat("【验证 1】细胞分类统计\n")
cat("   目的：检查 doublet 比例是否在合理范围（通常 1-10%）\n\n")

# 统计各类细胞数量
# 注意：我们将 Low/High Confidence 和 adjusted 版本合并统计
doublet_stats <- data.frame(
  Category = c("Singlet", "Low Confidence", "High Confidence"),
  Count = c(
    sum(seurat_obj$DFPrediction == "Singlet", na.rm = TRUE),
    sum(seurat_obj$DFPrediction %in% c("Low Confidence", "Low Confidence adjusted"), na.rm = TRUE),
    sum(seurat_obj$DFPrediction %in% c("High Confidence", "High Confidence adjusted"), na.rm = TRUE)
  )
)

# 计算百分比
doublet_stats$Percentage <- round(100 * doublet_stats$Count / sum(doublet_stats$Count), 2)

# 打印统计表
print(doublet_stats)

# 绘制柱状图
# 颜色方案：灰色（Singlet）、橙色（Low Confidence）、红色（High Confidence）
p_barplot <- ggplot(doublet_stats, aes(x = Category, y = Count, fill = Category)) +
  geom_bar(stat = "identity") +
  # 在柱子上方显示数量和百分比
  geom_text(aes(label = paste0(Count, " (", Percentage, "%)")), vjust = -0.5, size = 4) +
  scale_fill_manual(values = c("Singlet" = "#E6E6E6",
                                "Low Confidence" = "#FFA500",
                                "High Confidence" = "#E41A1C")) +
  labs(title = "Validation 1: Cell Classification Summary",
       subtitle = "Check if doublet rate is within expected range (1-10%)",
       x = "Category", y = "Cell Count") +
  theme_minimal(base_size = 14) +
  theme(legend.position = "none")

# 保存柱状图
plot_and_save(p_barplot, "doublet_barplot", subfolder = "DoubletFinder",
              width = 8, height = 6)

# ==================== 验证2：UMAP 空间分布 ====================
# 目的：检查 doublets 在转录组空间中的分布模式
#
# 【合理分布】doublets 主要位于细胞类型交界处（边界区域）
#   - 原因：两个不同类型的细胞融合，转录组介于两者之间
#   - 在 UMAP 上表现为分散在不同 cluster 的交界处
#
# 【可疑分布】整簇独立的细胞被标记为 doublet
#   - 可能是增殖细胞（高 RNA 含量，表达 Mki67, Top2a）
#   - 可能是某些特殊细胞类型（如浆细胞，天然高 RNA 含量）
#   - 建议保留这些细胞，进一步分析后再决定

cat("\n【验证 2】Doublets 在 UMAP 上的空间分布\n")
cat("   检查要点：\n")
cat("   ✅ 好的迹象：Doublets 分散在细胞类型交界处\n")
cat("   ⚠️  需警惕：整簇独立的细胞被标记（可能是增殖细胞或高 RNA 含量细胞）\n\n")

# 图A：显示所有细胞，按 DFPrediction 分类着色
# 用途：整体观察 doublet 的空间分布模式
p_umap_doublet <- DimPlot(seurat_obj, group.by = "DFPrediction", pt.size = 0.5) +
  scale_color_manual(values = c(
    "Singlet" = "lightgrey",
    "Low Confidence" = "#FFA500",
    "Low Confidence adjusted" = "#FFD700",
    "High Confidence" = "#E41A1C",
    "High Confidence adjusted" = "#FF6347"
  )) +
  labs(title = "Validation 2a: Doublet Distribution on UMAP",
       subtitle = "All cells colored by DoubletFinder prediction") +
  theme(legend.position = "right")

# 保存 UMAP 分布图（全部细胞）
plot_and_save(p_umap_doublet, "doublet_umap_all_cells", subfolder = "DoubletFinder",
              width = 10, height = 8)

# 图B：仅高亮显示 Doublets
# 用途：更清晰地看到 doublet 的位置
doublet_cells <- colnames(seurat_obj)[seurat_obj$DFPrediction != "Singlet"]
p_umap_doublet_only <- DimPlot(seurat_obj, cells.highlight = doublet_cells,
                               cols.highlight = "red", cols = "lightgrey",
                               pt.size = 0.5) +
  labs(title = "Validation 2b: Doublets Only (Highlighted)",
       subtitle = paste0("Highlighted: ", length(doublet_cells), " doublets")) +
  NoLegend()

# 保存 UMAP 分布图（仅 doublets）
plot_and_save(p_umap_doublet_only, "doublet_umap_highlighted", subfolder = "DoubletFinder",
              width = 10, height = 8)

# ==================== 验证3：特征基因分析 ====================
# 目的：从分子层面验证 doublet 预测的合理性
#
# 【预期特征】
# 1. 富集线粒体基因（MT-CO1, MT-ND1 等）
#    - 原因：两个细胞融合导致细胞质体积增大，线粒体相对富集
# 2. 富集核糖体基因（RPL3, RPS4 等）
#    - 原因：高转录活性，或者两个细胞的核糖体累加
# 3. 同时高表达互斥的 lineage markers
#    - 例如：Cd3e（T细胞）+ Cd19（B细胞）
#    - 例如：Xcr1（cDC1）+ Sirpa（cDC2）
#
# 【参数说明】
# - logfc.threshold = 0.25：只保留 log2FC > 0.25 的基因（约1.2倍差异）
#   选择理由：doublet 的分子特征通常不会有极高的倍数变化
# - min.pct = 0.1：基因至少在 10% 的细胞中表达
#   选择理由：排除极低表达的噪音基因

cat("【验证 3】Doublets 的特征基因分析\n")
cat("   检查要点：\n")
cat("   ✅ 支持 doublet 假说：\n")
cat("      - 富集线粒体基因（MT-CO1, MT-ND1 等）\n")
cat("      - 富集核糖体基因（RPL3, RPS4 等）\n")
cat("      - 同时高表达互斥 markers（如 Cd3e + Cd19）\n")
cat("   ⚠️  需要重新评估：\n")
cat("      - 主要富集增殖基因（Mki67, Top2a）\n")
cat("      - 富集某种细胞类型的 specific markers\n\n")

# 为 FindMarkers 准备分组
# 将 High Confidence doublets 作为实验组，Singlets 作为对照组
seurat_obj$is_doublet <- ifelse(
  seurat_obj$DFPrediction %in% c("High Confidence", "High Confidence adjusted"),
  "Doublet", "Singlet"
)

# 运行差异表达分析
# 使用 SCT assay（已归一化和方差稳定化处理）
doublet_markers <- FindMarkers(
  seurat_obj,
  ident.1 = "Doublet",           # 实验组：High Confidence doublets
  ident.2 = "Singlet",            # 对照组：Singlets
  group.by = "is_doublet",
  assay = "SCT",
  logfc.threshold = 0.25,         # 阈值选择：1.2倍差异（2^0.25 ≈ 1.19）
  min.pct = 0.1,                  # 至少在 10% 细胞中表达
  verbose = FALSE
)

# 提取 Top 20 上调基因
top_doublet_genes <- doublet_markers %>%
  filter(avg_log2FC > 0) %>%       # 只看上调基因
  arrange(desc(avg_log2FC)) %>%     # 按 log2FC 降序排列
  head(20)

cat("Top 20 上调基因（Doublet vs Singlet）:\n")
print(top_doublet_genes[, c("avg_log2FC", "pct.1", "pct.2", "p_val_adj")])

# 检查是否富集线粒体/核糖体基因
mt_genes <- grep("^MT-|^mt-", rownames(top_doublet_genes), value = TRUE, ignore.case = TRUE)
ribo_genes <- grep("^RPL|^RPS|^Rpl|^Rps", rownames(top_doublet_genes), value = TRUE)

cat("\n【分子特征总结】\n")
cat("  线粒体基因:", length(mt_genes), "个")
if (length(mt_genes) > 0) cat(" -", paste(mt_genes, collapse = ", "))
cat("\n  核糖体基因:", length(ribo_genes), "个")
if (length(ribo_genes) > 0) cat(" -", paste(ribo_genes, collapse = ", "))
cat("\n")

# 可视化：Doublet 特征基因的表达
# 优先展示 QC 基因（线粒体、核糖体）和 lineage markers
genes_to_plot <- c()

# 策略1：优先使用 QC 基因（如果 top genes 中包含）
if (length(mt_genes) > 0 | length(ribo_genes) > 0) {
  qc_genes_in_top <- c(mt_genes, ribo_genes)
  genes_to_plot <- c(genes_to_plot, qc_genes_in_top)
}

# 策略2：补充 lineage markers（检查互斥表达）
# 使用 Section 1.4 中定义的 genes_lineage
lineage_in_top <- intersect(genes_lineage, rownames(top_doublet_genes))
if (length(lineage_in_top) > 0) {
  genes_to_plot <- c(genes_to_plot, lineage_in_top)
}

# 策略3：如果还不够 9 个基因，用 top genes 补充
if (length(genes_to_plot) < 9) {
  remaining_genes <- setdiff(rownames(top_doublet_genes), genes_to_plot)
  genes_to_plot <- c(genes_to_plot, remaining_genes[1:(9 - length(genes_to_plot))])
}

# 最终只取前 9 个（3x3 布局）
genes_to_plot <- genes_to_plot[1:min(9, length(genes_to_plot))]

cat("\n【可视化基因】（从 Section 1.4 基因库和 top markers 中自动选择）:\n")
cat(" ", paste(genes_to_plot, collapse = ", "), "\n")

# 生成 FeaturePlot
p_feature <- FeaturePlot(seurat_obj, features = genes_to_plot,
                        ncol = 3, pt.size = 0.3) +
  plot_annotation(title = "Validation 3: Doublet Feature Genes",
                  subtitle = "Expression of marker genes enriched in doublets")

# 保存特征基因图
plot_and_save(p_feature, "doublet_feature_genes", subfolder = "DoubletFinder",
              width = 12, height = 12)

# ==================== 组合图表 ====================
# 将三个验证的关键图表组合成一份综合报告
# 布局：上半部分（数量统计 + UMAP 分布 x2），下半部分（特征基因 3x3）

cat("\n正在生成综合质控报告...\n")

# 上半部分：数量统计 + UMAP 分布
p_top <- p_barplot | p_umap_doublet | p_umap_doublet_only

# 下半部分：特征基因表达
p_bottom <- p_feature

# 组合（上下比例 1:1.5，因为下半部分是 3x3 的子图）
p_validation <- p_top / p_bottom +
  plot_layout(heights = c(1, 1.5)) +
  plot_annotation(
    title = "DoubletFinder Quality Control Validation Report",
    subtitle = paste0("Dataset: ", config$project_name, " | Total cells: ", ncol(seurat_obj)),
    theme = theme(plot.title = element_text(size = 18, face = "bold"))
  )

# 保存综合报告
plot_and_save(p_validation, "doublet_validation_summary", subfolder = "DoubletFinder",
              width = 18, height = 14)

cat("\n", strrep("=", 80), "\n")
cat("✓ DoubletFinder 质控验证完成\n")
cat(strrep("=", 80), "\n\n")

# ==================== 验证结论指南 ====================
cat("📋 如何解读验证结果？\n\n")

cat("【验证1：数量合理性】\n")
cat("  ✅ 正常：Doublet 比例在 1-10% 之间\n")
cat("     - 这是基于上样浓度和实验平台的合理范围\n")
cat("  ⚠️  异常：Doublet 比例 > 15%\n")
cat("     - 可能原因：上样浓度过高、实验操作问题\n")
cat("     - 建议：检查实验记录，或调整 DoubletFinder 参数\n\n")

cat("【验证2：空间分布合理性】（最关键的验证！）\n")
cat("  ✅ 支持 doublet 假说：\n")
cat("     - Doublets 主要分散在细胞类型交界处\n")
cat("     - 没有形成独立的 cluster\n")
cat("  ⚠️  需要重新评估：\n")
cat("     - 整簇独立的细胞被标记为 doublet\n")
cat("     - 可能是增殖细胞（检查 Mki67, Top2a 表达）\n")
cat("     - 可能是高 RNA 含量细胞（如浆细胞）\n")
cat("     - 建议：保留这些细胞，完成聚类后再分析\n\n")

cat("【验证3：分子特征合理性】\n")
cat("  ✅ 支持 doublet 假说：\n")
cat("     - 富集线粒体基因（MT-CO1, MT-ND1 等）\n")
cat("     - 富集核糖体基因（RPL3, RPS4 等）\n")
cat("     - 同时表达互斥 markers（如 Cd3e + Cd19, Xcr1 + Sirpa）\n")
cat("  ⚠️  需要重新评估：\n")
cat("     - 主要富集增殖基因 → 可能是增殖细胞\n")
cat("     - 富集某种细胞类型的 specific markers → 可能是真实细胞类型\n\n")

cat("【综合判断建议】\n")
cat("  1. 如果三个验证都支持 doublet 假说 → 可以安全删除\n")
cat("  2. 如果任何一个验证出现可疑信号 → 建议保留，进一步分析\n")
cat("  3. 记住：删除细胞是不可逆的，保守策略更安全\n\n")
```

> **🔍 结果解读示例**
>
> **情况1：理想的 doublet 检测结果**
> - 验证1：Doublet 比例 5.2%（正常）
> - 验证2：Doublets 分散在 T-B 交界、DC-Macrophage 交界处
> - 验证3：富集 MT-CO1, MT-ND2, RPL3, RPS4，同时表达 Cd3e + Cd19
> - **结论**：可以安全删除这些 doublets
>
> **情况2：可疑的 doublet 检测结果**
> - 验证1：Doublet 比例 8.3%（正常）
> - 验证2：在 UMAP 右上角形成一个独立的 cluster（⚠️）
> - 验证3：主要富集 Mki67, Top2a, Pcna（增殖基因，而非 MT/Ribo 基因）
> - **结论**：这可能是增殖细胞，建议保留，完成聚类后再决定

## 6.4 可选：过滤双细胞 (Optional - eval=FALSE)

```{r filter_doublets, eval=FALSE}
################################################################################
##################### 可选步骤：物理删除 Doublets ################################
################################################################################
#
# ⚠️ 默认不运行此代码块 (eval=FALSE)
#
# 运行前请考虑：
#
# 【保留 doublets 的优势】
# 1. 可以在聚类后检查某个 cluster 是否富集 doublets
# 2. 可以调整阈值重新分类，无需重新运行 DoubletFinder
# 3. 可以尝试不同的过滤策略
#
# 【删除 doublets 的时机】
# - 建议在完成初步聚类和质控可视化后再删除
# - 或者在单独的下游分析脚本中删除
#
# 【如何运行此代码块】
# 方法1: 将 eval=FALSE 改为 eval=TRUE
# 方法2: 在 RStudio 中手动运行此代码块
#
################################################################################

# ==================== 选择过滤策略 ====================

# 策略1：只删除 High Confidence doublets（推荐，保守）
seurat_obj_clean <- subset(seurat_obj,
                           subset = DFPrediction %in% c("Singlet",
                                                         "Low Confidence",
                                                         "Low Confidence adjusted"))

# 策略2：删除所有 doublets（严格）
# seurat_obj_clean <- subset(seurat_obj, subset = DFPrediction == "Singlet")

# 策略3：不删除任何细胞（最灵活，用于探索性分析）
# seurat_obj_clean <- seurat_obj

cat("\n✓ 双细胞过滤完成\n")
cat("  过滤前:", ncol(seurat_obj), "个细胞\n")
cat("  过滤后:", ncol(seurat_obj_clean), "个细胞\n")
cat("  移除:", ncol(seurat_obj) - ncol(seurat_obj_clean), "个细胞\n\n")
```

## 6.5 确定后续分析使用的对象

```{r set_analysis_object}
################################################################################
##################### 选择分析对象 ##############################################
################################################################################
#
# 根据是否运行了过滤代码块（6.4），自动选择使用的对象：
# - 如果未过滤：使用 seurat_obj（包含所有细胞）
# - 如果已过滤：使用 seurat_obj_clean（仅包含 Singlets）
#
################################################################################

# 检查是否创建了过滤后的对象
if (exists("seurat_obj_clean")) {
  # 用户运行了过滤代码块
  seurat_final <- seurat_obj_clean
  cat("✓ 检测到过滤后的对象 seurat_obj_clean\n")
  cat("  使用", ncol(seurat_final), "个细胞继续分析（已过滤 doublets）\n\n")
} else {
  # 用户未过滤，使用全部细胞
  seurat_final <- seurat_obj
  cat("✓ 使用完整对象 seurat_obj\n")
  cat("  使用", ncol(seurat_final), "个细胞继续分析（包含所有细胞）\n")

  # 显示 doublet 统计
  if ("DFPrediction" %in% colnames(seurat_final@meta.data)) {
    doublet_count <- sum(seurat_final$DFPrediction %in%
                        c("High Confidence", "High Confidence adjusted",
                          "Low Confidence", "Low Confidence adjusted"), na.rm = TRUE)
    cat("  其中包含", doublet_count, "个 doublets\n")
  }
  cat("\n")
}

# 为了向后兼容，将 seurat_final 赋值给 seurat_obj_clean
# 这样后续代码无需修改
seurat_obj_clean <- seurat_final
```

---

# 7. 差异基因分析

```{r deg_analysis}
################################################################################
##################### 差异基因分析 ##############################################
################################################################################

Idents(seurat_obj_clean) <- seurat_obj_clean$seurat_clusters

exp.markers <- load_or_compute(
  cache_file = paste0(config$cache_prefix, "_06_deg_markers.rds"),
  description = paste0("差异基因分析 - ", config$cache_prefix),
  compute_fn = function() {
    cat("  这可能需要 5-15 分钟...\n")
    markers <- FindAllMarkers(seurat_obj_clean, assay = "SCT", slot = "data", verbose = FALSE)
    cat("\n  找到", nrow(markers), "个差异表达基因\n")
    return(markers)
  }
)

# Top 5 markers
top5_markers <- exp.markers %>%
  group_by(cluster) %>%
  slice_max(n = 5, order_by = avg_log2FC)

cat("\nTop 5 Marker Genes per Cluster:\n")
print(top5_markers)

# 保存
deg_csv <- paste0(output.dir, "Files/", config$project_name, "_DEG.csv")
write.csv(exp.markers, deg_csv, row.names = FALSE)
cat("\n✓ 差异基因列表已保存:", deg_csv, "\n\n")
```

## 7.1 差异基因可视化

> **如何解读差异基因分析结果？**
>
> 差异基因分析（DEG）通过比较不同cluster之间的基因表达，帮助我们理解：
> 1. **细胞类型鉴定**：每个cluster的marker基因是什么？
> 2. **功能状态**：cluster之间的功能差异（如增殖、活化、成熟）
> 3. **质量控制**：是否存在技术性cluster（如高线粒体cluster）
>
> **三种互补的可视化方法**：
> - **DotPlot（气泡图）**：同时展示表达比例和表达强度，适合比较多个marker
> - **Heatmap（热图）**：展示基因表达的整体模式，识别共表达基因群
> - **FeaturePlot（特征图）**：展示marker在UMAP上的空间分布，验证cluster特异性

```{r deg_visualization, fig.width=14, fig.height=10}
################################################################################
##################### 差异基因可视化 ##############################################
################################################################################
#
# 【功能说明】
# 使用三种互补的方法可视化差异表达基因，帮助鉴定细胞类型和功能状态
#
# 【可视化策略】
# 策略1：优先使用 Section 1.4 中定义的 marker 基因（如果在 top markers 中）
# 策略2：补充 top DEG markers（数据驱动的发现）
# 策略3：根据 analysis_mode 自动选择相关基因
#
################################################################################

cat("\n", strrep("=", 80), "\n")
cat("📊 差异基因可视化\n")
cat(strrep("=", 80), "\n\n")

# ==================== 智能基因选择 ====================
# 策略：优先使用预定义的 marker 基因（Section 1.4），补充 top DEG

# 提取 top DEG markers
top_deg_genes <- unique(top5_markers$gene)

# 根据 analysis_mode 选择基因
genes_for_dotplot <- c()
genes_for_heatmap <- c()
genes_for_featureplot <- c()

if (analysis_mode == "cDC1") {
  # cDC1 模式：重点展示 cDC1 markers 和成熟相关基因
  genes_for_dotplot <- unique(c(genes_cDC1, genes_maturation,
                                intersect(top_deg_genes, genes_cDC2)))  # 也展示部分 cDC2 对比
  genes_for_featureplot <- genes_cDC1[1:min(6, length(genes_cDC1))]
} else if (analysis_mode == "cDC2") {
  # cDC2 模式：重点展示 cDC2 markers 和成熟相关基因
  genes_for_dotplot <- unique(c(genes_cDC2, genes_maturation,
                                intersect(top_deg_genes, genes_cDC1)))
  genes_for_featureplot <- genes_cDC2[1:min(6, length(genes_cDC2))]
} else if (analysis_mode == "integrated") {
  # 整合模式：展示 cDC1, cDC2, 成熟基因
  genes_for_dotplot <- unique(c(genes_cDC1, genes_cDC2, genes_maturation))
  genes_for_featureplot <- unique(c(genes_cDC1[1:3], genes_cDC2[1:3]))
} else if (analysis_mode == "full") {
  # 全量模式：展示 lineage markers 和功能基因
  genes_for_dotplot <- unique(c(genes_lineage, genes_functional))
  genes_for_featureplot <- genes_lineage[1:min(6, length(genes_lineage))]
}

# 补充 top DEG（如果预定义基因不够）
if (length(genes_for_dotplot) < 15) {
  additional_genes <- setdiff(top_deg_genes, genes_for_dotplot)
  genes_for_dotplot <- c(genes_for_dotplot, additional_genes[1:min(15 - length(genes_for_dotplot), length(additional_genes))])
}

# 热图使用前 30 个 top DEG（展示整体模式）
genes_for_heatmap <- top_deg_genes[1:min(30, length(top_deg_genes))]

# 只保留实际存在的基因
genes_for_dotplot <- intersect(genes_for_dotplot, rownames(seurat_obj_clean))
genes_for_heatmap <- intersect(genes_for_heatmap, rownames(seurat_obj_clean))
genes_for_featureplot <- intersect(genes_for_featureplot, rownames(seurat_obj_clean))

cat("【可视化基因选择】\n")
cat("  DotPlot:", length(genes_for_dotplot), "个基因 -", paste(head(genes_for_dotplot, 10), collapse = ", "), "...\n")
cat("  Heatmap:", length(genes_for_heatmap), "个基因\n")
cat("  FeaturePlot:", length(genes_for_featureplot), "个基因 -", paste(genes_for_featureplot, collapse = ", "), "\n\n")

# ==================== 可视化1：DotPlot（气泡图）====================
# 用途：同时展示基因的表达比例（点大小）和表达强度（颜色）
# 解读：
#   - 大而红的点：该基因在该 cluster 中高表达且表达细胞比例高（理想 marker）
#   - 大而蓝的点：虽然很多细胞表达，但表达强度低
#   - 小点：表达细胞比例低

cat("【可视化 1】DotPlot - 同时展示表达比例和强度\n")

p_dot <- DotPlot(seurat_obj_clean, features = genes_for_dotplot, assay = "SCT") +
  RotatedAxis() +
  scale_colour_gradientn(colours = rev(brewer.pal(n = 11, name = "RdYlBu"))) +
  labs(title = "Marker Expression across Clusters",
       subtitle = paste0("Dot size = % expressing | Color = expression level"),
       x = "Marker Genes", y = "Clusters") +
  theme(axis.text.x = element_text(size = 10, angle = 45, hjust = 1))

# 保存 DotPlot
plot_and_save(p_dot, "deg_dotplot_markers", subfolder = "DEG",
              width = max(12, length(genes_for_dotplot) * 0.4), height = 8)

# ==================== 可视化2：Heatmap（热图）====================
# 用途：展示基因表达的整体模式，识别共表达基因群
# 解读：
#   - 垂直条纹：某个 cluster 特异性高表达一组基因
#   - 水平条纹：某些基因在多个 cluster 中共表达
#   - 梯度变化：基因表达的连续变化（如成熟过程）

cat("\n【可视化 2】Heatmap - 展示基因表达的整体模式\n")

# 需要先对基因进行 ScaleData（Z-score 标准化）
seurat_obj_clean <- ScaleData(seurat_obj_clean, features = genes_for_heatmap, verbose = FALSE)

p_heat <- DoHeatmap(seurat_obj_clean, features = genes_for_heatmap, assay = "SCT") +
  scale_fill_gradientn(colors = c("blue", "white", "red")) +
  labs(title = "Top Marker Expression Heatmap",
       subtitle = paste0("Top ", length(genes_for_heatmap), " DEG genes across clusters")) +
  theme(axis.text.y = element_text(size = 8))

# 保存 Heatmap
plot_and_save(p_heat, "deg_heatmap_top_markers", subfolder = "DEG",
              width = 12, height = max(10, length(genes_for_heatmap) * 0.25))

# ==================== 可视化3：FeaturePlot（特征图）====================
# 用途：展示 marker 在 UMAP 上的空间分布
# 解读：
#   - 如果 marker 仅在某个 cluster 高表达 → cluster 特异性好
#   - 如果 marker 在多个 cluster 高表达 → 共享的功能状态
#   - 梯度分布：成熟或功能状态的连续变化

cat("\n【可视化 3】FeaturePlot - 展示 markers 的空间分布\n")

if (length(genes_for_featureplot) > 0) {
  p_feat <- FeaturePlot(seurat_obj_clean,
                       features = genes_for_featureplot,
                       reduction = "umap",
                       ncol = 3,
                       pt.size = 0.5) +
    plot_annotation(title = paste0("Key Marker Expression on UMAP - ", config$project_name),
                   subtitle = "Expression level shown as color intensity")

  # 保存 FeaturePlot
  plot_and_save(p_feat, "deg_featureplot_key_markers", subfolder = "DEG",
                width = 12, height = ceiling(length(genes_for_featureplot) / 3) * 4)

  cat("  ✓ FeaturePlot 包含", length(genes_for_featureplot), "个关键 marker\n")
} else {
  cat("  ⚠️  没有可用的 marker 基因用于 FeaturePlot\n")
}

cat("\n", strrep("=", 80), "\n")
cat("✓ 差异基因可视化完成\n")
cat(strrep("=", 80), "\n\n")
```

> **🔍 如何综合解读这三种可视化？**
>
> **步骤1：从 DotPlot 识别 cluster 特异性 marker**
> - 查找每个 cluster 的"大而红"的点
> - 例如：Cluster 0 高表达 Xcr1, Clec9a → 可能是 cDC1
>
> **步骤2：从 Heatmap 识别基因表达模式**
> - 查找垂直条纹（cluster 特异性）
> - 查找水平条纹（共享的功能状态）
> - 例如：Ccr7, Cd83 在多个 cluster 高表达 → 成熟 DC
>
> **步骤3：从 FeaturePlot 验证空间分布**
> - 确认 marker 是否真的局限于某个 cluster
> - 检查是否有梯度分布（提示连续的生物学过程）
>
> **常见模式**：
> - **清晰的细胞类型**：DotPlot 中每个 cluster 有 3-5 个特异性 marker
> - **功能状态连续变化**：FeaturePlot 中基因呈梯度分布（如 Ccr7 从低到高）
> - **技术性 cluster**：Heatmap 中某个 cluster 主要富集线粒体/核糖体基因 → 可能需要删除

---

# 8. ADT 分析和 WNN 融合

```{r wnn_integration}
################################################################################
##################### WNN 多模态融合 ############################################
################################################################################

seurat_obj_adt_rna_wnn <- load_or_compute(
  cache_file = paste0(config$cache_prefix, "_07_wnn_integrated.rds"),
  description = paste0("WNN 多模态融合 - ", config$cache_prefix),
  compute_fn = function() {
    cat("  步骤 1/8: 提取并对齐 ADT 矩阵...\n")
    # 提取 ADT
    raw_adt_matrix <- as.matrix(rawDataADT[, colnames(seurat_obj_clean)])

    # 添加 ADT Assay
    seurat_obj_clean[["ADT"]] <- CreateAssayObject(counts = raw_adt_matrix)

    cat("  步骤 2/8: ADT 归一化和降维...\n")
    seurat_obj_clean <- NormalizeData(seurat_obj_clean, assay = "ADT",
                                      normalization.method = "CLR", margin = 2, verbose = FALSE)
    seurat_obj_clean <- FindVariableFeatures(seurat_obj_clean, assay = "ADT",
                                             nfeatures = nrow(seurat_obj_clean[["ADT"]]), verbose = FALSE)
    seurat_obj_clean <- ScaleData(seurat_obj_clean, assay = "ADT", verbose = FALSE)
    seurat_obj_clean <- RunPCA(seurat_obj_clean, assay = "ADT",
                               reduction.name = "adt_pca", reduction.key = "adtPC_",
                               npcs = min(30, nrow(seurat_obj_clean[["ADT"]]) - 1), verbose = FALSE)

    cat("  步骤 3/8: ADT 驱动的聚类...\n")
    seurat_obj_clean <- FindNeighbors(seurat_obj_clean, reduction = "adt_pca",
                                      dims = 1:min(config$dims_adt, 20), verbose = FALSE)
    seurat_obj_clean <- FindClusters(seurat_obj_clean, resolution = config$resolution,
                                     graph.name = "ADT_snn", verbose = FALSE)
    seurat_obj_clean$ADT_clusters <- Idents(seurat_obj_clean)

    seurat_obj_clean <- RunUMAP(seurat_obj_clean, reduction = "adt_pca",
                                dims = 1:min(config$dims_adt, 15),
                                reduction.name = "adt_umap", reduction.key = "adtUMAP_", verbose = FALSE)

    cat("  步骤 4/8: RNA 驱动的聚类...\n")
    # 确定用哪个 RNA reduction
    rna_reduction <- if (analysis_mode == "integrated" && config$batch_correction &&
                        "harmony" %in% names(seurat_obj_clean@reductions)) {
      "harmony"
    } else {
      "pca"
    }

    seurat_obj_clean <- FindNeighbors(seurat_obj_clean, reduction = rna_reduction,
                                      dims = 1:config$dims_rna, verbose = FALSE)
    seurat_obj_clean <- FindClusters(seurat_obj_clean, resolution = config$resolution - 0.2,
                                     graph.name = "SCT_snn", verbose = FALSE)
    seurat_obj_clean$SCT_clusters <- Idents(seurat_obj_clean)

    # ==================== 💾 保存 Pre-WNN 对象（用于脚本4） ====================
    # 此时对象包含：
    # ✅ 独立的 ADT 聚类：ADT_clusters, adt_umap, adt_pca
    # ✅ 独立的 RNA 聚类：SCT_clusters, umap, tsne, pca/harmony
    # ❌ 尚未进行 WNN 整合：无 wnn_umap, WNN_clusters
    pre_wnn_rds <- paste0(output.dir, "Robjects/", config$project_name, "_pre_wnn.rds")
    saveRDS(seurat_obj_clean, pre_wnn_rds)
    cat("\n  💾 Pre-WNN 对象已保存（用于脚本4等下游分析）\n")
    cat("     文件:", pre_wnn_rds, "\n")
    cat("     包含: SCT_clusters (", length(unique(seurat_obj_clean$SCT_clusters)),
        " clusters) + ADT_clusters (", length(unique(seurat_obj_clean$ADT_clusters)), " clusters)\n\n")

    cat("  步骤 5/8: 计算 WNN 权重...\n")
    seurat_obj_clean <- FindMultiModalNeighbors(
      seurat_obj_clean,
      reduction.list = list(rna_reduction, "adt_pca"),
      dims.list = list(1:config$dims_rna, 1:min(config$dims_adt, 20)),
      verbose = FALSE
    )

    cat("  步骤 6/8: 运行 WNN UMAP...\n")
    seurat_obj_clean <- RunUMAP(seurat_obj_clean, nn.name = "weighted.nn",
                                reduction.name = "wnn_umap", reduction.key = "wnnUMAP_", verbose = FALSE)

    cat("  步骤 7/8: WNN 聚类...\n")
    seurat_obj_clean <- FindClusters(seurat_obj_clean, graph.name = "wsnn",
                                     resolution = config$resolution, verbose = FALSE)
    seurat_obj_clean$WNN_clusters <- Idents(seurat_obj_clean)

    cat("  步骤 8/8: 完成!\n")
    return(seurat_obj_clean)
  }
)

cat("\n✓ WNN 融合完成\n")
cat("  WNN 聚类数:", length(unique(seurat_obj_adt_rna_wnn$WNN_clusters)), "\n\n")
```

## 8.1 WNN 可视化对比

```{r wnn_visualization, fig.width=18, fig.height=6}
################################################################################
##################### WNN 多模态聚类对比 ##########################################
################################################################################
#
# 【功能说明】
# 对比三种聚类策略：ADT 驱动、RNA 驱动、WNN 整合
#
# 【解读要点】
# - ADT-driven: 基于蛋白表达的聚类（分辨率低但更稳定）
# - RNA-driven: 基于转录组的聚类（分辨率高但受批次影响）
# - WNN-combined: 整合两者优势的最终聚类
#
################################################################################

# 确保有足够的颜色
n_wnn_clusters <- length(unique(seurat_obj_adt_rna_wnn$WNN_clusters))
if (is.null(config$colors) || length(config$colors) < n_wnn_clusters) {
  config$colors <- generate_colors(n_wnn_clusters)
}

# 三种驱动方式对比
p_adt <- DimPlot(seurat_obj_adt_rna_wnn, reduction = "adt_umap", group.by = "ADT_clusters",
                label = TRUE, label.size = 4) +
  ggtitle("ADT-driven Clustering") +
  theme(legend.position = "bottom")

p_rna <- DimPlot(seurat_obj_adt_rna_wnn, reduction = "umap", group.by = "SCT_clusters",
                label = TRUE, label.size = 4) +
  ggtitle("RNA-driven Clustering") +
  theme(legend.position = "bottom")

p_wnn <- DimPlot(seurat_obj_adt_rna_wnn, reduction = "wnn_umap", group.by = "WNN_clusters",
                label = TRUE, label.size = 4, cols = config$colors) +
  ggtitle("WNN-combined Clustering") +
  theme(legend.position = "bottom")

# 组合并保存
p_wnn_comparison <- p_adt + p_rna + p_wnn +
  plot_annotation(title = "WNN Multi-Modal Clustering Comparison",
                 subtitle = paste0("ADT clusters: ", length(unique(seurat_obj_adt_rna_wnn$ADT_clusters)),
                                  " | RNA clusters: ", length(unique(seurat_obj_adt_rna_wnn$SCT_clusters)),
                                  " | WNN clusters: ", n_wnn_clusters))

plot_and_save(p_wnn_comparison, "wnn_clustering_comparison",
              subfolder = "WNN", width = 18, height = 7)
```

## 8.2 基因 vs 蛋白对比

> **为什么要对比基因和蛋白表达？**
>
> CITE-seq 的独特优势是可以同时测量 mRNA（基因）和蛋白表达。理想情况下，基因和蛋白表达应该高度一致，但也可能出现差异：
> - **转录后调控**：蛋白表达滞后于 mRNA
> - **蛋白稳定性**：某些蛋白半衰期很长
> - **技术差异**：ADT 和 RNA 检测灵敏度不同

```{r gene_protein_comparison, fig.width=12, fig.height=5}
# 检查是否有对应的 ADT marker
# 示例：Ccr7 基因 vs CCR7/CD197 蛋白
if ("Ccr7" %in% rownames(seurat_obj_adt_rna_wnn[["SCT"]])) {
  adt_features <- grep("CD197|CCR7", rownames(seurat_obj_adt_rna_wnn[["ADT"]]),
                      value = TRUE, ignore.case = TRUE)

  if (length(adt_features) > 0) {
    p_gene_protein <- FeaturePlot(seurat_obj_adt_rna_wnn,
                                  features = c("Ccr7", adt_features[1]),
                                  reduction = "wnn_umap", ncol = 2,
                                  cols = c("lightgrey", "#DE2D26")) +
      plot_annotation(title = "Gene vs Protein Expression Comparison",
                     subtitle = "Ccr7 mRNA (left) vs CCR7 protein (right)")

    plot_and_save(p_gene_protein, "gene_vs_protein_Ccr7",
                  subfolder = "WNN", width = 12, height = 5)
  } else {
    cat("⚠️  未找到 CCR7/CD197 ADT marker\n")
  }
} else {
  cat("⚠️  未找到 Ccr7 基因\n")
}
```

## 8.3 WNN 权重分布

> **如何解读 WNN 权重？**
>
> WNN 为每个细胞分配 RNA 和 ADT 的权重，表示哪种模态对该细胞的聚类贡献更大：
> - **RNA weight 高**：该细胞主要由转录组驱动聚类（如高异质性的细胞亚群）
> - **ADT weight 高**：该细胞主要由蛋白表达驱动聚类（如稳定的表面标志物）
> - **权重平衡**：两种模态贡献相当
>
> **不同 cluster 的权重差异反映了生物学特性**：
> - 成熟 DC（高 CCR7 蛋白）可能 ADT weight 更高
> - 增殖细胞（高转录活性）可能 RNA weight 更高

```{r wnn_weights, fig.width=12, fig.height=5}
# 检查权重列名
weight_cols <- grep("weight", colnames(seurat_obj_adt_rna_wnn@meta.data), value = TRUE, ignore.case = TRUE)

if (length(weight_cols) > 0) {
  cat("找到 WNN 权重列:", paste(weight_cols, collapse = ", "), "\n")

  # 通常是 "RNA.weight" 或 "ADT.weight"
  rna_weight <- grep("RNA", weight_cols, value = TRUE, ignore.case = TRUE)[1]
  adt_weight <- grep("ADT", weight_cols, value = TRUE, ignore.case = TRUE)[1]

  plots_to_combine <- list()

  if (!is.na(rna_weight)) {
    p_weight_rna <- VlnPlot(seurat_obj_adt_rna_wnn, features = rna_weight,
                           group.by = "WNN_clusters", pt.size = 0) +
      ggtitle("RNA Modality Weight") +
      labs(subtitle = "Higher = RNA-driven clustering") +
      NoLegend() +
      theme(axis.text.x = element_text(angle = 45, hjust = 1))
    plots_to_combine[[1]] <- p_weight_rna
  }

  if (!is.na(adt_weight)) {
    p_weight_adt <- VlnPlot(seurat_obj_adt_rna_wnn, features = adt_weight,
                           group.by = "WNN_clusters", pt.size = 0) +
      ggtitle("ADT Modality Weight") +
      labs(subtitle = "Higher = ADT-driven clustering") +
      NoLegend() +
      theme(axis.text.x = element_text(angle = 45, hjust = 1))
    plots_to_combine[[2]] <- p_weight_adt
  }

  if (length(plots_to_combine) > 0) {
    p_weights <- wrap_plots(plots_to_combine, ncol = 2) +
      plot_annotation(title = "WNN Modality Weights across Clusters",
                     subtitle = "Weight distribution shows which modality drives clustering for each cluster")

    plot_and_save(p_weights, "wnn_modality_weights",
                  subfolder = "WNN", width = 12, height = 5)
  }
} else {
  cat("⚠️  未找到 WNN 权重列，可能是 WNN 整合未成功\n")
}
```

## 8.4 WNN 差异基因分析

```{r wnn_deg}
Idents(seurat_obj_adt_rna_wnn) <- "WNN_clusters"
DefaultAssay(seurat_obj_adt_rna_wnn) <- "SCT"

wnn_markers <- load_or_compute(
  cache_file = paste0(config$cache_prefix, "_08_wnn_deg.rds"),
  description = paste0("WNN 差异基因分析 - ", config$cache_prefix),
  compute_fn = function() {
    markers <- FindAllMarkers(seurat_obj_adt_rna_wnn, assay = "SCT", slot = "data",
                             only.pos = TRUE, verbose = FALSE)
    cat("\n  找到", nrow(markers), "个差异表达基因\n")
    return(markers)
  }
)

# 保存
wnn_deg_csv <- paste0(output.dir, "Files/", config$project_name, "_WNN_DEG.csv")
write.csv(wnn_markers, wnn_deg_csv, row.names = FALSE)
cat("✓ WNN 差异基因列表已保存:", wnn_deg_csv, "\n\n")
```

---

# 9. 最终结果保存

```{r save_final_results}
################################################################################
##################### 保存最终结果 ##############################################
################################################################################

# 保存最终 Seurat 对象
final_rds <- paste0(output.dir, "Robjects/", config$project_name, "_final.rds")
saveRDS(seurat_obj_adt_rna_wnn, final_rds)
cat("✓ 最终对象已保存:", final_rds, "\n")

# 保存关键信息汇总
summary_info <- list(
  analysis_mode = analysis_mode,
  project_name = config$project_name,
  total_cells = ncol(seurat_obj_adt_rna_wnn),
  total_genes = nrow(seurat_obj_adt_rna_wnn),
  n_clusters_RNA = length(unique(seurat_obj_adt_rna_wnn$SCT_clusters)),
  n_clusters_ADT = length(unique(seurat_obj_adt_rna_wnn$ADT_clusters)),
  n_clusters_WNN = length(unique(seurat_obj_adt_rna_wnn$WNN_clusters)),
  dims_used = list(RNA = config$dims_rna, ADT = config$dims_adt),
  resolution = config$resolution
)

if (analysis_mode == "integrated") {
  summary_info$batch_correction <- config$integration_method
  summary_info$cell_counts <- table(seurat_obj_adt_rna_wnn$batch)
}

if (analysis_mode == "full") {
  summary_info$predicted_types <- table(seurat_obj_adt_rna_wnn$cell_type_predicted)
}

summary_file <- paste0(output.dir, "Files/", config$project_name, "_summary.rds")
saveRDS(summary_info, summary_file)

cat("\n", strrep("=", 80), "\n")
cat("🎉 分析完成!\n")
cat(strrep("=", 80), "\n")
cat("  模式:", analysis_mode, "\n")
cat("  项目:", config$project_name, "\n")
cat("  细胞数:", summary_info$total_cells, "\n")
cat("  基因数:", summary_info$total_genes, "\n")
cat("  WNN 聚类数:", summary_info$n_clusters_WNN, "\n")
if (analysis_mode == "full") {
  cat("\n预测的细胞类型:\n")
  print(summary_info$predicted_types)
}
cat("\n输出文件:\n")
cat("  - 最终对象:", final_rds, "\n")
cat("  - 差异基因 (RNA):", deg_csv, "\n")
cat("  - 差异基因 (WNN):", wnn_deg_csv, "\n")
cat("  - 分析摘要:", summary_file, "\n")
cat(strrep("=", 80), "\n\n")
```

---

# 10. 会话信息

```{r session_info}
sessionInfo()
```

---

# 附录: 缓存管理工具

```{r cache_tools, eval=FALSE}
# 查看当前模式的缓存
list_mode_cache <- function(mode = analysis_mode) {
  files <- list.files(cache_dir, pattern = paste0("^", mode, "_"), full.names = TRUE)
  if (length(files) == 0) {
    cat("No cache files found for mode:", mode, "\n")
    return(NULL)
  }

  info <- file.info(files)
  info$file <- basename(rownames(info))
  info$size_mb <- round(info$size / 1024 / 1024, 2)
  return(info[order(-info$size_mb), c("file", "size_mb", "mtime")])
}

# 清除特定模式的缓存
clear_mode_cache <- function(mode) {
  files <- list.files(cache_dir, pattern = paste0("^", mode, "_"), full.names = TRUE)
  if (length(files) > 0) {
    file.remove(files)
    cat("✓ Removed", length(files), "cache files for mode:", mode, "\n")
  } else {
    cat("No cache files to remove for mode:", mode, "\n")
  }
}

# 示例用法
# list_mode_cache("cDC1")
# list_mode_cache("cDC2")
# list_mode_cache("integrated")
# list_mode_cache("full")
# clear_mode_cache("cDC2")  # 谨慎使用!
```

---

**分析完成时间:** `r Sys.time()`

**脚本版本:** v3.0 - 完整版 (包含全量分析 + 自动读取 Idents)
