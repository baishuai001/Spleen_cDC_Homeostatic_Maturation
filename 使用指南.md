# CITEViz 错误解决方案

## 问题：[object Object] 错误

这个错误表明 Seurat 对象的格式不兼容 CITEViz。

---

## 解决方案 1：修复 Seurat 对象（推荐）

### 步骤 1：运行修复脚本

```bash
cd /home/user/Spleen_cDC_Homeostatic_Maturation
Rscript fix_seurat_for_citeviz.R
```

这会创建一个兼容的文件：`seurat_obj_citeviz_compatible.rds`

### 步骤 2：使用修复后的对象

**方式 A：在 CITEViz 网页上传**
- 在 CITEViz 的 "Getting Started" 页面
- 点击 "Browse..."
- 选择：`seurat_obj_citeviz_compatible.rds`
- 上传并开始分析

**方式 B：使用脚本启动**
```bash
# 编辑 run_citeviz.R，修改路径
DATA_PATH <- "seurat_obj_citeviz_compatible.rds"

# 运行
Rscript run_citeviz.R
```

---

## 解决方案 2：使用 Seurat 静态分析（更快，无错误）

如果 CITEViz 仍有问题，直接使用 Seurat：

```bash
# 运行静态分析脚本
Rscript adt_analysis_seurat.R
```

**输出**：
- ADT 双参数散点图（类似 FlowJo）
- ADT 在 UMAP 上的表达
- 各细胞类型的 ADT 热图
- 手动门控示例
- ADT 相关性矩阵

**优点**：
- ✅ 无需交互界面，不卡顿
- ✅ 生成高质量静态图
- ✅ 可以自定义门控阈值
- ✅ 结果保存为 PNG，方便分享

**输出位置**：
```
results/cDC1/ADT_analysis/
├── scatter_CD11c_vs_CD317.png
├── scatter_CD11c_vs_Sirpa.png
├── scatter_CD80_vs_CD86.png
├── adt_umap_overlay.png
├── adt_heatmap_by_celltype.png
├── manual_gating_example.png
└── adt_correlation.png
```

---

## 解决方案 3：手动调试 CITEViz

### 可能的问题和修复

**问题 1：ADT 名称格式**
- CITEViz 可能期望特定的前缀（如 "adt-" 或 "ADT-"）
- 检查您的 ADT 名称格式

**问题 2：Seurat 版本不兼容**
- CITEViz 可能不支持 Seurat v5
- 尝试降级到 Seurat v4

**问题 3：降维名称**
- 选择下拉菜单中的其他选项
- 尝试 "umap" 而不是 "wnn_umap"

**问题 4：数据层问题**
- Seurat v5 使用新的 layer 系统
- 需要合并数据层

---

## 推荐流程

### 第一次尝试（最快）
```bash
# 使用 Seurat 静态分析
Rscript adt_analysis_seurat.R

# 查看结果
ls -lh results/cDC1/ADT_analysis/
```

### 如果需要交互式分析
```bash
# 1. 修复 Seurat 对象
Rscript fix_seurat_for_citeviz.R

# 2. 启动 CITEViz（快速版）
Rscript run_citeviz_fast.R

# 3. 在浏览器中上传
#    seurat_obj_citeviz_compatible.rds
```

---

## 对比：静态分析 vs CITEViz

| 特性 | Seurat 静态分析 | CITEViz 交互式 |
|------|----------------|----------------|
| **速度** | ⚡️ 快（秒级） | 🐌 慢（分钟级） |
| **卡顿** | ✅ 无 | ❌ 可能卡顿 |
| **门控** | 🔧 代码定义 | 🖱️ 鼠标拖拽 |
| **兼容性** | ✅ 无问题 | ⚠️ 可能报错 |
| **适用场景** | 批量分析、高质量图 | 探索性分析 |

---

## 故障排除

### CITEViz 持续报错
➡️ **改用 Seurat 静态分析**

### 想要交互式门控
➡️ **使用 FlowJo 风格的 R 代码**

示例：
```r
# 在 adt_analysis_seurat.R 中自定义门控
cd11c <- GetAssayData(seurat_obj, assay = "ADT")["adt-CD11c", ]
cd317 <- GetAssayData(seurat_obj, assay = "ADT")["adt-CD317", ]

# 定义您的门控阈值
seurat_obj$my_gate <- ifelse(
  cd11c > 2 & cd317 < 1,  # 修改这里的阈值
  "CD11c+ CD317-",
  "Other"
)

# 可视化
DimPlot(seurat_obj, group.by = "my_gate")
```

### 数据太大导致卡顿
➡️ **使用 run_citeviz_fast.R（下采样）**

---

## 总结

**最简单的方案**：
```bash
# 一行命令，无错误，快速出图
Rscript adt_analysis_seurat.R
```

**如果坚持使用 CITEViz**：
```bash
# 先修复对象
Rscript fix_seurat_for_citeviz.R

# 再启动（使用修复后的文件）
Rscript run_citeviz_fast.R
```
