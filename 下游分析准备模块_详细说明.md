# "下游分析准备"模块详细说明

## 📋 模块概述

**位置：** `4.downstream_analysis.Rmd` 第 2345-2550 行

**核心目标：** 将 Seurat 分析的结果转换成其他专业分析工具所需的数据格式，方便进行后续的深度分析。

---

## 🎯 为什么需要这个模块？

### 问题背景

在 `CITE-seq_Complete_Analysis_v3.Rmd` 中，我们完成了：
- ✅ 质控、归一化、降维、聚类
- ✅ RNA + ADT 多模态整合（WNN）
- ✅ 细胞类型注释

**但是**，Seurat 对象（`.rds` 文件）是 R 语言特有的数据结构，其他专业分析工具**无法直接使用**：

| 分析工具 | 主要用途 | 需要的数据格式 |
|---------|---------|--------------|
| **Monocle3** | 拟时序分析（细胞发育轨迹） | 表达矩阵 + UMAP 坐标 + 元数据 |
| **CellOracle** | 基因调控网络（GRN）预测 | 归一化矩阵 + UMAP 坐标 + 高变基因 |
| **scTenifoldKnk** | 基因敲除模拟（扰动分析） | 原始计数矩阵 + 细胞类型 |
| **Python 工具** | 自定义分析（如 scanpy） | CSV/TSV 表格文件 |

### 解决方案

**"下游分析准备"模块的作用**：从 Seurat 对象中提取关键数据，按照各个工具的要求重新打包，导出成标准格式。

---

## 🔧 模块功能详解

### 1️⃣ 保存带注释的 Seurat 对象

**代码位置：** 第 2365-2384 行

**功能：** 保存一个完整的、带有作者注释的 Seurat 对象，方便以后随时加载。

```r
# 设置默认细胞身份（Idents）为作者注释
Idents(seurat_obj) <- seurat_obj$author_annotation

# 保存完整对象
saveRDS(seurat_obj, "seurat_obj_annotated.rds")
```

**输出文件：** `results/{mode}/Robjects/seurat_obj_annotated.rds`

**用途：**
- 后续如果需要用 Seurat 做补充分析，可以直接加载这个文件
- 避免重新运行整个 CITE-seq 分析流程

---

### 2️⃣ 为 Monocle3 准备数据

**代码位置：** 第 2386-2424 行

**Monocle3 是什么？**
- 专门用于**拟时序分析（pseudotime analysis）**的工具
- 可以推断细胞的发育轨迹（例如：Pre-cDC1 → Proliferating → Immature → Mature）
- 需要基因表达矩阵、降维坐标（UMAP）、细胞类型等信息

**提取的数据：**

```r
monocle3_data <- list(
  counts = GetAssayData(seurat_obj, slot = "counts", assay = "RNA"),  # 原始计数
  normalized = GetAssayData(seurat_obj, slot = "data", assay = "SCT"),  # 归一化表达
  metadata = seurat_obj@meta.data,  # 细胞元数据（包含注释、聚类等）
  umap = Embeddings(seurat_obj, "umap"),  # RNA UMAP 坐标
  wnn_umap = Embeddings(seurat_obj, "wnn_umap"),  # 🆕 WNN UMAP 坐标（整合RNA+ADT）
  cell_type = seurat_obj$author_annotation,  # 细胞类型
  pca = Embeddings(seurat_obj, "pca")  # PCA 结果（可选）
)
```

**输出文件：** `results/{mode}/Robjects/data_for_monocle3.rds`

**使用示例：**

```r
# 在 Monocle3 脚本中
library(monocle3)

# 加载准备好的数据
data <- readRDS("results/cDC1/Robjects/data_for_monocle3.rds")

# 创建 Monocle3 对象
cds <- new_cell_data_set(
  expression_data = data$counts,
  cell_metadata = data$metadata
)

# 添加 UMAP 坐标（可以选择 RNA UMAP 或 WNN UMAP）
reducedDims(cds)$UMAP <- data$wnn_umap  # 使用 WNN UMAP

# 运行拟时序分析
cds <- learn_graph(cds)
cds <- order_cells(cds)

# 可视化发育轨迹
plot_cells(cds, color_cells_by = "pseudotime")
```

---

### 3️⃣ 为 CellOracle 准备数据

**代码位置：** 第 2426-2461 行

**CellOracle 是什么？**
- Python 工具，用于构建**基因调控网络（Gene Regulatory Network, GRN）**
- 可以预测转录因子如何调控基因表达
- 可以模拟基因扰动（如敲除某个转录因子）对细胞命运的影响

**提取的数据：**

```r
celloracle_data <- list(
  expression_matrix = as.matrix(GetAssayData(seurat_obj, slot = "data", assay = "SCT")),  # 归一化矩阵
  metadata = seurat_obj@meta.data,  # 元数据
  umap_coords = as.data.frame(Embeddings(seurat_obj, "umap")),  # RNA UMAP 坐标
  wnn_umap_coords = as.data.frame(Embeddings(seurat_obj, "wnn_umap")),  # 🆕 WNN UMAP 坐标
  cell_type = as.character(seurat_obj$author_annotation),  # 细胞类型
  variable_genes = VariableFeatures(seurat_obj)  # 高变基因（用于GRN构建）
)
```

**输出文件：** `results/{mode}/Robjects/data_for_celloracle.rds`

**使用示例：**

```python
# 在 Python CellOracle 脚本中
import celloracle as co
import pandas as pd
import pickle

# 加载准备好的数据
with open("results/cDC1/Robjects/data_for_celloracle.rds", "rb") as f:
    data = pickle.load(f)

# 创建 CellOracle 对象
oracle = co.Oracle()

# 添加表达数据和 UMAP 坐标
oracle.adata = sc.AnnData(
    X=data['expression_matrix'],
    obs=data['metadata'],
    obsm={'X_umap': data['wnn_umap_coords']}  # 使用 WNN UMAP
)

# 构建基因调控网络
oracle.get_cluster_specific_TFdict_from_Links()
oracle.fit_GRN_for_simulation()

# 模拟转录因子扰动
oracle.simulate_shift(perturb_condition={"Irf8": 0.0})  # 模拟 Irf8 敲除
```

---

### 4️⃣ 为 scTenifoldKnk 准备数据

**代码位置：** 第 2463-2486 行

**scTenifoldKnk 是什么？**
- 用于**虚拟基因敲除（virtual gene knockout）**模拟
- 预测敲除某个基因后对细胞状态的影响
- 基于张量分解（tensor decomposition）的方法

**提取的数据：**

```r
sctenifold_data <- list(
  counts = as.matrix(GetAssayData(seurat_obj, slot = "counts", assay = "RNA")),  # 原始计数
  metadata = seurat_obj@meta.data,  # 元数据
  cell_type = as.character(seurat_obj$author_annotation)  # 细胞类型
)
```

**输出文件：** `results/{mode}/Robjects/data_for_sctenifoldknk.rds`

**使用示例：**

```r
# 在 scTenifoldKnk 脚本中
library(scTenifoldKnk)

# 加载数据
data <- readRDS("results/cDC1/Robjects/data_for_sctenifoldknk.rds")

# 运行虚拟敲除分析
result <- scTenifoldKnk(
  X = data$counts,
  knockout_gene = "Irf8",  # 模拟敲除 Irf8
  cell_labels = data$cell_type
)

# 查看哪些基因受 Irf8 调控
head(result$diff_genes)
```

---

### 5️⃣ 导出通用格式（CSV）

**代码位置：** 第 2488-2541 行

**功能：** 导出 CSV 格式的表格文件，方便在 Python、Excel、或其他工具中使用。

**导出的文件：**

#### a. 细胞元数据

```r
write.csv(seurat_obj@meta.data, "cell_metadata.csv")
```

**文件内容示例：**

| 细胞ID | nCount_RNA | nFeature_RNA | author_annotation | SCT_clusters | WNN_clusters |
|--------|-----------|-------------|------------------|--------------|--------------|
| AAACCCAAGAAACCCA | 5432 | 1823 | Pre-cDC1s | 0 | 0 |
| AAACCCAAGAAACGCC | 6821 | 2134 | Early mature cDC1 | 2 | 3 |
| ... | ... | ... | ... | ... | ... |

**用途：** 任何分析工具都可以读取这个表格，获取细胞的基本信息和注释。

---

#### b. RNA UMAP 坐标

```r
umap_df <- as.data.frame(Embeddings(seurat_obj, "umap"))
umap_df$cell_id <- rownames(umap_df)
umap_df$cell_type <- seurat_obj$author_annotation
umap_df$cluster <- seurat_obj$SCT_clusters

write.csv(umap_df, "umap_coordinates.csv")
```

**文件内容示例：**

| UMAP_1 | UMAP_2 | cell_id | cell_type | cluster |
|--------|--------|---------|-----------|---------|
| -2.34 | 5.67 | AAACCCAAGAAACCCA | Pre-cDC1s | 0 |
| 3.21 | -1.45 | AAACCCAAGAAACGCC | Early mature cDC1 | 2 |
| ... | ... | ... | ... | ... |

**用途：**
- 在 Python 中用 `matplotlib` 或 `seaborn` 绘制 UMAP 图
- 在 Excel 中分析 UMAP 坐标分布

---

#### c. WNN UMAP 坐标（新增）⭐

```r
if ("wnn_umap" %in% names(seurat_obj@reductions)) {
  wnn_umap_df <- as.data.frame(Embeddings(seurat_obj, "wnn_umap"))
  wnn_umap_df$cell_id <- rownames(wnn_umap_df)
  wnn_umap_df$cell_type <- seurat_obj$author_annotation
  wnn_umap_df$cluster <- seurat_obj$SCT_clusters
  wnn_umap_df$WNN_cluster <- seurat_obj$WNN_clusters

  write.csv(wnn_umap_df, "wnn_umap_coordinates.csv")
}
```

**文件内容示例：**

| wnnUMAP_1 | wnnUMAP_2 | cell_id | cell_type | cluster | WNN_cluster |
|-----------|-----------|---------|-----------|---------|-------------|
| -3.12 | 4.56 | AAACCCAAGAAACCCA | Pre-cDC1s | 0 | 0 |
| 2.78 | -2.34 | AAACCCAAGAAACGCC | Early mature cDC1 | 2 | 3 |
| ... | ... | ... | ... | ... | ... |

**用途：**
- 在外部工具中使用 WNN UMAP 坐标绘图
- 对比 RNA UMAP 和 WNN UMAP 的差异

---

#### d. 基因列表参考

```r
gene_lists <- list(
  cDC1_stages = genes_cDC1_stages,  # cDC1 各阶段标志基因
  cDC2_stages = genes_cDC2_stages,  # cDC2 各阶段标志基因
  lineage_markers = list(cDC1 = genes_cDC1, cDC2 = genes_cDC2),  # 谱系标志
  functional = list(maturation = genes_maturation,
                    migration = genes_migration,
                    functional = genes_functional)  # 功能基因
)

saveRDS(gene_lists, "gene_lists_reference.rds")
```

**用途：** 在后续分析中快速查找关键基因，无需重新定义。

---

## 📂 完整输出文件结构

运行这个模块后，会在 `results/{mode}/` 目录下生成以下文件：

```
results/cDC1/
├── Robjects/
│   ├── seurat_obj_annotated.rds          # 带注释的完整 Seurat 对象
│   ├── data_for_monocle3.rds             # Monocle3 拟时序分析数据
│   ├── data_for_celloracle.rds           # CellOracle 基因调控网络数据
│   ├── data_for_sctenifoldknk.rds        # scTenifoldKnk 基因敲除数据
│   └── gene_lists_reference.rds          # 基因列表参考
│
└── Tables/
    ├── cell_metadata.csv                  # 细胞元数据（所有注释、QC信息）
    ├── umap_coordinates.csv               # RNA UMAP 坐标 + 细胞类型
    └── wnn_umap_coordinates.csv           # 🆕 WNN UMAP 坐标 + 细胞类型
```

---

## 🎯 使用场景示例

### 场景 1：拟时序分析（Monocle3）

**研究问题：** cDC1 从 Pre-cDC1 发育到 Mature cDC1 的轨迹是怎样的？

**使用数据：** `data_for_monocle3.rds`

**分析流程：**
1. 加载数据并创建 Monocle3 对象
2. 在 WNN UMAP 上推断发育轨迹
3. 识别轨迹关键分支点
4. 找出轨迹不同阶段的差异表达基因

---

### 场景 2：基因调控网络分析（CellOracle）

**研究问题：** 转录因子 Irf8 在 cDC1 发育中调控哪些基因？

**使用数据：** `data_for_celloracle.rds`

**分析流程：**
1. 构建 cDC1 特异的基因调控网络
2. 识别 Irf8 的下游靶基因
3. 模拟敲除 Irf8，预测细胞命运变化
4. 在 WNN UMAP 上可视化扰动效果

---

### 场景 3：基因敲除模拟（scTenifoldKnk）

**研究问题：** 敲除 Batf3 会影响哪些基因的表达？

**使用数据：** `data_for_sctenifoldknk.rds`

**分析流程：**
1. 虚拟敲除 Batf3
2. 预测受影响的基因网络
3. 与实验数据对比验证

---

### 场景 4：Python 自定义分析

**研究问题：** 用 Python scanpy 包做补充可视化

**使用数据：** `cell_metadata.csv` + `wnn_umap_coordinates.csv`

**分析流程：**

```python
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 读取数据
metadata = pd.read_csv("results/cDC1/Tables/cell_metadata.csv", index_col=0)
wnn_umap = pd.read_csv("results/cDC1/Tables/wnn_umap_coordinates.csv")

# 合并数据
df = wnn_umap.merge(metadata[['author_annotation']], left_on='cell_id', right_index=True)

# 绘制 WNN UMAP
plt.figure(figsize=(10, 8))
sns.scatterplot(data=df, x='wnnUMAP_1', y='wnnUMAP_2',
                hue='author_annotation', s=10, alpha=0.7)
plt.title("cDC1 Development on WNN UMAP")
plt.show()
```

---

## 🔑 关键要点总结

| 数据文件 | 主要用途 | 包含 WNN UMAP |
|---------|---------|--------------|
| `seurat_obj_annotated.rds` | Seurat 后续分析 | ✅ |
| `data_for_monocle3.rds` | 拟时序轨迹分析 | ✅ |
| `data_for_celloracle.rds` | 基因调控网络 | ✅ |
| `data_for_sctenifoldknk.rds` | 基因敲除模拟 | ❌（不需要 UMAP） |
| `cell_metadata.csv` | 通用元数据 | ❌（在 metadata 列中） |
| `umap_coordinates.csv` | RNA UMAP 可视化 | ❌ |
| `wnn_umap_coordinates.csv` | WNN UMAP 可视化 | ✅ |
| `gene_lists_reference.rds` | 基因列表查询 | ❌（不需要 UMAP） |

---

## ❓ 常见问题

### Q1: 为什么要导出这么多格式的数据？

**A:** 不同的分析工具有不同的数据格式要求：
- Monocle3 需要原始计数 + 归一化数据
- CellOracle 只需要归一化数据 + 高变基因
- Python 工具通常需要 CSV 表格
- 提前准备好可以节省后续分析的时间

### Q2: 我一定要用这些下游分析工具吗？

**A:** 不一定。这个模块是**可选的**，如果您：
- 只需要 Seurat 的基本分析结果 → 不需要运行这个模块
- 需要做轨迹分析或基因调控网络 → 建议运行这个模块

### Q3: WNN UMAP 和 RNA UMAP 在下游分析中该选哪个？

**A:** 推荐优先使用 **WNN UMAP**，因为：
- WNN UMAP 整合了 RNA + ADT 蛋白信息，更全面
- 细胞分群更准确，轨迹推断更可靠
- RNA UMAP 可以作为对比参考

### Q4: 导出的 `.rds` 文件可以在 Python 中使用吗？

**A:** 可以，但需要转换：

```python
# 方法 1: 使用 rpy2 包
import rpy2.robjects as ro
from rpy2.robjects import pandas2ri

pandas2ri.activate()
readRDS = ro.r['readRDS']
data = readRDS("data_for_monocle3.rds")

# 方法 2: 在 R 中先转换成 pickle 格式
# R 代码：
# library(reticulate)
# data <- readRDS("data_for_monocle3.rds")
# py_save_object(data, "data_for_monocle3.pkl")
```

---

## 🎉 总结

**"下游分析准备"模块的核心作用：**

1. **数据格式转换** - 从 Seurat 专用格式转为通用格式
2. **提取关键信息** - 表达矩阵、UMAP 坐标、细胞注释等
3. **支持多种工具** - Monocle3、CellOracle、scTenifoldKnk、Python 工具
4. **包含 WNN UMAP** - 所有支持 UMAP 的数据都包含 WNN UMAP 坐标
5. **方便后续分析** - 一次准备，多次使用

**这个模块就像一个"数据中转站"，把 Seurat 分析的结果打包成各种专业工具需要的格式，让您可以无缝衔接到更深入的分析流程中。** 🚀
