# 4.downstream_analysis.Rmd 修改总结

## 📅 修改日期
2026-01-19

## 🎯 修改目标
1. 实施方案三：在脚本4中从RDS缓存文件读取并添加作者原始注释（已有功能）
2. 扩展"感兴趣的基因列表"，加入8组功能基因
3. 添加ADT蛋白可视化（发育门控蛋白）
4. 增强可视化代码：8组基因合并/分别DotPlot + FeaturePlot，ADT蛋白DotPlot + FeaturePlot

---

## ✅ 完成的修改

### 1. 作者注释自动加载（脚本原有功能）

**位置：** 第1343-1450行（原有代码）

**功能：**
- 自动从 `results/Cache/{mode}_01_subset_extracted.rds` 加载作者原始注释
- 支持三种模式的路径：
  - `cDC1_01_subset_extracted.rds`（cDC1模式）
  - `cDC2_01_subset_extracted.rds`（cDC2模式）
  - `integrated_01_subset_extracted.rds`（integrated模式）
- 添加作者注释字段到Seurat对象

---

### 2. 扩展基因列表（8组功能基因）

**位置：** 第419-458行（新增）

#### 2.1 八组功能基因

| 组别 | 基因列表 | 基因数 |
|------|---------|--------|
| **调控基因** | Cd274, Pdcd1lg2, Cd200, Fas, Aldh1a2, Socs1, Socs2 | 7 |
| **迁移基因** | Ccr7, Myo1g, Cxcl16, Adam8, Icam1, Fscn1, Marcks, Marcksl1 | 8 |
| **标记基因** | H2-Ab1, Itgax, Itgam, Itgae, Xcr1, Sirpa, Cd24a | 7 |
| **成熟基因** | Cd80, Cd86, Cd40, Relb, Cd83 | 5 |
| **TH2反应基因** | Il4ra, Il4i1, Ccl17, Ccl22, Tnfrsf4, Stat6, Bcl2l1 | 7 |
| **转录因子** | Sfpi1, Zbtb46, Irf8, Nfil3, Id2, Batf3, Irf4, Cebpa, Cebpb, Zeb2, Klf4, Tbx21, Rorc, Notch2, Nr4a3 | 15 |
| **发育门控** | Itgax, H2-Ab1, Flt3, Kit, Sirpa, Csf1r, Siglech, Ly6c1, Itgam, Spn, Fcgr1, Ly6d | 12 |

**总计：** 61个基因

#### 2.2 ADT蛋白列表

| 组别 | 蛋白标记 | 蛋白数 |
|------|---------|--------|
| **蛋白_发育门控** | adt_CD11c, adt_IA-IE, adt_CD135, adt_CD117, adt_CD172a, adt_CD115, adt_SiglecH, adt_Ly6C, adt_CD11b, adt_CD43, adt_CD64, adt_Ly6D | 12 |

---

### 3. 新增扩展基因组可视化代码

**位置：** 第2024-2221行（新增代码块）

**章节：** `## 🔥 扩展基因组可视化（8组基因 + ADT蛋白）`

#### 3.1 功能概述

新增可视化代码块包含以下4个部分：

1. **8组基因合并DotPlot**
   - 将8组基因放在一起的DotPlot
   - 限制最多80个基因（每组最多10个）
   - 使用RdYlBu颜色主题

2. **8组基因分别FeaturePlot**
   - 为每组基因生成单独的FeaturePlot
   - 每组最多显示9个基因（3x3布局）
   - 使用红色渐变（lightgrey -> red）

3. **ADT蛋白合并DotPlot**
   - 所有发育门控蛋白放在一起的DotPlot
   - 自动切换到ADT assay
   - 使用RdYlBu颜色主题

4. **ADT蛋白分别FeaturePlot**
   - 每批次显示9个蛋白（3x3布局）
   - 使用蓝色渐变（lightgrey -> blue）
   - 如果蛋白超过9个，会分批显示

#### 3.2 关键特性

- ✅ **自动检测可用基因**：只绘制在Seurat对象中存在的基因
- ✅ **错误处理**：
  - 检查ADT assay是否存在
  - 列出缺失的基因/蛋白
  - 显示各组基因的可用率
- ✅ **智能布局**：
  - 根据基因数量自动调整图片尺寸
  - DotPlot宽度：`max(16, n_genes * 0.3)`
  - FeaturePlot高度：`ceiling(n_genes / 3) * 4`
- ✅ **文件命名规范**：
  - 基因组：`extended_genes_dotplot_8groups.png`
  - 分组FeaturePlot：`extended_genes_featureplot_{组名}.png`
  - ADT DotPlot：`adt_proteins_dotplot.png`
  - ADT FeaturePlot：`adt_proteins_featureplot_batch{N}.png`

---

## 📊 输出文件清单

运行修改后的脚本将生成以下新文件：

### 基因可视化（在 `results/{mode}/Plots/Markers/` 目录）

1. **DotPlot：**
   - `extended_genes_dotplot_8groups.png` / `.pdf`

2. **FeaturePlot（7个文件，每组一个）：**
   - `extended_genes_featureplot_调控基因.png` / `.pdf`
   - `extended_genes_featureplot_迁移基因.png` / `.pdf`
   - `extended_genes_featureplot_标记基因.png` / `.pdf`
   - `extended_genes_featureplot_成熟基因.png` / `.pdf`
   - `extended_genes_featureplot_TH2反应基因.png` / `.pdf`
   - `extended_genes_featureplot_转录因子.png` / `.pdf`
   - `extended_genes_featureplot_发育门控.png` / `.pdf`

### ADT蛋白可视化（在 `results/{mode}/Plots/Markers/` 目录）

1. **DotPlot：**
   - `adt_proteins_dotplot.png` / `.pdf`

2. **FeaturePlot：**
   - `adt_proteins_featureplot_batch1.png` / `.pdf`
   - （如果蛋白超过9个，会有batch2等）

---

## 🔧 使用方法

### 在RStudio中运行

1. **设置参数：**
   ```r
   analysis_mode <- "cDC1"  # 或 "cDC2" 或 "integrated"
   ```

2. **运行脚本：**
   ```r
   rmarkdown::render("4.downstream_analysis.Rmd")
   ```

3. **查看输出：**
   - 图片：`results/{mode}/Plots/Markers/`
   - 对象：`results/{mode}/Robjects/seurat_obj_annotated.rds`

### 验证作者注释是否加载

```r
# 读取处理后的对象
seurat_obj <- readRDS("results/cDC1/Robjects/seurat_obj_annotated.rds")

# 检查作者注释
"author_annotation" %in% colnames(seurat_obj@meta.data)  # 应该返回 TRUE
table(seurat_obj$author_annotation)

# 可视化对比
library(Seurat)
library(ggplot2)

p1 <- DimPlot(seurat_obj, group.by = "SCT_clusters", label = TRUE)
p2 <- DimPlot(seurat_obj, group.by = "author_annotation", label = TRUE)
p1 | p2
```

---

## ⚠️ 注意事项

### 1. ADT蛋白名称格式
不同数据集ADT蛋白名称格式可能不同：
- `adt_CD11c`（Seurat默认）
- `adt-CD11c`
- `CD11c-adt`

脚本会自动检测可用的ADT并列出缺失的蛋白。

### 2. 基因名称大小写
小鼠基因名称通常首字母大写，后续小写：
- ✅ `Ccr7`
- ❌ `CCR7`

### 3. 缓存文件路径
确保以下缓存文件存在：
- cDC1模式：`results/Cache/cDC1_01_subset_extracted.rds`
- cDC2模式：`results/Cache/cDC2_01_subset_extracted.rds`
- integrated模式：`results/Cache/integrated_01_subset_extracted.rds`

这些文件由 `CITE-seq_Complete_Analysis_v3.Rmd` 的 Section 2.2 生成。

### 4. 内存使用
可视化大量基因会占用较多内存：
- FeaturePlot：每组9个基因（3x3）
- DotPlot：8组合并最多80个基因

如果遇到内存不足，可以减少基因数量。

---

## 📝 技术细节

### 代码结构

```r
# 第419-458行：定义扩展基因列表
genes_regulatory <- c(...)
genes_migration_extended <- c(...)
# ... 其他组 ...

genes_extended_groups <- list(
  `调控基因` = genes_regulatory,
  # ... 其他组 ...
)

adt_extended_groups <- list(
  `蛋白_发育门控` = adt_developmental_gating
)

# 第2024-2221行：扩展基因组可视化
if (has_author_annotation) {
  # 1. 准备基因列表，自动检测可用性
  # 2. 生成8组基因合并DotPlot
  # 3. 生成8组基因分别FeaturePlot
  # 4. 生成ADT蛋白合并DotPlot
  # 5. 生成ADT蛋白分别FeaturePlot
}
```

### 可视化参数

| 可视化类型 | 布局 | 颜色方案 | 文件格式 |
|-----------|------|---------|----------|
| 基因DotPlot | 自适应 | RdYlBu | PNG + PDF |
| 基因FeaturePlot | 3x3 | lightgrey->red | PNG + PDF |
| ADT DotPlot | 自适应 | RdYlBu | PNG + PDF |
| ADT FeaturePlot | 3x3 | lightgrey->blue | PNG + PDF |

---

## 🎯 下一步工作

1. **在云服务器RStudio上运行修改后的脚本**
   - 设置 `analysis_mode <- "cDC1"` / `"cDC2"` / `"integrated"`
   - Knit 脚本生成可视化

2. **验证输出结果**
   - 检查8组基因的DotPlot和FeaturePlot
   - 检查ADT蛋白的可视化
   - 验证作者注释是否正确添加

3. **后续分析**
   - 使用扩展基因列表进行差异分析
   - 对比基因和蛋白表达的一致性
   - 结合作者注释解读生物学意义

---

## 📚 参考

- **原始文献：** Janssens et al., Science Immunology 2023
- **数据集：** GSE228544
- **脚本版本：** 4.downstream_analysis.Rmd (modified 2026-01-19)
- **分支：** claude/analyze-cdc-annotations-qgyXf
