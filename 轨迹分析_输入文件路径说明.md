# 轨迹分析脚本 - 输入文件路径说明

## 📁 正确的文件路径结构

从 **4.downstream_analysis.Rmd** 运行后，输入文件会自动生成在：

```
项目根目录/
├─ data/
│  ├─ cDC1/
│  │  └─ Robjects/
│  │     └─ data_for_monocle3.rds    ← cDC1模式的输入文件
│  │
│  ├─ cDC2/
│  │  └─ Robjects/
│  │     └─ data_for_monocle3.rds    ← cDC2模式的输入文件
│  │
│  └─ integrated/
│     └─ Robjects/
│        └─ data_for_monocle3.rds    ← integrated模式的输入文件
```

## 🚀 使用方法

### 方法1：自动搜索（推荐）⭐

脚本会根据 `default_analysis_mode` 参数**自动搜索**对应的文件，无需手动复制！

```r
# 在脚本开头配置分析模式
default_analysis_mode <- "cDC1"  # 可选: "cDC1", "cDC2", "integrated"
```

脚本会自动搜索以下路径（按优先级）：

1. **data/{mode}/Robjects/data_for_monocle3.rds** （推荐，来自上游输出）
2. monocle3_analysis_{mode}/data/data_for_monocle3.rds （备用）
3. trajectory_analysis_{mode}/data/data_for_monocle3.rds （备用）

### 方法2：手动指定（可选）

如果数据文件在其他位置，可以手动复制到备用位置：

```bash
# 示例：cDC1模式
mkdir -p monocle3_analysis_cDC1/data
cp data/cDC1/Robjects/data_for_monocle3.rds monocle3_analysis_cDC1/data/

# 或
mkdir -p trajectory_analysis_cDC1/data
cp data/cDC1/Robjects/data_for_monocle3.rds trajectory_analysis_cDC1/data/
```

## 📊 完整工作流程

### 步骤1：运行上游脚本

```r
# 在 4.downstream_analysis.Rmd 中设置分析模式
analysis_mode <- "cDC1"

# Knit脚本，自动生成：
# data/cDC1/Robjects/data_for_monocle3.rds
```

### 步骤2：运行轨迹分析

#### 选项A：单一Monocle3脚本

```r
# 打开 5.monocle3_trajectory_analysis.Rmd
default_analysis_mode <- "cDC1"  # 设置模式

# Knit脚本
# 脚本自动搜索 data/cDC1/Robjects/data_for_monocle3.rds
```

#### 选项B：整合验证脚本

```r
# 打开 5.trajectory_analysis_integrated.Rmd
default_analysis_mode <- "cDC1"  # 设置模式

# Knit脚本
# 脚本自动搜索 data/cDC1/Robjects/data_for_monocle3.rds
```

### 步骤3：查看结果

```
monocle3_analysis_cDC1/     # 单一脚本输出
或
trajectory_analysis_cDC1/   # 整合脚本输出
├─ Plots/
├─ Tables/
└─ Robjects/
```

## 🔍 脚本如何搜索文件

两个轨迹分析脚本都使用相同的智能搜索逻辑：

```r
# 1. 根据 default_analysis_mode 构建路径
possible_modes <- c(default_analysis_mode, "cDC1", "cDC2", "integrated")

# 2. 按优先级搜索
for (mode in possible_modes) {
  # 优先搜索：data/{mode}/Robjects/data_for_monocle3.rds
  path <- file.path("data", mode, "Robjects", "data_for_monocle3.rds")
  if (file.exists(path)) {
    # 找到文件！
    input_file <- path
    break
  }
}

# 3. 如果没找到，尝试备用位置
# 4. 如果仍未找到，报错并提示正确路径
```

## ✅ 验证机制

脚本加载数据后会进行双重验证：

1. **路径模式**：从文件路径检测模式（如 `data/cDC1/...` → `cDC1`）
2. **数据模式**：从数据对象读取模式（`data_obj$analysis_mode`）

如果两者不一致，会发出警告并使用数据中的模式。

### 示例输出

```
搜索输入文件...
  检查: data/cDC1/Robjects/data_for_monocle3.rds ... ✓ 找到

✓ 输入文件: data/cDC1/Robjects/data_for_monocle3.rds
  检测到的模式: cDC1
  数据中的模式: cDC1
  ✓ 模式验证通过: cDC1
```

## ❓ 常见问题

### Q1: 如何同时分析三种模式？

**A**: 分别运行，每次修改 `default_analysis_mode`：

```bash
# 运行1：cDC1
default_analysis_mode <- "cDC1"
# Knit → 生成 monocle3_analysis_cDC1/

# 运行2：cDC2
default_analysis_mode <- "cDC2"
# Knit → 生成 monocle3_analysis_cDC2/

# 运行3：整合
default_analysis_mode <- "integrated"
# Knit → 生成 monocle3_analysis_integrated/
```

### Q2: 路径模式与数据模式不一致怎么办？

**A**: 这种情况会触发警告，脚本会优先使用**数据中的模式**。

例如：
```
输入文件: data/cDC1/Robjects/data_for_monocle3.rds
检测到的模式: cDC1
数据中的模式: cDC2
⚠ 警告：路径模式(cDC1) 与数据模式(cDC2) 不一致
→ 使用数据中的模式: cDC2
```

**建议**：确保文件路径与内容一致，避免混淆。

### Q3: 找不到输入文件怎么办？

**A**: 检查以下事项：

1. **确认上游脚本已运行**
   ```r
   # 运行 4.downstream_analysis.Rmd
   # 确保生成了 data/{mode}/Robjects/data_for_monocle3.rds
   ```

2. **检查文件是否存在**
   ```bash
   ls -la data/cDC1/Robjects/data_for_monocle3.rds
   ls -la data/cDC2/Robjects/data_for_monocle3.rds
   ls -la data/integrated/Robjects/data_for_monocle3.rds
   ```

3. **查看错误提示**
   ```
   ❌ 未找到 data_for_monocle3.rds！

   请确保文件位于以下位置之一：
     推荐位置（来自4.downstream_analysis.Rmd输出）：
       - data/cDC1/Robjects/data_for_monocle3.rds
       - data/cDC2/Robjects/data_for_monocle3.rds
       - data/integrated/Robjects/data_for_monocle3.rds
   ```

4. **手动复制到备用位置**（最后的选择）
   ```bash
   mkdir -p monocle3_analysis_cDC1/data
   cp data/cDC1/Robjects/data_for_monocle3.rds monocle3_analysis_cDC1/data/
   ```

### Q4: 可以更改输入文件路径吗？

**A**: 可以，但不推荐。如果必须更改，修改脚本中的搜索逻辑：

```r
# 在 load_data 代码块中添加自定义路径
custom_path <- "/your/custom/path/data_for_monocle3.rds"

if (file.exists(custom_path)) {
  input_file <- custom_path
}
```

## 📚 相关文档

- **上游脚本**：`4.downstream_analysis.Rmd` - 生成输入文件
- **单一分析**：`5.monocle3_trajectory_analysis.Rmd`
- **整合分析**：`5.trajectory_analysis_integrated.Rmd`
- **技术文档**：`5.Monocle3_轨迹分析_说明文档.md`
- **选择指南**：`轨迹分析_脚本选择指南.md`

---

**最后更新**：2026-01-20
**版本**：v1.1（修正输入路径说明）
